apiVersion: installer.confighub.com/v1alpha1
kind: Package
metadata:
  name: "18-external-dns-external-dns"
  version: "1.21.1"
spec:
  bases:
    - name: default
      path: base
      default: true
      description: "Imported Helm render for external-dns/external-dns"
