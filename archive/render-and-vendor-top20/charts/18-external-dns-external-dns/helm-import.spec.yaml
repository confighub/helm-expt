apiVersion: installer.confighub.com/v1alpha1
kind: HelmImportSpec
metadata:
  name: "18-external-dns-external-dns"
spec:
  chart:
    repositoryName: "external-dns"
    repositoryURL: "https://kubernetes-sigs.github.io/external-dns/"
    resolvedRepositoryURL: "https://kubernetes-sigs.github.io/external-dns/"
    name: "external-dns"
    version: "1.21.1"
  render:
    releaseName: "external-dns"
    namespace: "external-dns"
    kubeVersion: "1.30.0"
    apiVersions: []
    flags:
      - "--include-crds"
      - "--skip-tests"
      - "--no-hooks"
    valuesFiles:
      - values.yaml
