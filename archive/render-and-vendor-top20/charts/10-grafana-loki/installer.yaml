apiVersion: installer.confighub.com/v1alpha1
kind: Package
metadata:
  name: "10-grafana-loki"
  version: "7.0.0"
spec:
  bases:
    - name: default
      path: base
      default: true
      description: "Imported Helm render for grafana/loki"
