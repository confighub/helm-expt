apiVersion: installer.confighub.com/v1alpha1
kind: Package
metadata:
  name: "17-external-secrets-operator-external-secrets"
  version: "2.5.0"
spec:
  bases:
    - name: default
      path: base
      default: true
      description: "Imported Helm render for external-secrets-operator/external-secrets"
