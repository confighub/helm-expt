apiVersion: installer.confighub.com/v1alpha1
kind: Package
metadata:
  name: "12-hashicorp-vault"
  version: "0.32.0"
spec:
  bases:
    - name: default
      path: base
      default: true
      description: "Imported Helm render for hashicorp/vault"
