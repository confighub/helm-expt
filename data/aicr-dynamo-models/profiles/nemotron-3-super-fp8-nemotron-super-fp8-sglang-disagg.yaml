apiVersion: "catalog.confighub.com/v1alpha1"
kind: "DynamoModelProfileRecord"
metadata:
  name: "nemotron-3-super-fp8-nemotron-super-fp8-sglang-disagg"
spec:
  description: "A generated model profile for a retained DynamoGraphDeployment recipe from NVIDIA's ai-dynamo/dynamo recipes corpus, model directory \"nemotron-3-super-fp8\". It requests 4 GPUs (best-effort). This record is configuration data about the recipe; it grants nothing and fetches nothing."
  delivery: "dynamo"
  dynamoGraphDeployment: "nemotron-super-fp8-sglang-disagg"
  recipeModel: "nemotron-3-super-fp8"
  dynamoGraphDeploymentApiVersion: "nvidia.com/v1alpha1"
  sourceFile: "data/aicr-dynamo-models/upstream/recipes/nemotron-3-super-fp8/sglang/disagg/deploy.yaml"
  sourceSha256: "d29102b811ecba9554d27aea324ba2c5b82e6927f5fc55c6572b455cbd24c01f"
  backend: "sglang"
  backendSource: "spec.backendFramework"
  pattern: "disagg"
  patternSource: "path"
  model: "nvidia/NVIDIA-Nemotron-3-Super-120B-A12B-FP8"
  images:
    - "nvcr.io/nvidia/ai-dynamo/sglang-runtime:1.3.0"
  pinned: true
  pinnedNote: null
  gpuRequests: 4
  gpuRequestsNote: "best-effort: each service/component's own GPU resource request (its limit, if no request is set) multiplied by that unit's replicas, summed across the recipe; 0 when no GPU resource field was found."
  secretNames:
    - "hf-token-secret"
  accelerator: null
  acceleratorNote: "best-effort and informational only: a GPU-product nodeSelector or node-affinity match found anywhere in this recipe, or null if none is present. Not used to gate platform attachment -- see data/aicr-dynamo-models/summary.md."
  governingTerms:
    modelSource: "the model is referenced by its HuggingFace repository name only; this generator never fetched model weights and never read a model license."
    huggingFaceModelPage: "https://huggingface.co/nvidia/NVIDIA-Nemotron-3-Super-120B-A12B-FP8"
    governingTermsReadAt: null
    governingTermsNamed:
      []
    governingTerms: "This recipe's model has not had its HuggingFace license read. Read it at https://huggingface.co/nvidia/NVIDIA-Nemotron-3-Super-120B-A12B-FP8 before deploying this recipe; the model's own license governs, not this catalog."
  notes:
    []
status:
  result: "described-offline-terms-pending"
  modelFetched: false
  imagePulled: false
