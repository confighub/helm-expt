apiVersion: "catalog.confighub.com/v1alpha1"
kind: "DynamoModelProfileRecord"
metadata:
  name: "qwen3-5-122b-qwen35-122b-disagg-b200-agentic"
spec:
  description: "A generated model profile for a retained DynamoGraphDeployment recipe from NVIDIA's ai-dynamo/dynamo recipes corpus, model directory \"qwen3.5-122b\". It requests 3 GPUs (best-effort). This record is configuration data about the recipe; it grants nothing and fetches nothing."
  delivery: "dynamo"
  dynamoGraphDeployment: "qwen35-122b-disagg-b200-agentic"
  recipeModel: "qwen3.5-122b"
  dynamoGraphDeploymentApiVersion: "nvidia.com/v1beta1"
  sourceFile: "data/aicr-dynamo-models/upstream/recipes/qwen3.5-122b/nvfp4/vllm/disagg-b200-agentic/deploy.yaml"
  sourceSha256: "1c6d864d6709765a8e73d163c0c87b9aa2c96da2cdc580e22b965285ba39fbda"
  backend: "vllm"
  backendSource: "spec.backendFramework"
  pattern: "disagg-b200-agentic"
  patternSource: "path"
  model: "nvidia/Qwen3.5-122B-A10B-NVFP4"
  images:
    - "nvcr.io/nvidia/ai-dynamo/vllm-runtime:1.3.0"
  pinned: true
  pinnedNote: null
  gpuRequests: 3
  gpuRequestsNote: "best-effort: each service/component's own GPU resource request (its limit, if no request is set) multiplied by that unit's replicas, summed across the recipe; 0 when no GPU resource field was found."
  secretNames:
    - "hf-token-secret"
  accelerator:
    - "NVIDIA-B200"
  acceleratorNote: "best-effort and informational only: a GPU-product nodeSelector or node-affinity match found anywhere in this recipe, or null if none is present. Not used to gate platform attachment -- see data/aicr-dynamo-models/summary.md."
  governingTerms:
    modelSource: "the model is referenced by its HuggingFace repository name only; this generator never fetched model weights and never read a model license."
    huggingFaceModelPage: "https://huggingface.co/nvidia/Qwen3.5-122B-A10B-NVFP4"
    governingTermsReadAt: null
    governingTermsNamed:
      []
    governingTerms: "This recipe's model has not had its HuggingFace license read. Read it at https://huggingface.co/nvidia/Qwen3.5-122B-A10B-NVFP4 before deploying this recipe; the model's own license governs, not this catalog."
  notes:
    []
status:
  result: "described-offline-terms-pending"
  modelFetched: false
  imagePulled: false
