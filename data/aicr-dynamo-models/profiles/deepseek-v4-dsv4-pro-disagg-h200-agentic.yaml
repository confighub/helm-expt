apiVersion: "catalog.confighub.com/v1alpha1"
kind: "DynamoModelProfileRecord"
metadata:
  name: "deepseek-v4-dsv4-pro-disagg-h200-agentic"
spec:
  description: "A generated model profile for a retained DynamoGraphDeployment recipe from NVIDIA's ai-dynamo/dynamo recipes corpus, model directory \"deepseek-v4\". It requests 32 GPUs (best-effort). This record is configuration data about the recipe; it grants nothing and fetches nothing."
  delivery: "dynamo"
  dynamoGraphDeployment: "dsv4-pro-disagg-h200-agentic"
  recipeModel: "deepseek-v4"
  dynamoGraphDeploymentApiVersion: "nvidia.com/v1beta1"
  sourceFile: "data/aicr-dynamo-models/upstream/recipes/deepseek-v4/deepseek-v4-pro/vllm/disagg-h200-agentic/deploy.yaml"
  sourceSha256: "fab26fdfea4bfcc6e05631dfc2cfb4051eb8acedcd2308f14606173c6c2d9585"
  backend: "vllm"
  backendSource: "spec.backendFramework"
  pattern: "disagg-h200-agentic"
  patternSource: "path"
  model: "deepseek-ai/DeepSeek-V4-Pro"
  images:
    - "nvcr.io/nvidia/ai-dynamo/vllm-runtime:1.3.0"
  pinned: true
  pinnedNote: null
  gpuRequests: 32
  gpuRequestsNote: "best-effort: each service/component's own GPU resource request (its limit, if no request is set) multiplied by that unit's replicas, summed across the recipe; 0 when no GPU resource field was found."
  secretNames:
    - "hf-token-secret"
  accelerator:
    - "NVIDIA-H200"
  acceleratorNote: "best-effort and informational only: a GPU-product nodeSelector or node-affinity match found anywhere in this recipe, or null if none is present. Not used to gate platform attachment -- see data/aicr-dynamo-models/summary.md."
  governingTerms:
    modelSource: "the model is referenced by its HuggingFace repository name only; this generator never fetched model weights and never read a model license."
    huggingFaceModelPage: "https://huggingface.co/deepseek-ai/DeepSeek-V4-Pro"
    governingTermsReadAt: null
    governingTermsNamed:
      []
    governingTerms: "This recipe's model has not had its HuggingFace license read. Read it at https://huggingface.co/deepseek-ai/DeepSeek-V4-Pro before deploying this recipe; the model's own license governs, not this catalog."
  notes:
    []
status:
  result: "described-offline-terms-pending"
  modelFetched: false
  imagePulled: false
