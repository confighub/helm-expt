# Default-credential check — does a chart's default base ship a fixed shared password?

**UNOFFICIAL/EXPERIMENTAL.** Live receipt generated by `scripts/run-default-credential-check.mjs`; do not hand-edit. Regenerate with `npm run default-credential:check`.

**Claim.** Helm generates a RANDOM password per install. cub renders deterministically and ships an auth Secret in out/secrets/ — but for these default bases the value is FIXED (identical across namespaces, committed in the package, e.g. `confighub-redis-password`). That is a shared, well-known default credential, not a generated one — convenient for demos, a footgun deployed unchanged. Bases named `static-passwords` that ship a fixed value are misleadingly named.

Helm generates a **random** password per install. cub renders **deterministically** (good for config-as-data) and ships the auth Secret in `out/secrets/` — but where that value is **identical for every install** (same across namespaces, committed in the package), it is a **shared default credential**. Found by rendering each default base with two namespaces and comparing the Secret values.

**Default bases shipping a FIXED credential: 5/12 (42%). Misleadingly-named (`static-passwords` yet fixed): 4.** Overall: **watch**.

## The fixed default credentials

| Chart | Default base | Fixed Secret(s) — placeholder shown where committed |
| --- | --- | --- |
| bitnami/redis | default | `redis/redis-password` = `confighub-redis-password` |
| bitnami/mysql | static-passwords ⚠️ misleading | `mysql/mysql-password` = `confighub-mysql-user-password`; `mysql/mysql-root-password` = `confighub-mysql-root-password` |
| bitnami/mongodb | static-passwords ⚠️ misleading | `mongodb/mongodb-root-password` = `confighub-mongodb-root-password` |
| bitnami/rabbitmq | static-passwords ⚠️ misleading | `rabbitmq/rabbitmq-password` = `confighub-rabbitmq-password` |
| bitnami/postgresql | static-passwords ⚠️ misleading | `postgresql/postgres-password` = `confighub-postgres-password` |

This is a footgun, not a vulnerability per se: the values are committed placeholders meant for render-parity/demos, and a `reuse-existing-secret` base exists for production. The honest fixes: (1) **don't name a fixed-value base `static-passwords`** — it implies generation; (2) surface, at install time, that the default base ships a known placeholder credential that must be replaced before production; (3) consider per-install entropy for the default base so two installs don't share a password.

## Per chart

| Chart | Status |
| --- | --- |
| bitnami/nginx/24.0.2@http-clusterip | no-secret |
| bitnami/redis@default | fixed-credential |
| bitnami/mysql@static-passwords | fixed-credential (misleading name) |
| bitnami/mongodb@static-passwords | fixed-credential (misleading name) |
| bitnami/rabbitmq@static-passwords | fixed-credential (misleading name) |
| bitnami/postgresql@static-passwords | fixed-credential (misleading name) |
| bitnami/apache@default | no-secret |
| bitnami/memcached@default | no-secret |
| bitnami/zookeeper@default | no-secret |
| bitnami/contour@default | no-secret |
| bitnami/opensearch@default | no-secret |
| bitnami/phpmyadmin@default | no-secret |

- Companion to the cub-installer fuzz's namespace findings — this is the **secret** rough edge. Receipt: `runs/default-credential-check/receipt.yaml`.
