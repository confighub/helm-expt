apiVersion: helm-expt.confighub.com/v1alpha1
kind: HelmCatalogDemoReadme
metadata:
  name: readme
  labels:
    app.kubernetes.io/part-of: helm-catalog
    helm-expt.confighub.com/space: "route-sketch-kube-prometheus-stack"
    helm-expt.confighub.com/readme-kind: "route"
spec:
  space: "route-sketch-kube-prometheus-stack"
  title: "Kube Prometheus Stack route sketch"
  summary: "A design sketch for handling the work around a complex chart: CRDs, setup jobs, webhook certificates, target facts, and receipts."
  sourcePath: "data/helm-catalog-readmes/spaces/route-sketch-kube-prometheus-stack/README.md"
  links:
    - label: "Kube Prometheus Stack chart page"
      url: "https://confighub.github.io/helm-expt/site/charts/prometheus-community-kube-prometheus-stack-85-3-3.html"
    - label: "CRD ordering proof"
      url: "https://github.com/confighub/helm-expt/blob/main/runs/crd-ordering-gap/receipt.yaml"
    - label: "How it works"
      url: "https://confighub.github.io/helm-expt/site/how-it-works.html"
  markdown: |-
    <!-- Generated by npm run helm-catalog-readmes. Do not edit by hand. -->

    # Kube Prometheus Stack route sketch

    A design sketch for handling the work around a complex chart: CRDs, setup jobs, webhook certificates, target facts, and receipts.

    Start here when you open this Space in Hub. This page explains what the example is for, what to inspect, why it exists, and where the evidence lives.

    ## What to look at

    - Large Helm charts often need more than a rendered Deployment and Service.
    - This example names seven lifecycle routes so the extra work is reviewable instead of hidden in an install.
    - Routes stay marked `automatic: false` until a live proof earns automation.

    ## Open in Hub

    - This README.
    - Lifecycle route YAML.
    - The Kube Prometheus Stack chart page.

    ## Why it exists

    Use this Space to see how we handle the hard parts around a Helm chart without pretending there is one universal answer.

    We keep the chart, then write down the chart-specific choices for CRDs, hooks, certificates, setup jobs, and target facts. Each choice can be tested, reused, and improved instead of rediscovered by every team.

    ## Evidence and source

    - [Kube Prometheus Stack chart page](https://confighub.github.io/helm-expt/site/charts/prometheus-community-kube-prometheus-stack-85-3-3.html)
    - [CRD ordering proof](https://github.com/confighub/helm-expt/blob/main/runs/crd-ordering-gap/receipt.yaml)
    - [How it works](https://confighub.github.io/helm-expt/site/how-it-works.html)

    ## Limits

    - This is a sketch Space. It names the planned work but does not claim every route is automated today.
