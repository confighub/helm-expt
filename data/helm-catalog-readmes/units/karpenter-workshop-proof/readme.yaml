apiVersion: helm-expt.confighub.com/v1alpha1
kind: HelmCatalogDemoReadme
metadata:
  name: readme
  labels:
    app.kubernetes.io/part-of: helm-catalog
    helm-expt.confighub.com/space: "karpenter-workshop-proof"
    helm-expt.confighub.com/readme-kind: "environment"
spec:
  space: "karpenter-workshop-proof"
  title: "Karpenter: workshop configuration"
  summary: "This Space starts from `karpenter-base` and records the settings used by the EKS inference workshop. The workshop runs in ConfigHub without creating AWS or Kubernetes resources."
  sourcePath: "data/helm-catalog-readmes/spaces/karpenter-workshop-proof/README.md"
  links:
    - label: "Exact source and package receipt"
      url: "https://github.com/confighub/helm-expt/blob/main/data/certified-bundles/receipts/eks-inference/karpenter/receipt.yaml"
    - label: "Eight-component EKS inference guide"
      url: "https://github.com/confighub/helm-expt/blob/main/data/certified-bundles/eks-inference-stack.md"
    - label: "ConfigHub sandbox result"
      url: "https://github.com/confighub/helm-expt/blob/main/data/eks-inference-sandbox-proof/summary.md"
  markdown: |-
    <!-- Generated by npm run helm-catalog-readmes. Do not edit by hand. -->

    # Karpenter: workshop configuration

    This Space starts from `karpenter-base` and records the settings used by the EKS inference workshop. The workshop runs in ConfigHub without creating AWS or Kubernetes resources.

    Start here when you open this Space in Hub. This page explains the problem this example is meant to show, what to inspect, why it matters, and where the evidence lives.

    ## Why this example exists

    A reusable source still needs the AWS, cluster, network, and workload settings for its destination.

    This Space records those settings separately from the public base. A reviewer can inspect the changes without rebuilding the source.

    ## What this example shows

    - All 4 Units retain an upstream link to `karpenter-base`.
    - 8 destination fields are filled through 2 links from the shared platform profile.
    - The placeholder check found 0 unfinished values.
    - ConfigHub published release OCI #1 with manifest digest `sha256:dda652ea53c443d0c81137a1696e22429e145fa89e15610307263f16fea3c937`.

    ## Where each setting comes from

    | Place | What this Space records |
    | --- | --- |
    | Starting configuration | This Space is derived from `karpenter-base`, which is pinned to `oci://ghcr.io/confighub/configs/eks-inference/karpenter@sha256:d182fce8c942d0a40497b1d13ec067e4707c0c31027fc049d256afab01b1814e`. |
    | ConfigHub changes | 8 destination fields are filled through 2 links from the shared platform profile. |
    | Install work | Delivery must also handle installing CRDs before the objects that use them. |
    | Live cluster | The workshop checked ConfigHub records and OCI publication only. It did not create AWS, EKS, GPU, or model-serving resources. |

    If an upstream change and a local ConfigHub revision touch the same field, review the overlap before promotion.

    ## What to inspect in Hub

    - This README.
    - Unit revision history to see the fields supplied for the workshop.
    - `karpenter-base` to inspect the unchanged public starting configuration.

    ## Evidence and source

    - [Exact source and package receipt](https://github.com/confighub/helm-expt/blob/main/data/certified-bundles/receipts/eks-inference/karpenter/receipt.yaml)
    - [Eight-component EKS inference guide](https://github.com/confighub/helm-expt/blob/main/data/certified-bundles/eks-inference-stack.md)
    - [ConfigHub sandbox result](https://github.com/confighub/helm-expt/blob/main/data/eks-inference-sandbox-proof/summary.md)

    ## Limits

    - A published OCI is a configuration result, not proof that Argo CD, Flux, Kubernetes, a GPU, or a model consumed it.
