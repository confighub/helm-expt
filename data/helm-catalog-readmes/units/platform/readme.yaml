apiVersion: helm-expt.confighub.com/v1alpha1
kind: HelmCatalogDemoReadme
metadata:
  name: readme
  labels:
    app.kubernetes.io/part-of: helm-catalog
    helm-expt.confighub.com/space: "platform"
    helm-expt.confighub.com/readme-kind: "org"
spec:
  space: "platform"
  title: "Shared checks and gates for the demo org"
  summary: "Shared platform plumbing used by the demo Spaces: checks, gates, and filters that keep the examples honest."
  sourcePath: "data/helm-catalog-readmes/spaces/platform/README.md"
  links:
    - label: "Gate-scope fix in org summary"
      url: "https://github.com/confighub/helm-expt/blob/main/data/helm-org/summary.md"
    - label: "Verification landing page"
      url: "https://confighub.github.io/helm-expt/site/verification.html"
  markdown: |-
    <!-- Generated by npm run helm-catalog-readmes. Do not edit by hand. -->
    
    # Shared checks and gates for the demo org
    
    Shared platform plumbing used by the demo Spaces: checks, gates, and filters that keep the examples honest.
    
    Use this README as the first stop inside Hub. It explains what this Space is for, what to open, why it matters, and where the evidence lives.
    
    ## What this Space shows
    
    - Production Spaces carry approval gates. Non-production Spaces do not pretend to have a human approval workflow.
    - The demo uses checks such as placeholder detection and vetting to keep unsafe examples out of the happy path.
    
    ## What to open in Hub
    
    - The `readme` Unit.
    - Trigger and filter Units that name the checks applied to other Spaces.
    - Production Spaces such as `bitnami-redis-prod` and `hashicorp-vault-env-prod` to see where gates matter.
    
    ## Why this matters
    
    A catalog is not only rendered YAML. It also needs the rules that say when a change is safe to move.
    
    The platform Space shows those rules separately so chart examples do not hide policy decisions inside prose.
    
    ## Evidence and source
    
    - [Gate-scope fix in org summary](https://github.com/confighub/helm-expt/blob/main/data/helm-org/summary.md)
    - [Verification landing page](https://confighub.github.io/helm-expt/site/verification.html)
    
    ## Limits
    
    - This Space explains shared demo mechanics. It is not a replacement for a company policy model.
