apiVersion: helm-expt.confighub.com/v1alpha1
kind: HelmCatalogDemoReadme
metadata:
  name: readme
  labels:
    app.kubernetes.io/part-of: helm-catalog
    helm-expt.confighub.com/space: "bitnami-redis-base"
    helm-expt.confighub.com/readme-kind: "environment"
spec:
  space: "bitnami-redis-base"
  title: "Redis base application"
  summary: "The Redis starting point for the environment promotion demo."
  sourcePath: "data/helm-catalog-readmes/spaces/bitnami-redis-base/README.md"
  links:
    - label: "Redis chart page"
      url: "https://confighub.github.io/helm-expt/site/charts/bitnami-redis-25-5-3.html"
    - label: "Benchmark: upgrade keeps edit"
      url: "https://github.com/confighub/helm-expt/blob/main/data/pilot-benchmark/task1-upgrade-keeps-edit.md"
    - label: "Org summary"
      url: "https://github.com/confighub/helm-expt/blob/main/data/helm-org/summary.md"
  markdown: |-
    <!-- Generated by npm run helm-catalog-readmes. Do not edit by hand. -->

    # Redis base application

    The Redis starting point for the environment promotion demo.

    Start here when you open this Space in Hub. This page explains what the example is for, what to inspect, why it exists, and where the evidence lives.

    ## What to look at

    - A chart render can become an application base instead of a one-off install.
    - The base receives upstream chart refreshes. Environment Spaces can then take those refreshes without losing their local edits.
    - This is the beginning of the 25.5.3 to 27.0.0 Redis upgrade story.

    ## Open in Hub

    - This README.
    - The Redis workload YAML to see the captured render.
    - `bitnami-redis-staging` and `bitnami-redis-prod` to see environment versions.

    ## Why it exists

    Helm can install Redis. The harder operations problem is what happens after install, when teams create staging and production versions and later upgrade the chart.

    This example keeps the base chart output and the later environment changes separate enough to audit.

    ## Evidence and source

    - [Redis chart page](https://confighub.github.io/helm-expt/site/charts/bitnami-redis-25-5-3.html)
    - [Benchmark: upgrade keeps edit](https://github.com/confighub/helm-expt/blob/main/data/pilot-benchmark/task1-upgrade-keeps-edit.md)
    - [Org summary](https://github.com/confighub/helm-expt/blob/main/data/helm-org/summary.md)

    ## Limits

    - The Redis demo is a worked example, not a claim that every Redis values combination has been proven.
