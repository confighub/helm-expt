apiVersion: helm-expt.confighub.com/v1alpha1
kind: HelmCatalogDemoReadme
metadata:
  name: readme
  labels:
    app.kubernetes.io/part-of: helm-catalog
    helm-expt.confighub.com/space: "bitnami-redis-27-0-0-default-pilot-live-20260705"
    helm-expt.confighub.com/readme-kind: "pilot"
spec:
  space: "bitnami-redis-27-0-0-default-pilot-live-20260705"
  title: "Redis 27.0.0 default pilot run"
  summary: "A live pilot snapshot from the Redis 25.5.3 to 27.0.0 upgrade test for the default lane."
  sourcePath: "data/helm-catalog-readmes/spaces/bitnami-redis-27-0-0-default-pilot-live-20260705/README.md"
  links:
    - label: "Benchmark: upgrade keeps edit"
      url: "https://github.com/confighub/helm-expt/blob/main/data/pilot-benchmark/task1-upgrade-keeps-edit.md"
  markdown: |-
    <!-- Generated by npm run helm-catalog-readmes. Do not edit by hand. -->

    # Redis 27.0.0 default pilot run

    A live pilot snapshot from the Redis 25.5.3 to 27.0.0 upgrade test for the default lane.

    Start here when you open this Space in Hub. This page explains what the example is for, what to inspect, why it exists, and where the evidence lives.

    ## What to look at

    - The chart upgrade was tested against a real throwaway cluster.
    - The important question was whether a local operations edit survived the chart upgrade.
    - The result was recorded so someone can inspect the run later instead of trusting a demo claim.

    ## Open in Hub

    - This README.
    - Redis workload YAML.
    - Revision history for the changed YAML.

    ## Why it exists

    This example makes the pilot concrete. It is easier to trust the story when the actual run has a place in the org.

    The lesson is practical: keep the Helm chart, but record the change so an upgrade does not depend on remembering the right flag.

    ## Evidence and source

    - [Benchmark: upgrade keeps edit](https://github.com/confighub/helm-expt/blob/main/data/pilot-benchmark/task1-upgrade-keeps-edit.md)

    ## Limits

    - This is a dated pilot snapshot from 2026-07-05.
