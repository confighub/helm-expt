apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "InstallGate"
metadata:
  name: "nginx-http-clusterip-r001"
spec:
  variantRevision: "../variant-revision.yaml"
  renderedObjectSetSHA256: "5c175126acf3b7abd9fce0c17791cfce7236eac8f51e9781d20814cad14b0bac"
  decision: "warn"
  allowedScopes:
    - "local-test"
  blockedScopes:
    - "production"
  reasons:
    - "Helm equivalence passed for http-clusterip"
    - "Default generated TLS is either disabled or replaced by explicit target Secret requirements"
    - "Helm hook behavior needs explicit lifecycle policy before production"
    - "NGINX deployment, NetworkPolicy, PDB, service exposure, ingress, and extension slots need production review"
    - "avoids chart-generated TLS material and renders a simple internal NGINX service deterministically"
