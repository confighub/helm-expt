apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "InstallGate"
metadata:
  name: "postgresql-generated-passwords-r001"
spec:
  variantRevision: "../variant-revision.yaml"
  renderedObjectSetSHA256: "53745953b073f2a42a1833dbc4808b82463894c50405610e7dde62915550cb40"
  decision: "warn"
  allowedScopes:
    - "local-test"
  blockedScopes:
    - "production"
  reasons:
    - "Helm equivalence passed for generated-passwords"
    - "Generated credential handling needs explicit generated fact or target fact policy before production"
    - "Helm hook behavior needs explicit lifecycle policy before production"
    - "PostgreSQL StatefulSet and volumeClaimTemplates need storage, upgrade, and rollback policy"
    - "uses a persisted generated fact for auth.postgresPassword and renders the chart Secret deterministically"
