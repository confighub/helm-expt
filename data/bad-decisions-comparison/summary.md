# Helm's footgun profile — when does a bad `--set` get caught?

**UNOFFICIAL/EXPERIMENTAL.** Live receipt generated by `scripts/run-bad-decisions-comparison.mjs`; do not hand-edit. Regenerate with `npm run bad-decisions:comparison`.

**Claim.** HELM's footgun timing profile (measured): where a careless --set value gets caught — at render, only at the live k8s API at apply, or never (silent). This measures HELM, not cub; whether OUR tool does better with the same kind of input is measured by the cub-installer fuzz (data/cub-installer-fuzz/), where an unknown input is a hard error and cub's own rough edges are surfaced.

> **This page measures HELM, not cub.** Whether *our* tool does better with the same kind of
> input is measured separately — see the **[cub-installer fuzz](../cub-installer-fuzz/summary.md)**,
> where an unknown input is a hard error (the footgun Helm absorbs) and cub's own rough edges
> are surfaced. We test our own tool rather than score points off Helm's.

The [careless-dev fuzz](../bad-decisions-fuzz/summary.md) showed Helm's *outcomes*. This runs the **same 180 decisions** (18 types × 10 charts, one corpus in `scripts/lib/bad-decisions.mjs`) and asks **when Helm surfaces each** — before the cluster, only at the live API, or never.

| When does Helm surface the bad decision? | Raw Helm (`--set` → install / Argo-helm) |
| --- | --- |
| **Before the cluster** (at render) | render-rejected only — **2 (1%)** |
| **Only at the live k8s API** (at apply) | leaked — **60 (33%)** |
| **Never** — silent success | absorbed — **118 (66%)** |

Overall: **pass** (0 unclassified). **Headline:** for these 180 careless decisions, raw Helm catches only **1%** before the cluster — the rest reach the live k8s API (33%) or vanish silently (66%).

## On the config-as-data claim (honest)

It is tempting to say "config-as-data surfaces all of these at review." That is a **model property, not a measurement** — an earlier draft of this page overstated it as a cub result. cub renders with the same engine, so the rejected/leaked/absorbed split (2/60/118) is identical to Helm's. The *measured* difference is in the [cub-installer fuzz](../cub-installer-fuzz/summary.md): cub's inputs are declared, so the unknown-key footgun is a hard error — and cub has its own rough edges (it does not validate namespace format or length). Measured, not asserted.

## Per chart

| Chart | Cases | Helm outcome |
| --- | --- | --- |
| bitnami/nginx@25.0.0 | 18 | 0 rejected · 11 leaked · 7 absorbed |
| bitnami/redis@27.0.0 | 18 | 0 rejected · 1 leaked · 17 absorbed |
| bitnami/mysql@14.0.3 | 18 | 0 rejected · 1 leaked · 17 absorbed |
| bitnami/mongodb@19.1.0 | 18 | 0 rejected · 11 leaked · 7 absorbed |
| bitnami/rabbitmq@16.0.14 | 18 | 1 rejected · 11 leaked · 6 absorbed |
| fluent/fluent-bit@0.57.6 | 18 | 0 rejected · 10 leaked · 8 absorbed |
| prometheus-community/prometheus@29.9.0 | 18 | 0 rejected · 0 leaked · 18 absorbed |
| grafana/grafana@10.5.15 | 18 | 0 rejected · 8 leaked · 10 absorbed |
| ingress-nginx/ingress-nginx@4.15.1 | 18 | 0 rejected · 0 leaked · 18 absorbed |
| metrics-server/metrics-server@3.13.0 | 18 | 1 rejected · 7 leaked · 10 absorbed |

- Receipt: `runs/bad-decisions-comparison/receipt.yaml`. Corpus: `scripts/lib/bad-decisions.mjs` (shared with the fuzz).
