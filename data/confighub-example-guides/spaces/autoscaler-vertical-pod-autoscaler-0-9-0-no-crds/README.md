<!-- Generated by npm run confighub-example-guides. Do not edit by hand. -->

# autoscaler/vertical-pod-autoscaler 0.9.0 - no-crds

This guide follows one public Helm chart into a ConfigHub Space. It covers why the preset exists, how to repeat it, and what still needs care.

It is generated from the same records that build the package, the chart page and the render intent, along with the scripts and receipts. The proof links are lower down.

## Why this preset exists

A Helm chart exposes many settings, and a values file records the settings rather than the operations story around them. A team still has to know which Kubernetes objects get created and which Secrets or CRDs have to exist first. Hooks and setup jobs may need special handling, and something has to back the result.

This preset is a named answer to one operating choice. It keeps the upstream chart, records the inputs and the rendered YAML, and leaves the team a repeatable starting point instead of a private values-file guess.

## What this is

This is the `no-crds` preset config for `autoscaler/vertical-pod-autoscaler@0.9.0`. The repo also calls this a base variant. Use this when your platform, GitOps bootstrap, or another chart owns the CRDs.

The matching catalog page is [autoscaler/vertical-pod-autoscaler@0.9.0](https://confighub.github.io/helm-expt/site/charts/autoscaler-vertical-pod-autoscaler-0-9-0.html).

## The chart journey

We keep the Helm chart. We lock `autoscaler/vertical-pod-autoscaler@0.9.0` and choose the `no-crds` preset config, then render it with the recorded values, namespace and release name against the recorded Kubernetes capabilities, and save the output as files.

That captured output is the render variant: [`recipes/autoscaler/vertical-pod-autoscaler/0.9.0/revisions/no-crds/r001/rendered/release-objects.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/autoscaler/vertical-pod-autoscaler/0.9.0/revisions/no-crds/r001/rendered/release-objects.yaml). It contains 37 Kubernetes object(s): ClusterRole x11, ClusterRoleBinding x11, Deployment x3, PodDisruptionBudget x3, ServiceAccount x3, Role x2, RoleBinding x2, MutatingWebhookConfiguration x1.

The public package is `oci://europe-west1-docker.pkg.dev/nth-fort-499605-q5/helm-expt/autoscaler-vertical-pod-autoscaler:0.9.0@sha256:1ee72dbefe5df3af05372c9e565302b3dfbdd4f6e91e8f116b2b6042764267e7`. Users can pull these exact bytes without cloning this repo. When someone runs `cub installer upload`, ConfigHub stores the rendered Kubernetes YAML in a Space, where it can be searched and compared, reviewed and changed, then delivered. The example script defaults to Space `helm-vertical-pod-autoscaler-no-crds`, but users can choose another name with `CUB_SPACE=...`.

## What to check

This preset config records 1 prerequisite: 1 Secret. Follow the instructions below before you apply the rendered objects.

The catalog does not currently record a separate hook, setup job, or cleanup step for this preset.

CRDs are made into an explicit choice instead of being mixed into the application install. CRD ownership is recorded as part of the preset config. At least one Secret must be created with your values before apply.

## Why you can trust it

- The chart version, source, namespace, release name, values, and capability profile are recorded in the render intent.
- The render variant is committed as YAML and contains 37 Kubernetes object(s).
- The installer package OCI ref points to the package users pull for this chart version.
- Render parity is recorded as passing for this preset config.
- Prerequisites are named before apply, so they are not discovered after rollout.

This claim covers this recorded preset config alone. Every other values file for this chart remains unchecked.

## Repeat it

Fast path with no ConfigHub account:

```sh
bash <(curl -fsSL https://confighub.github.io/helm-expt/site/sh/autoscaler-vertical-pod-autoscaler-0-9-0/no-crds/try.sh)
```

Fast path with a ConfigHub account:

```sh
bash <(curl -fsSL https://confighub.github.io/helm-expt/site/sh/autoscaler-vertical-pod-autoscaler-0-9-0/no-crds/confighub.sh)
```

The core render command is:

```sh
cub installer setup --pull oci://europe-west1-docker.pkg.dev/nth-fort-499605-q5/helm-expt/autoscaler-vertical-pod-autoscaler:0.9.0@sha256:1ee72dbefe5df3af05372c9e565302b3dfbdd4f6e91e8f116b2b6042764267e7 --base no-crds --work-dir ./autoscaler-vertical-pod-autoscaler-0-9-0-no-crds --non-interactive --namespace default
```

After upload, create environment versions with `cub variant create` and move reviewed changes with `cub variant promote`. The walkthrough is [After upload: create a variant and promote changes](../../../../docs/user/variants-after-upload.md).

## Preset details

| Item | Value |
| --- | --- |
| Chart | `autoscaler/vertical-pod-autoscaler@0.9.0` |
| Preset config | `no-crds` |
| Namespace | `default` |
| Release name | `vertical-pod-autoscaler` |
| Values | [`recipes/autoscaler/vertical-pod-autoscaler/0.9.0/effective-values-no-crds.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/autoscaler/vertical-pod-autoscaler/0.9.0/effective-values-no-crds.yaml) |
| Render intent | [`data/helm-render-intents/intents/autoscaler-vertical-pod-autoscaler-0-9-0-no-crds.yaml`](https://github.com/confighub/helm-expt/blob/main/data/helm-render-intents/intents/autoscaler-vertical-pod-autoscaler-0-9-0-no-crds.yaml) |
| Render variant | [`recipes/autoscaler/vertical-pod-autoscaler/0.9.0/revisions/no-crds/r001/rendered/release-objects.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/autoscaler/vertical-pod-autoscaler/0.9.0/revisions/no-crds/r001/rendered/release-objects.yaml) |
| Package base | [`packages/autoscaler/vertical-pod-autoscaler/0.9.0/bases/no-crds`](https://github.com/confighub/helm-expt/tree/main/packages/autoscaler/vertical-pod-autoscaler/0.9.0/bases/no-crds) |
| Scripts | [try.sh](https://confighub.github.io/helm-expt/site/sh/autoscaler-vertical-pod-autoscaler-0-9-0/no-crds/try.sh) · [confighub.sh](https://confighub.github.io/helm-expt/site/sh/autoscaler-vertical-pod-autoscaler-0-9-0/no-crds/confighub.sh) |

## Prerequisites and lifecycle steps

| When | What | How it is handled |
| --- | --- | --- |
| Before install | ClusterFeature: Secret default/vpa-tls-certs keys ca,cert,key | Run the chart certgen lifecycle action or stage a valid vpa-tls-certs Secret before waiting for the admission controller |

## Evidence

| Check | Status |
| --- | --- |
| Render parity | `yes` |
| ConfigHub scan/upload proof | `yes` |
| Earlier local-cluster test | `yes` |
| GitOps OCI live run | `yes` |
| Live Helm vs ConfigHub comparison | `yes` |
| Lifecycle routes | `0` |

## Limits

- Do not present as a catalog-supported chart until promotion review and support decisions are recorded.

## Source files

- Chart page: [https://confighub.github.io/helm-expt/site/charts/autoscaler-vertical-pod-autoscaler-0-9-0.html](https://confighub.github.io/helm-expt/site/charts/autoscaler-vertical-pod-autoscaler-0-9-0.html)
- Render intent: [`data/helm-render-intents/intents/autoscaler-vertical-pod-autoscaler-0-9-0-no-crds.yaml`](https://github.com/confighub/helm-expt/blob/main/data/helm-render-intents/intents/autoscaler-vertical-pod-autoscaler-0-9-0-no-crds.yaml)
- Rendered YAML: [`recipes/autoscaler/vertical-pod-autoscaler/0.9.0/revisions/no-crds/r001/rendered/release-objects.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/autoscaler/vertical-pod-autoscaler/0.9.0/revisions/no-crds/r001/rendered/release-objects.yaml)
- Package source: [`packages/autoscaler/vertical-pod-autoscaler/0.9.0/bases/no-crds`](https://github.com/confighub/helm-expt/tree/main/packages/autoscaler/vertical-pod-autoscaler/0.9.0/bases/no-crds)
- Generated scripts: [`site/sh/autoscaler-vertical-pod-autoscaler-0-9-0/no-crds`](https://github.com/confighub/helm-expt/tree/main/site/sh/autoscaler-vertical-pod-autoscaler-0-9-0/no-crds)
- Preset doctrine: [Helm Chart Presets And Values](../../../../docs/user/helm-presets-and-values.md)
