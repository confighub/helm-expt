<!-- Generated by npm run confighub-example-guides. Do not edit by hand. -->

# elastic/eck-operator 3.4.0 - ha

This guide explains the journey from a public Helm chart to a ConfigHub Space. Use it when you want to know why this preset exists, what problem it solves, how to repeat it, and what still needs care.

It is generated from the same records that build the package, chart page, render intent, scripts, and receipts. The proof links are lower down.

## Why this preset exists

Helm charts often expose many settings, but a values file alone does not tell the whole operations story. A team still needs to know what Kubernetes objects will be created, which Secrets or CRDs must already exist, whether hooks or setup jobs need special handling, and what evidence backs the result.

This preset is a named answer for one useful operating choice. It keeps the upstream chart, records the inputs and rendered YAML, and gives the team a repeatable starting point instead of a private values-file guess.

## What this is

This is the `ha` preset config for `elastic/eck-operator@3.4.0`. The repo also calls this a base variant. Use this when you want the reviewed high-availability shape instead of the simplest default.

The matching catalog page is [elastic/eck-operator@3.4.0](https://confighub.github.io/helm-expt/site/charts/elastic-eck-operator-3-4-0.html).

## The chart journey

We keep the Helm chart. We lock `elastic/eck-operator@3.4.0`, choose the `ha` preset config, render it with the recorded values, namespace, release name, and Kubernetes capabilities, then save the output as files.

That captured output is the render variant: [`recipes/elastic/eck-operator/3.4.0/revisions/ha/r001/rendered/release-objects.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/elastic/eck-operator/3.4.0/revisions/ha/r001/rendered/release-objects.yaml). It contains 22 Kubernetes object(s): CustomResourceDefinition x12, ClusterRole x3, ClusterRoleBinding x1, ConfigMap x1, Secret x1, Service x1, ServiceAccount x1, StatefulSet x1.

The public package is `oci://europe-west1-docker.pkg.dev/nth-fort-499605-q5/helm-expt/elastic-eck-operator:3.4.0`. Users can pull it without cloning this repo. When someone runs `cub installer upload`, ConfigHub stores the rendered Kubernetes YAML in a Space so it can be searched, compared, reviewed, changed, and delivered. The example script defaults to Space `helm-eck-operator-ha`, but users can choose another name with `CUB_SPACE=...`.

## What to check

No chart-specific prerequisite is recorded for this preset config beyond a cluster and namespace.

No hook or lifecycle route is recorded for this preset config.

For this preset, the main change from plain Helm is that the render inputs and output files are recorded before upload.

## Why you can trust it

- The chart version, source, namespace, release name, values, and capability profile are recorded in the render intent.
- The render variant is committed as YAML and contains 22 Kubernetes object(s).
- The installer package OCI ref points to the package users pull for this chart version.
- Render parity is recorded as passing for this preset config.

This is a claim about this recorded preset config. It is not a claim that every possible values file for this chart has been checked.

## Repeat it

Fast path with no ConfigHub account:

```sh
bash <(curl -fsSL https://confighub.github.io/helm-expt/site/sh/elastic-eck-operator-3-4-0-ha/try.sh)
```

Fast path with a ConfigHub account:

```sh
bash <(curl -fsSL https://confighub.github.io/helm-expt/site/sh/elastic-eck-operator-3-4-0-ha/confighub.sh)
```

The core render command is:

```sh
cub installer setup --pull oci://europe-west1-docker.pkg.dev/nth-fort-499605-q5/helm-expt/elastic-eck-operator:3.4.0 --base ha --work-dir ./elastic-eck-operator-3-4-0-ha --non-interactive --namespace default
```

After upload, create environment versions with `cub variant create` and move reviewed changes with `cub variant promote`. The walkthrough is [After Upload: Create A Variant And Promote Changes](../../../../docs/user/variants-after-upload.md).

## Preset details

| Item | Value |
| --- | --- |
| Chart | `elastic/eck-operator@3.4.0` |
| Preset config | `ha` |
| Namespace | `default` |
| Release name | `eck-operator` |
| Values | [`recipes/elastic/eck-operator/3.4.0/effective-values-ha.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/elastic/eck-operator/3.4.0/effective-values-ha.yaml) |
| Render intent | [`data/helm-render-intents/intents/elastic-eck-operator-3-4-0-ha.yaml`](https://github.com/confighub/helm-expt/blob/main/data/helm-render-intents/intents/elastic-eck-operator-3-4-0-ha.yaml) |
| Render variant | [`recipes/elastic/eck-operator/3.4.0/revisions/ha/r001/rendered/release-objects.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/elastic/eck-operator/3.4.0/revisions/ha/r001/rendered/release-objects.yaml) |
| Package base | [`packages/elastic/eck-operator/3.4.0/bases/ha`](https://github.com/confighub/helm-expt/tree/main/packages/elastic/eck-operator/3.4.0/bases/ha) |
| Scripts | [try.sh](https://confighub.github.io/helm-expt/site/sh/elastic-eck-operator-3-4-0-ha/try.sh) · [confighub.sh](https://confighub.github.io/helm-expt/site/sh/elastic-eck-operator-3-4-0-ha/confighub.sh) |

## Prerequisites

| Kind | What | How to provide it |
| --- | --- | --- |
| None recorded | This preset does not record chart-specific prerequisites beyond a cluster and namespace. | - |

## Evidence

| Check | Status |
| --- | --- |
| Render parity | `yes` |
| ConfigHub scan/upload proof | `yes` |
| Local kind run | `yes` |
| GitOps OCI live run | `yes` |
| Live Helm vs ConfigHub comparison | `yes` |
| Lifecycle routes | `0` |

## Limits

- Do not present as a catalog-supported chart until promotion review and support decisions are recorded.

## Source files

- Chart page: [https://confighub.github.io/helm-expt/site/charts/elastic-eck-operator-3-4-0.html](https://confighub.github.io/helm-expt/site/charts/elastic-eck-operator-3-4-0.html)
- Render intent: [`data/helm-render-intents/intents/elastic-eck-operator-3-4-0-ha.yaml`](https://github.com/confighub/helm-expt/blob/main/data/helm-render-intents/intents/elastic-eck-operator-3-4-0-ha.yaml)
- Rendered YAML: [`recipes/elastic/eck-operator/3.4.0/revisions/ha/r001/rendered/release-objects.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/elastic/eck-operator/3.4.0/revisions/ha/r001/rendered/release-objects.yaml)
- Package source: [`packages/elastic/eck-operator/3.4.0/bases/ha`](https://github.com/confighub/helm-expt/tree/main/packages/elastic/eck-operator/3.4.0/bases/ha)
- Generated scripts: [`site/sh/elastic-eck-operator-3-4-0-ha`](https://github.com/confighub/helm-expt/tree/main/site/sh/elastic-eck-operator-3-4-0-ha)
- Preset doctrine: [Helm Chart Presets And Values](../../../../docs/user/helm-presets-and-values.md)
