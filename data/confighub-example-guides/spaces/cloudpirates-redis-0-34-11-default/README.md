<!-- Generated by npm run confighub-example-guides. Do not edit by hand. -->

# cloudpirates/redis 0.34.11 - default

This guide follows one public Helm chart into a ConfigHub Space. It covers why the preset exists, how to repeat it, and what still needs care.

It is generated from the same records that build the package, the chart page and the render intent, along with the scripts and receipts. The proof links are lower down.

## Why this preset exists

A Helm chart exposes many settings, and a values file records the settings rather than the operations story around them. A team still has to know which Kubernetes objects get created and which Secrets or CRDs have to exist first. Hooks and setup jobs may need special handling, and something has to back the result.

This preset is a named answer to one operating choice. It keeps the upstream chart, records the inputs and the rendered YAML, and leaves the team a repeatable starting point instead of a private values-file guess.

## What this is

This is the `default` preset config for `cloudpirates/redis@0.34.11`. The repo also calls this a base variant. Use this when you want to start from the chart author's normal path, with the inputs recorded.

The matching catalog page is [cloudpirates/redis@0.34.11](https://confighub.github.io/helm-expt/site/charts/cloudpirates-redis-0-34-11.html).

## The chart journey

We keep the Helm chart. We lock `cloudpirates/redis@0.34.11` and choose the `default` preset config, then render it with the recorded values, namespace and release name against the recorded Kubernetes capabilities, and save the output as files.

That captured output is the render variant: [`recipes/cloudpirates/redis/0.34.11/revisions/default/r001/rendered/release-objects.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/cloudpirates/redis/0.34.11/revisions/default/r001/rendered/release-objects.yaml). It contains 5 Kubernetes object(s): Service x2, ConfigMap x1, Secret x1, StatefulSet x1.

The public package is `oci://europe-west1-docker.pkg.dev/nth-fort-499605-q5/helm-expt/cloudpirates-redis:0.34.11@sha256:29a96a9f6b3e2cedfe30cefe20c51e46cbcb1e363c69c45c58f9118530099333`. Users can pull these exact bytes without cloning this repo. When someone runs `cub installer upload`, ConfigHub stores the rendered Kubernetes YAML in a Space, where it can be searched and compared, reviewed and changed, then delivered. The example script defaults to Space `helm-redis-default`, but users can choose another name with `CUB_SPACE=...`.

## What to check

The current catalog record does not identify an extra Secret, CRD, or setup step that must be supplied before install.

The catalog does not currently record a separate hook, setup job, or cleanup step for this preset.

For this preset, the main change from plain Helm is that the render inputs and output files are recorded before upload.

## Why you can trust it

- The chart version, source, namespace, release name, values, and capability profile are recorded in the render intent.
- The render variant is committed as YAML and contains 5 Kubernetes object(s).
- The installer package OCI ref points to the package users pull for this chart version.
- Render parity is recorded as passing for this preset config.

This claim covers this recorded preset config alone. Every other values file for this chart remains unchecked.

## Repeat it

Fast path with no ConfigHub account:

```sh
bash <(curl -fsSL https://confighub.github.io/helm-expt/site/sh/cloudpirates-redis-0-34-11/default/try.sh)
```

Fast path with a ConfigHub account:

```sh
bash <(curl -fsSL https://confighub.github.io/helm-expt/site/sh/cloudpirates-redis-0-34-11/default/confighub.sh)
```

The core render command is:

```sh
cub installer setup --pull oci://europe-west1-docker.pkg.dev/nth-fort-499605-q5/helm-expt/cloudpirates-redis:0.34.11@sha256:29a96a9f6b3e2cedfe30cefe20c51e46cbcb1e363c69c45c58f9118530099333 --base default --work-dir ./cloudpirates-redis-0-34-11-default --non-interactive --namespace default
```

After upload, create environment versions with `cub variant create` and move reviewed changes with `cub variant promote`. The walkthrough is [After upload: create a variant and promote changes](../../../../docs/user/variants-after-upload.md).

## Preset details

| Item | Value |
| --- | --- |
| Chart | `cloudpirates/redis@0.34.11` |
| Preset config | `default` |
| Namespace | `default` |
| Release name | `redis` |
| Values | [`recipes/cloudpirates/redis/0.34.11/effective-values.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/cloudpirates/redis/0.34.11/effective-values.yaml) |
| Render intent | [`data/helm-render-intents/intents/cloudpirates-redis-0-34-11-default.yaml`](https://github.com/confighub/helm-expt/blob/main/data/helm-render-intents/intents/cloudpirates-redis-0-34-11-default.yaml) |
| Render variant | [`recipes/cloudpirates/redis/0.34.11/revisions/default/r001/rendered/release-objects.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/cloudpirates/redis/0.34.11/revisions/default/r001/rendered/release-objects.yaml) |
| Package base | [`packages/cloudpirates/redis/0.34.11/bases/default`](https://github.com/confighub/helm-expt/tree/main/packages/cloudpirates/redis/0.34.11/bases/default) |
| Scripts | [try.sh](https://confighub.github.io/helm-expt/site/sh/cloudpirates-redis-0-34-11/default/try.sh) · [confighub.sh](https://confighub.github.io/helm-expt/site/sh/cloudpirates-redis-0-34-11/default/confighub.sh) |

## Prerequisites and lifecycle steps

| When | What | How it is handled |
| --- | --- | --- |
| No extra step recorded | The catalog has not identified a separate prerequisite or lifecycle step for this preset. | Check the limits and evidence before production use. |

## Evidence

| Check | Status |
| --- | --- |
| Render parity | `yes` |
| ConfigHub scan/upload proof | `todo` |
| Earlier local-cluster test | `todo` |
| GitOps OCI live run | `todo` |
| Live Helm vs ConfigHub comparison | `todo` |
| Lifecycle routes | `0` |

## Limits

- Local kind evidence is todo for this preset config.
- GitOps OCI live evidence is todo for this preset config.
- Render parity alone is useful evidence, not a production or catalog recommendation.

## Source files

- Chart page: [https://confighub.github.io/helm-expt/site/charts/cloudpirates-redis-0-34-11.html](https://confighub.github.io/helm-expt/site/charts/cloudpirates-redis-0-34-11.html)
- Render intent: [`data/helm-render-intents/intents/cloudpirates-redis-0-34-11-default.yaml`](https://github.com/confighub/helm-expt/blob/main/data/helm-render-intents/intents/cloudpirates-redis-0-34-11-default.yaml)
- Rendered YAML: [`recipes/cloudpirates/redis/0.34.11/revisions/default/r001/rendered/release-objects.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/cloudpirates/redis/0.34.11/revisions/default/r001/rendered/release-objects.yaml)
- Package source: [`packages/cloudpirates/redis/0.34.11/bases/default`](https://github.com/confighub/helm-expt/tree/main/packages/cloudpirates/redis/0.34.11/bases/default)
- Generated scripts: [`site/sh/cloudpirates-redis-0-34-11/default`](https://github.com/confighub/helm-expt/tree/main/site/sh/cloudpirates-redis-0-34-11/default)
- Preset doctrine: [Helm Chart Presets And Values](../../../../docs/user/helm-presets-and-values.md)
