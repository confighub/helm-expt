<!-- Generated by npm run confighub-example-guides. Do not edit by hand. -->

# grafana/pyroscope 2.0.2 - no-crds

This guide explains the journey from a public Helm chart to a ConfigHub Space. Use it when you want to know why this preset exists, what problem it solves, how to repeat it, and what still needs care.

It is generated from the same records that build the package, chart page, render intent, scripts, and receipts. The proof links are lower down.

## Why this preset exists

Helm charts often expose many settings, but a values file alone does not tell the whole operations story. A team still needs to know what Kubernetes objects will be created, which Secrets or CRDs must already exist, whether hooks or setup jobs need special handling, and what evidence backs the result.

This preset is a named answer for one useful operating choice. It keeps the upstream chart, records the inputs and rendered YAML, and gives the team a repeatable starting point instead of a private values-file guess.

## What this is

This is the `no-crds` preset config for `grafana/pyroscope@2.0.2`. The repo also calls this a base variant. Use this when your platform, GitOps bootstrap, or another chart owns the CRDs.

The matching catalog page is [grafana/pyroscope@2.0.2](https://confighub.github.io/helm-expt/site/charts/grafana-pyroscope-2-0-2.html).

## The chart journey

We keep the Helm chart. We lock `grafana/pyroscope@2.0.2`, choose the `no-crds` preset config, render it with the recorded values, namespace, release name, and Kubernetes capabilities, then save the output as files.

That captured output is the render variant: [`recipes/grafana/pyroscope/2.0.2/revisions/no-crds/r001/rendered/release-objects.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/grafana/pyroscope/2.0.2/revisions/no-crds/r001/rendered/release-objects.yaml). It contains 17 Kubernetes object(s): Service x5, ConfigMap x3, ServiceAccount x2, StatefulSet x2, ClusterRole x1, ClusterRoleBinding x1, PodDisruptionBudget x1, Role x1.

The public package is `oci://europe-west1-docker.pkg.dev/nth-fort-499605-q5/helm-expt/grafana-pyroscope:2.0.2`. Users can pull it without cloning this repo. When someone runs `cub installer upload`, ConfigHub stores the rendered Kubernetes YAML in a Space so it can be searched, compared, reviewed, changed, and delivered. The example script defaults to Space `helm-pyroscope-no-crds`, but users can choose another name with `CUB_SPACE=...`.

## What to check

No chart-specific prerequisite is recorded for this preset config beyond a cluster and namespace.

No hook or lifecycle route is recorded for this preset config.

CRDs are made into an explicit choice instead of being mixed into the application install. CRD ownership is recorded as part of the preset config. Known limitation: existing-secret (chart ships no Secret toggle).

## Why you can trust it

- The chart version, source, namespace, release name, values, and capability profile are recorded in the render intent.
- The render variant is committed as YAML and contains 17 Kubernetes object(s).
- The installer package OCI ref points to the package users pull for this chart version.
- Render parity is recorded as passing for this preset config.

This is a claim about this recorded preset config. It is not a claim that every possible values file for this chart has been checked.

## Repeat it

Fast path with no ConfigHub account:

```sh
bash <(curl -fsSL https://confighub.github.io/helm-expt/site/sh/grafana-pyroscope-2-0-2-no-crds/try.sh)
```

Fast path with a ConfigHub account:

```sh
bash <(curl -fsSL https://confighub.github.io/helm-expt/site/sh/grafana-pyroscope-2-0-2-no-crds/confighub.sh)
```

The core render command is:

```sh
cub installer setup --pull oci://europe-west1-docker.pkg.dev/nth-fort-499605-q5/helm-expt/grafana-pyroscope:2.0.2 --base no-crds --work-dir ./grafana-pyroscope-2-0-2-no-crds --non-interactive --namespace default
```

After upload, create environment versions with `cub variant create` and move reviewed changes with `cub variant promote`. The walkthrough is [After Upload: Create A Variant And Promote Changes](../../../../docs/user/variants-after-upload.md).

## Preset details

| Item | Value |
| --- | --- |
| Chart | `grafana/pyroscope@2.0.2` |
| Preset config | `no-crds` |
| Namespace | `default` |
| Release name | `pyroscope` |
| Values | [`recipes/grafana/pyroscope/2.0.2/effective-values-no-crds.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/grafana/pyroscope/2.0.2/effective-values-no-crds.yaml) |
| Render intent | [`data/helm-render-intents/intents/grafana-pyroscope-2-0-2-no-crds.yaml`](https://github.com/confighub/helm-expt/blob/main/data/helm-render-intents/intents/grafana-pyroscope-2-0-2-no-crds.yaml) |
| Render variant | [`recipes/grafana/pyroscope/2.0.2/revisions/no-crds/r001/rendered/release-objects.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/grafana/pyroscope/2.0.2/revisions/no-crds/r001/rendered/release-objects.yaml) |
| Package base | [`packages/grafana/pyroscope/2.0.2/bases/no-crds`](https://github.com/confighub/helm-expt/tree/main/packages/grafana/pyroscope/2.0.2/bases/no-crds) |
| Scripts | [try.sh](https://confighub.github.io/helm-expt/site/sh/grafana-pyroscope-2-0-2-no-crds/try.sh) · [confighub.sh](https://confighub.github.io/helm-expt/site/sh/grafana-pyroscope-2-0-2-no-crds/confighub.sh) |

## Prerequisites

| Kind | What | How to provide it |
| --- | --- | --- |
| None recorded | This preset does not record chart-specific prerequisites beyond a cluster and namespace. | - |

## Evidence

| Check | Status |
| --- | --- |
| Render parity | `yes` |
| ConfigHub scan/upload proof | `yes` |
| Local kind run | `yes` |
| GitOps OCI live run | `watch` |
| Live Helm vs ConfigHub comparison | `watch` |
| Lifecycle routes | `0` |

## Limits

- GitOps OCI live evidence is watch for this preset config.
- Known gap for this row: existing-secret (chart ships no Secret toggle).
- Resolve the named gap first: existing-secret (chart ships no Secret toggle).

## Source files

- Chart page: [https://confighub.github.io/helm-expt/site/charts/grafana-pyroscope-2-0-2.html](https://confighub.github.io/helm-expt/site/charts/grafana-pyroscope-2-0-2.html)
- Render intent: [`data/helm-render-intents/intents/grafana-pyroscope-2-0-2-no-crds.yaml`](https://github.com/confighub/helm-expt/blob/main/data/helm-render-intents/intents/grafana-pyroscope-2-0-2-no-crds.yaml)
- Rendered YAML: [`recipes/grafana/pyroscope/2.0.2/revisions/no-crds/r001/rendered/release-objects.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/grafana/pyroscope/2.0.2/revisions/no-crds/r001/rendered/release-objects.yaml)
- Package source: [`packages/grafana/pyroscope/2.0.2/bases/no-crds`](https://github.com/confighub/helm-expt/tree/main/packages/grafana/pyroscope/2.0.2/bases/no-crds)
- Generated scripts: [`site/sh/grafana-pyroscope-2-0-2-no-crds`](https://github.com/confighub/helm-expt/tree/main/site/sh/grafana-pyroscope-2-0-2-no-crds)
- Preset doctrine: [Helm Chart Presets And Values](../../../../docs/user/helm-presets-and-values.md)
