<!-- Generated by npm run confighub-example-guides. Do not edit by hand. -->

# argo-cd/argo-workflows 1.0.14 - minimal-crds

This guide follows one public Helm chart into a ConfigHub Space. It covers why the preset exists, how to repeat it, and what still needs care.

It is generated from the same records that build the package, the chart page and the render intent, along with the scripts and receipts. The proof links are lower down.

## Why this preset exists

A Helm chart exposes many settings, and a values file records the settings rather than the operations story around them. A team still has to know which Kubernetes objects get created and which Secrets or CRDs have to exist first. Hooks and setup jobs may need special handling, and something has to back the result.

This preset is a named answer to one operating choice. It keeps the upstream chart, records the inputs and the rendered YAML, and leaves the team a repeatable starting point instead of a private values-file guess.

## What this is

This is the `minimal-crds` preset config for `argo-cd/argo-workflows@1.0.14`. The repo also calls this a base variant. Use this when this package should bring the CRDs needed for the install.

The matching catalog page is [argo-cd/argo-workflows@1.0.14](https://confighub.github.io/helm-expt/site/charts/argo-cd-argo-workflows-1-0-14.html).

## The chart journey

We keep the Helm chart. We lock `argo-cd/argo-workflows@1.0.14` and choose the `minimal-crds` preset config, then render it with the recorded values, namespace and release name against the recorded Kubernetes capabilities, and save the output as files.

That captured output is the render variant: [`recipes/argo-cd/argo-workflows/1.0.14/revisions/minimal-crds/r001/rendered/release-objects.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/argo-cd/argo-workflows/1.0.14/revisions/minimal-crds/r001/rendered/release-objects.yaml). It contains 27 Kubernetes object(s): CustomResourceDefinition x8, ClusterRole x7, ClusterRoleBinding x4, Deployment x2, ServiceAccount x2, ConfigMap x1, Role x1, RoleBinding x1.

The public package is `oci://europe-west1-docker.pkg.dev/nth-fort-499605-q5/helm-expt/argo-cd-argo-workflows:1.0.14@sha256:abbebaeccc07e0583c7ccc5ad0c9d315057e6b4c0a766bd72c167ee92d3f160f`. Users can pull these exact bytes without cloning this repo. When someone runs `cub installer upload`, ConfigHub stores the rendered Kubernetes YAML in a Space, where it can be searched and compared, reviewed and changed, then delivered. The example script defaults to Space `helm-argo-workflows-minimal-crds`, but users can choose another name with `CUB_SPACE=...`.

## What to check

This preset needs a setup step before Kubernetes can accept all of the rendered objects. The step is described below and is part of the tested install path.

The catalog records 1 extra step for this preset. We call these lifecycle routes because they say what must happen before, during, or after Kubernetes applies the main set of files.

- **Before install:** Argo Workflows default full CRDs are installed through a Helm hook using server-side apply; this base chooses the self-contained minimal-CRD route instead.

CRD ownership is recorded as part of the preset config. Hooks, setup jobs, and other install or upgrade steps are listed separately, so you can see what must run and when.

## Why you can trust it

- The chart version, source, namespace, release name, values, and capability profile are recorded in the render intent.
- The render variant is committed as YAML and contains 27 Kubernetes object(s).
- The installer package OCI ref points to the package users pull for this chart version.
- Render parity is recorded as passing for this preset config.
- Hook and lifecycle work is counted and linked to the route record.

This claim covers this recorded preset config alone. Every other values file for this chart remains unchecked.

## Repeat it

Fast path with no ConfigHub account:

```sh
bash <(curl -fsSL https://confighub.github.io/helm-expt/site/sh/argo-cd-argo-workflows-1-0-14/minimal-crds/try.sh)
```

Fast path with a ConfigHub account:

```sh
bash <(curl -fsSL https://confighub.github.io/helm-expt/site/sh/argo-cd-argo-workflows-1-0-14/minimal-crds/confighub.sh)
```

The core render command is:

```sh
cub installer setup --pull oci://europe-west1-docker.pkg.dev/nth-fort-499605-q5/helm-expt/argo-cd-argo-workflows:1.0.14@sha256:abbebaeccc07e0583c7ccc5ad0c9d315057e6b4c0a766bd72c167ee92d3f160f --base minimal-crds --work-dir ./argo-cd-argo-workflows-1-0-14-minimal-crds --non-interactive --namespace default
```

After upload, create environment versions with `cub variant create` and move reviewed changes with `cub variant promote`. The walkthrough is [After upload: create a variant and promote changes](../../../../docs/user/variants-after-upload.md).

## Preset details

| Item | Value |
| --- | --- |
| Chart | `argo-cd/argo-workflows@1.0.14` |
| Preset config | `minimal-crds` |
| Namespace | `default` |
| Release name | `argo-workflows` |
| Values | [`recipes/argo-cd/argo-workflows/1.0.14/effective-values-minimal-crds.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/argo-cd/argo-workflows/1.0.14/effective-values-minimal-crds.yaml) |
| Render intent | [`data/helm-render-intents/intents/argo-cd-argo-workflows-1-0-14-minimal-crds.yaml`](https://github.com/confighub/helm-expt/blob/main/data/helm-render-intents/intents/argo-cd-argo-workflows-1-0-14-minimal-crds.yaml) |
| Render variant | [`recipes/argo-cd/argo-workflows/1.0.14/revisions/minimal-crds/r001/rendered/release-objects.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/argo-cd/argo-workflows/1.0.14/revisions/minimal-crds/r001/rendered/release-objects.yaml) |
| Package base | [`packages/argo-cd/argo-workflows/1.0.14/bases/minimal-crds`](https://github.com/confighub/helm-expt/tree/main/packages/argo-cd/argo-workflows/1.0.14/bases/minimal-crds) |
| Scripts | [try.sh](https://confighub.github.io/helm-expt/site/sh/argo-cd-argo-workflows-1-0-14/minimal-crds/try.sh) · [confighub.sh](https://confighub.github.io/helm-expt/site/sh/argo-cd-argo-workflows-1-0-14/minimal-crds/confighub.sh) |

## Prerequisites and lifecycle steps

| When | What | How it is handled |
| --- | --- | --- |
| Before install | Apply CRDs before custom resources | Argo Workflows default full CRDs are installed through a Helm hook using server-side apply; this base chooses the self-contained minimal-CRD route instead. |

## Evidence

| Check | Status |
| --- | --- |
| Render parity | `yes` |
| ConfigHub scan/upload proof | `yes` |
| Earlier local-cluster test | `yes` |
| GitOps OCI live run | `yes` |
| Live Helm vs ConfigHub comparison | `yes` |
| Lifecycle routes | `1` |

## Limits

- Do not present as a catalog-supported chart until promotion review and support decisions are recorded.

## Source files

- Chart page: [https://confighub.github.io/helm-expt/site/charts/argo-cd-argo-workflows-1-0-14.html](https://confighub.github.io/helm-expt/site/charts/argo-cd-argo-workflows-1-0-14.html)
- Render intent: [`data/helm-render-intents/intents/argo-cd-argo-workflows-1-0-14-minimal-crds.yaml`](https://github.com/confighub/helm-expt/blob/main/data/helm-render-intents/intents/argo-cd-argo-workflows-1-0-14-minimal-crds.yaml)
- Rendered YAML: [`recipes/argo-cd/argo-workflows/1.0.14/revisions/minimal-crds/r001/rendered/release-objects.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/argo-cd/argo-workflows/1.0.14/revisions/minimal-crds/r001/rendered/release-objects.yaml)
- Package source: [`packages/argo-cd/argo-workflows/1.0.14/bases/minimal-crds`](https://github.com/confighub/helm-expt/tree/main/packages/argo-cd/argo-workflows/1.0.14/bases/minimal-crds)
- Generated scripts: [`site/sh/argo-cd-argo-workflows-1-0-14/minimal-crds`](https://github.com/confighub/helm-expt/tree/main/site/sh/argo-cd-argo-workflows-1-0-14/minimal-crds)
- Preset doctrine: [Helm Chart Presets And Values](../../../../docs/user/helm-presets-and-values.md)
