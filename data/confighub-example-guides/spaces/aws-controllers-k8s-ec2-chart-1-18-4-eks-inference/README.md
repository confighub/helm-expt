<!-- Generated by npm run confighub-example-guides. Do not edit by hand. -->

# aws-controllers-k8s/ec2-chart 1.18.4 - eks-inference

This guide follows one public Helm chart into a ConfigHub Space. It covers why the preset exists, how to repeat it, and what still needs care.

It is generated from the same records that build the package, the chart page and the render intent, along with the scripts and receipts. The proof links are lower down.

## Why this preset exists

A Helm chart exposes many settings, and a values file records the settings rather than the operations story around them. A team still has to know which Kubernetes objects get created and which Secrets or CRDs have to exist first. Hooks and setup jobs may need special handling, and something has to back the result.

This preset is a named answer to one operating choice. It keeps the upstream chart, records the inputs and the rendered YAML, and leaves the team a repeatable starting point instead of a private values-file guess.

## What this is

This is the `eks-inference` preset config for `aws-controllers-k8s/ec2-chart@1.18.4`. The repo also calls this a base variant. ec2-chart eks-inference variant rendered from aws-controllers-k8s/ec2-chart@1.18.4.

The matching catalog page is [aws-controllers-k8s/ec2-chart@1.18.4](https://confighub.github.io/helm-expt/site/charts/aws-controllers-k8s-ec2-chart-1-18-4.html).

## The chart journey

We keep the Helm chart. We lock `aws-controllers-k8s/ec2-chart@1.18.4` and choose the `eks-inference` preset config, then render it with the recorded values, namespace and release name against the recorded Kubernetes capabilities, and save the output as files.

That captured output is the render variant: [`recipes/aws-controllers-k8s/ec2-chart/1.18.4/revisions/eks-inference/r001/rendered/release-objects.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/aws-controllers-k8s/ec2-chart/1.18.4/revisions/eks-inference/r001/rendered/release-objects.yaml). It contains 32 Kubernetes object(s): CustomResourceDefinition x22, Role x3, ClusterRole x2, ClusterRoleBinding x2, Deployment x1, RoleBinding x1, ServiceAccount x1.

The public package is `oci://europe-west1-docker.pkg.dev/nth-fort-499605-q5/helm-expt/aws-controllers-k8s-ec2-chart:1.18.4@sha256:40806df17d8ff6732613c13d151c5eff9c1e9551389718a9f3d3dac68d9f3721`. Users can pull these exact bytes without cloning this repo. When someone runs `cub installer upload`, ConfigHub stores the rendered Kubernetes YAML in a Space, where it can be searched and compared, reviewed and changed, then delivered. The example script defaults to Space `helm-ec2-chart-eks-inference`, but users can choose another name with `CUB_SPACE=...`.

## What to check

This preset config records 1 prerequisite: 1 Secret. Follow the instructions below before you apply the rendered objects.

The catalog does not currently record a separate hook, setup job, or cleanup step for this preset.

At least one Secret must be created with your values before apply.

## Why you can trust it

- The chart version, source, namespace, release name, values, and capability profile are recorded in the render intent.
- The render variant is committed as YAML and contains 32 Kubernetes object(s).
- The installer package OCI ref points to the package users pull for this chart version.
- Render parity is recorded as passing for this preset config.
- Prerequisites are named before apply, so they are not discovered after rollout.

This claim covers this recorded preset config alone. Every other values file for this chart remains unchecked.

## Repeat it

Fast path with no ConfigHub account:

```sh
bash <(curl -fsSL https://confighub.github.io/helm-expt/site/sh/aws-controllers-k8s-ec2-chart-1-18-4/eks-inference/try.sh)
```

Fast path with a ConfigHub account:

```sh
bash <(curl -fsSL https://confighub.github.io/helm-expt/site/sh/aws-controllers-k8s-ec2-chart-1-18-4/eks-inference/confighub.sh)
```

The core render command is:

```sh
cub installer setup --pull oci://europe-west1-docker.pkg.dev/nth-fort-499605-q5/helm-expt/aws-controllers-k8s-ec2-chart:1.18.4@sha256:40806df17d8ff6732613c13d151c5eff9c1e9551389718a9f3d3dac68d9f3721 --base eks-inference --work-dir ./aws-controllers-k8s-ec2-chart-1-18-4-eks-inference --non-interactive --namespace ack-system
```

After upload, create environment versions with `cub variant create` and move reviewed changes with `cub variant promote`. The walkthrough is [After upload: create a variant and promote changes](../../../../docs/user/variants-after-upload.md).

## Preset details

| Item | Value |
| --- | --- |
| Chart | `aws-controllers-k8s/ec2-chart@1.18.4` |
| Preset config | `eks-inference` |
| Namespace | `ack-system` |
| Release name | `ack-ec2` |
| Values | [`recipes/aws-controllers-k8s/ec2-chart/1.18.4/effective-values-eks-inference.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/aws-controllers-k8s/ec2-chart/1.18.4/effective-values-eks-inference.yaml) |
| Render intent | [`data/helm-render-intents/intents/aws-controllers-k8s-ec2-chart-1-18-4-eks-inference.yaml`](https://github.com/confighub/helm-expt/blob/main/data/helm-render-intents/intents/aws-controllers-k8s-ec2-chart-1-18-4-eks-inference.yaml) |
| Render variant | [`recipes/aws-controllers-k8s/ec2-chart/1.18.4/revisions/eks-inference/r001/rendered/release-objects.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/aws-controllers-k8s/ec2-chart/1.18.4/revisions/eks-inference/r001/rendered/release-objects.yaml) |
| Package base | [`packages/aws-controllers-k8s/ec2-chart/1.18.4/bases/eks-inference`](https://github.com/confighub/helm-expt/tree/main/packages/aws-controllers-k8s/ec2-chart/1.18.4/bases/eks-inference) |
| Scripts | [try.sh](https://confighub.github.io/helm-expt/site/sh/aws-controllers-k8s-ec2-chart-1-18-4/eks-inference/try.sh) · [confighub.sh](https://confighub.github.io/helm-expt/site/sh/aws-controllers-k8s-ec2-chart-1-18-4/eks-inference/confighub.sh) |

## Prerequisites and lifecycle steps

| When | What | How it is handled |
| --- | --- | --- |
| Before install | ClusterFeature: Secret ack-system/aws-creds key credentials | kubectl -n ack-system create secret generic aws-creds --from-literal=credentials=<value> |

## Evidence

| Check | Status |
| --- | --- |
| Render parity | `yes` |
| ConfigHub scan/upload proof | `todo` |
| Earlier local-cluster test | `todo` |
| GitOps OCI live run | `todo` |
| Live Helm vs ConfigHub comparison | `todo` |
| Lifecycle routes | `0` |

## Limits

- Local kind evidence is todo for this preset config.
- GitOps OCI live evidence is todo for this preset config.
- No production claim.

## Source files

- Chart page: [https://confighub.github.io/helm-expt/site/charts/aws-controllers-k8s-ec2-chart-1-18-4.html](https://confighub.github.io/helm-expt/site/charts/aws-controllers-k8s-ec2-chart-1-18-4.html)
- Render intent: [`data/helm-render-intents/intents/aws-controllers-k8s-ec2-chart-1-18-4-eks-inference.yaml`](https://github.com/confighub/helm-expt/blob/main/data/helm-render-intents/intents/aws-controllers-k8s-ec2-chart-1-18-4-eks-inference.yaml)
- Rendered YAML: [`recipes/aws-controllers-k8s/ec2-chart/1.18.4/revisions/eks-inference/r001/rendered/release-objects.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/aws-controllers-k8s/ec2-chart/1.18.4/revisions/eks-inference/r001/rendered/release-objects.yaml)
- Package source: [`packages/aws-controllers-k8s/ec2-chart/1.18.4/bases/eks-inference`](https://github.com/confighub/helm-expt/tree/main/packages/aws-controllers-k8s/ec2-chart/1.18.4/bases/eks-inference)
- Generated scripts: [`site/sh/aws-controllers-k8s-ec2-chart-1-18-4/eks-inference`](https://github.com/confighub/helm-expt/tree/main/site/sh/aws-controllers-k8s-ec2-chart-1-18-4/eks-inference)
- Preset doctrine: [Helm Chart Presets And Values](../../../../docs/user/helm-presets-and-values.md)
