<!-- Generated by npm run confighub-example-guides. Do not edit by hand. -->

# jetstack/trust-manager v0.22.1 - default

This guide explains the journey from a public Helm chart to a ConfigHub Space. Use it when you want to know why this preset exists, what problem it solves, how to repeat it, and what still needs care.

It is generated from the same records that build the package, chart page, render intent, scripts, and receipts. The proof links are lower down.

## Why this preset exists

Helm charts often expose many settings, but a values file alone does not tell the whole operations story. A team still needs to know what Kubernetes objects will be created, which Secrets or CRDs must already exist, whether hooks or setup jobs need special handling, and what evidence backs the result.

This preset is a named answer for one useful operating choice. It keeps the upstream chart, records the inputs and rendered YAML, and gives the team a repeatable starting point instead of a private values-file guess.

## What this is

This is the `default` preset config for `jetstack/trust-manager@v0.22.1`. The repo also calls this a base variant. Use this when you want to start from the chart author's normal path, with the inputs recorded.

The matching catalog page is [jetstack/trust-manager@v0.22.1](https://confighub.github.io/helm-expt/site/charts/jetstack-trust-manager-v0-22-1.html).

## The chart journey

We keep the Helm chart. We lock `jetstack/trust-manager@v0.22.1`, choose the `default` preset config, render it with the recorded values, namespace, release name, and Kubernetes capabilities, then save the output as files.

That captured output is the render variant: [`recipes/jetstack/trust-manager/v0.22.1/revisions/default/r001/rendered/release-objects.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/jetstack/trust-manager/v0.22.1/revisions/default/r001/rendered/release-objects.yaml). It contains 14 Kubernetes object(s): Role x2, RoleBinding x2, Service x2, Certificate x1, ClusterRole x1, ClusterRoleBinding x1, CustomResourceDefinition x1, Deployment x1.

The public package is `oci://europe-west1-docker.pkg.dev/nth-fort-499605-q5/helm-expt/jetstack-trust-manager:v0.22.1`. Users can pull it without cloning this repo. When someone runs `cub installer upload`, ConfigHub stores the rendered Kubernetes YAML in a Space so it can be searched, compared, reviewed, changed, and delivered. The example script defaults to Space `helm-trust-manager-default`, but users can choose another name with `CUB_SPACE=...`.

## What to check

This preset config records 4 prerequisite(s): 2 CRDs, 1 Secret, 1 other item. Create these with your own values before you apply the rendered objects.

No hook or lifecycle route is recorded for this preset config.

Some CRDs must already exist before the rendered objects are applied. At least one Secret must be created with your values before apply.

## Why you can trust it

- The chart version, source, namespace, release name, values, and capability profile are recorded in the render intent.
- The render variant is committed as YAML and contains 14 Kubernetes object(s).
- The installer package OCI ref points to the package users pull for this chart version.
- Render parity is recorded as passing for this preset config.
- Prerequisites are named before apply, so they are not discovered after rollout.

This is a claim about this recorded preset config. It is not a claim that every possible values file for this chart has been checked.

## Repeat it

Fast path with no ConfigHub account:

```sh
bash <(curl -fsSL https://confighub.github.io/helm-expt/site/sh/jetstack-trust-manager-v0-22-1-default/try.sh)
```

Fast path with a ConfigHub account:

```sh
bash <(curl -fsSL https://confighub.github.io/helm-expt/site/sh/jetstack-trust-manager-v0-22-1-default/confighub.sh)
```

The core render command is:

```sh
cub installer setup --pull oci://europe-west1-docker.pkg.dev/nth-fort-499605-q5/helm-expt/jetstack-trust-manager:v0.22.1 --base default --work-dir ./jetstack-trust-manager-v0-22-1-default --non-interactive --namespace default
```

After upload, create environment versions with `cub variant create` and move reviewed changes with `cub variant promote`. The walkthrough is [After Upload: Create A Variant And Promote Changes](../../../../docs/user/variants-after-upload.md).

## Preset details

| Item | Value |
| --- | --- |
| Chart | `jetstack/trust-manager@v0.22.1` |
| Preset config | `default` |
| Namespace | `default` |
| Release name | `trust-manager` |
| Values | [`recipes/jetstack/trust-manager/v0.22.1/effective-values.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/jetstack/trust-manager/v0.22.1/effective-values.yaml) |
| Render intent | [`data/helm-render-intents/intents/jetstack-trust-manager-v0-22-1-default.yaml`](https://github.com/confighub/helm-expt/blob/main/data/helm-render-intents/intents/jetstack-trust-manager-v0-22-1-default.yaml) |
| Render variant | [`recipes/jetstack/trust-manager/v0.22.1/revisions/default/r001/rendered/release-objects.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/jetstack/trust-manager/v0.22.1/revisions/default/r001/rendered/release-objects.yaml) |
| Package base | [`packages/jetstack/trust-manager/v0.22.1/bases/default`](https://github.com/confighub/helm-expt/tree/main/packages/jetstack/trust-manager/v0.22.1/bases/default) |
| Scripts | [try.sh](https://confighub.github.io/helm-expt/site/sh/jetstack-trust-manager-v0-22-1-default/try.sh) · [confighub.sh](https://confighub.github.io/helm-expt/site/sh/jetstack-trust-manager-v0-22-1-default/confighub.sh) |

## Prerequisites

| Kind | What | How to provide it |
| --- | --- | --- |
| ClusterFeature | Secret default/trust-manager-tls keys tls.crt,tls.key | kubectl -n default create secret generic trust-manager-tls --from-literal=tls.crt=<value> --from-literal=tls.key=<value> |
| ClusterFeature | CRD certificates.cert-manager.io | package://prerequisites/target-facts/default-crds.yaml |
| ClusterFeature | CRD issuers.cert-manager.io | package://prerequisites/target-facts/default-crds.yaml |
| ClusterFeature | Namespace cert-manager | kubectl create namespace cert-manager |

## Evidence

| Check | Status |
| --- | --- |
| Render parity | `yes` |
| ConfigHub scan/upload proof | `yes` |
| Local kind run | `no` |
| GitOps OCI live run | `watch` |
| Live Helm vs ConfigHub comparison | `watch` |
| Lifecycle routes | `0` |

## Limits

- Local kind evidence is no for this preset config.
- GitOps OCI live evidence is watch for this preset config.
- Do not present as a catalog-supported chart until promotion review and support decisions are recorded.

## Source files

- Chart page: [https://confighub.github.io/helm-expt/site/charts/jetstack-trust-manager-v0-22-1.html](https://confighub.github.io/helm-expt/site/charts/jetstack-trust-manager-v0-22-1.html)
- Render intent: [`data/helm-render-intents/intents/jetstack-trust-manager-v0-22-1-default.yaml`](https://github.com/confighub/helm-expt/blob/main/data/helm-render-intents/intents/jetstack-trust-manager-v0-22-1-default.yaml)
- Rendered YAML: [`recipes/jetstack/trust-manager/v0.22.1/revisions/default/r001/rendered/release-objects.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/jetstack/trust-manager/v0.22.1/revisions/default/r001/rendered/release-objects.yaml)
- Package source: [`packages/jetstack/trust-manager/v0.22.1/bases/default`](https://github.com/confighub/helm-expt/tree/main/packages/jetstack/trust-manager/v0.22.1/bases/default)
- Generated scripts: [`site/sh/jetstack-trust-manager-v0-22-1-default`](https://github.com/confighub/helm-expt/tree/main/site/sh/jetstack-trust-manager-v0-22-1-default)
- Preset doctrine: [Helm Chart Presets And Values](../../../../docs/user/helm-presets-and-values.md)
