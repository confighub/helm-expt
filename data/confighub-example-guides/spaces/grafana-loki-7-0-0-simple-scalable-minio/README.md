<!-- Generated by npm run confighub-example-guides. Do not edit by hand. -->

# grafana/loki 7.0.0 - simple-scalable-minio

This guide follows one public Helm chart into a ConfigHub Space. It covers why the preset exists, how to repeat it, and what still needs care.

It is generated from the same records that build the package, the chart page and the render intent, along with the scripts and receipts. The proof links are lower down.

## Why this preset exists

A Helm chart exposes many settings, and a values file records the settings rather than the operations story around them. A team still has to know which Kubernetes objects get created and which Secrets or CRDs have to exist first. Hooks and setup jobs may need special handling, and something has to back the result.

This preset is a named answer to one operating choice. It keeps the upstream chart, records the inputs and the rendered YAML, and leaves the team a repeatable starting point instead of a private values-file guess.

## What this is

This is the `simple-scalable-minio` preset config for `grafana/loki@7.0.0`. The repo also calls this a base variant. loki simple scalable with MinIO variant rendered from grafana/loki@7.0.0.

The matching catalog page is [grafana/loki@7.0.0](https://confighub.github.io/helm-expt/site/charts/grafana-loki-7-0-0.html).

## The chart journey

We keep the Helm chart. We lock `grafana/loki@7.0.0` and choose the `simple-scalable-minio` preset config, then render it with the recorded values, namespace and release name against the recorded Kubernetes capabilities, and save the output as files.

That captured output is the render variant: [`recipes/grafana/loki/7.0.0/revisions/simple-scalable-minio/r001/rendered/release-objects.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/grafana/loki/7.0.0/revisions/simple-scalable-minio/r001/rendered/release-objects.yaml). It contains 33 Kubernetes object(s): Service x15, StatefulSet x5, ConfigMap x4, ServiceAccount x3, Deployment x2, ClusterRole x1, ClusterRoleBinding x1, DaemonSet x1.

The public package is `oci://europe-west1-docker.pkg.dev/nth-fort-499605-q5/helm-expt/grafana-loki:7.0.0@sha256:c89f127344879cdb6c7e4033735bcde8eaca3257d5c16bdcca89aad2a0f602da`. Users can pull these exact bytes without cloning this repo. When someone runs `cub installer upload`, ConfigHub stores the rendered Kubernetes YAML in a Space, where it can be searched and compared, reviewed and changed, then delivered. The example script defaults to Space `helm-loki-simple-scalable-minio`, but users can choose another name with `CUB_SPACE=...`.

## What to check

The current catalog record does not identify an extra Secret, CRD, or setup step that must be supplied before install.

The catalog does not currently record a separate hook, setup job, or cleanup step for this preset.

For this preset, the main change from plain Helm is that the render inputs and output files are recorded before upload.

## Why you can trust it

- The chart version, source, namespace, release name, values, and capability profile are recorded in the render intent.
- The render variant is committed as YAML and contains 33 Kubernetes object(s).
- The installer package OCI ref points to the package users pull for this chart version.
- Render parity is recorded as passing for this preset config.

This claim covers this recorded preset config alone. Every other values file for this chart remains unchecked.

## Repeat it

Fast path with no ConfigHub account:

```sh
bash <(curl -fsSL https://confighub.github.io/helm-expt/site/sh/grafana-loki-7-0-0/simple-scalable-minio/try.sh)
```

Fast path with a ConfigHub account:

```sh
bash <(curl -fsSL https://confighub.github.io/helm-expt/site/sh/grafana-loki-7-0-0/simple-scalable-minio/confighub.sh)
```

The core render command is:

```sh
cub installer setup --pull oci://europe-west1-docker.pkg.dev/nth-fort-499605-q5/helm-expt/grafana-loki:7.0.0@sha256:c89f127344879cdb6c7e4033735bcde8eaca3257d5c16bdcca89aad2a0f602da --base simple-scalable-minio --work-dir ./grafana-loki-7-0-0-simple-scalable-minio --non-interactive --namespace loki
```

After upload, create environment versions with `cub variant create` and move reviewed changes with `cub variant promote`. The walkthrough is [After upload: create a variant and promote changes](../../../../docs/user/variants-after-upload.md).

## Preset details

| Item | Value |
| --- | --- |
| Chart | `grafana/loki@7.0.0` |
| Preset config | `simple-scalable-minio` |
| Namespace | `loki` |
| Release name | `loki` |
| Values | [`recipes/grafana/loki/7.0.0/effective-values-simple-scalable-minio.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/grafana/loki/7.0.0/effective-values-simple-scalable-minio.yaml) |
| Render intent | [`data/helm-render-intents/intents/grafana-loki-7-0-0-simple-scalable-minio.yaml`](https://github.com/confighub/helm-expt/blob/main/data/helm-render-intents/intents/grafana-loki-7-0-0-simple-scalable-minio.yaml) |
| Render variant | [`recipes/grafana/loki/7.0.0/revisions/simple-scalable-minio/r001/rendered/release-objects.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/grafana/loki/7.0.0/revisions/simple-scalable-minio/r001/rendered/release-objects.yaml) |
| Package base | [`packages/grafana/loki/7.0.0/bases/simple-scalable-minio`](https://github.com/confighub/helm-expt/tree/main/packages/grafana/loki/7.0.0/bases/simple-scalable-minio) |
| Scripts | [try.sh](https://confighub.github.io/helm-expt/site/sh/grafana-loki-7-0-0/simple-scalable-minio/try.sh) · [confighub.sh](https://confighub.github.io/helm-expt/site/sh/grafana-loki-7-0-0/simple-scalable-minio/confighub.sh) |

## Prerequisites and lifecycle steps

| When | What | How it is handled |
| --- | --- | --- |
| No extra step recorded | The catalog has not identified a separate prerequisite or lifecycle step for this preset. | Check the limits and evidence before production use. |

## Evidence

| Check | Status |
| --- | --- |
| Render parity | `yes` |
| ConfigHub scan/upload proof | `yes` |
| Earlier local-cluster test | `yes` |
| GitOps OCI live run | `yes` |
| Live Helm vs ConfigHub comparison | `yes` |
| Lifecycle routes | `0` |

## Limits

- Production support is target-scoped. Use production support decisions before claiming production readiness.

## Source files

- Chart page: [https://confighub.github.io/helm-expt/site/charts/grafana-loki-7-0-0.html](https://confighub.github.io/helm-expt/site/charts/grafana-loki-7-0-0.html)
- Render intent: [`data/helm-render-intents/intents/grafana-loki-7-0-0-simple-scalable-minio.yaml`](https://github.com/confighub/helm-expt/blob/main/data/helm-render-intents/intents/grafana-loki-7-0-0-simple-scalable-minio.yaml)
- Rendered YAML: [`recipes/grafana/loki/7.0.0/revisions/simple-scalable-minio/r001/rendered/release-objects.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/grafana/loki/7.0.0/revisions/simple-scalable-minio/r001/rendered/release-objects.yaml)
- Package source: [`packages/grafana/loki/7.0.0/bases/simple-scalable-minio`](https://github.com/confighub/helm-expt/tree/main/packages/grafana/loki/7.0.0/bases/simple-scalable-minio)
- Generated scripts: [`site/sh/grafana-loki-7-0-0/simple-scalable-minio`](https://github.com/confighub/helm-expt/tree/main/site/sh/grafana-loki-7-0-0/simple-scalable-minio)
- Preset doctrine: [Helm Chart Presets And Values](../../../../docs/user/helm-presets-and-values.md)
