<!-- Generated by npm run confighub-example-guides. Do not edit by hand. -->

# bitnami/zookeeper 13.8.7 - ha

This guide shows how this Helm chart preset config becomes ConfigHub Units. It is generated from the same records that build the package, chart page, render intent, scripts, and receipts.

Start here when you want the short version. The proof links are lower down.

## What this is

This is the `ha` preset config for `bitnami/zookeeper@13.8.7`. The repo also calls this a base variant. Use this when you want the reviewed high-availability shape instead of the simplest default.

The matching catalog page is [bitnami/zookeeper@13.8.7](https://confighub.github.io/helm-expt/site/charts/bitnami-zookeeper-13-8-7.html).

## The chart journey

We keep the Helm chart. We lock `bitnami/zookeeper@13.8.7`, choose the `ha` preset config, render it with the recorded values, namespace, release name, and Kubernetes capabilities, then save the output as files.

That captured output is the render variant: [`recipes/bitnami/zookeeper/13.8.7/revisions/ha/r001/rendered/release-objects.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/bitnami/zookeeper/13.8.7/revisions/ha/r001/rendered/release-objects.yaml). It contains 7 Kubernetes object(s): Service x2, ConfigMap x1, NetworkPolicy x1, PodDisruptionBudget x1, ServiceAccount x1, StatefulSet x1.

The public package is `oci://europe-west1-docker.pkg.dev/nth-fort-499605-q5/helm-expt/bitnami-zookeeper:13.8.7`. Users can pull it without cloning this repo. When someone runs `cub installer upload`, ConfigHub stores the rendered objects as Units in a Space. The example script defaults to Space `helm-zookeeper-ha`, but users can choose another name with `CUB_SPACE=...`.

## What to check

No chart-specific prerequisite is recorded for this preset config beyond a cluster and namespace.

No hook or lifecycle route is recorded for this preset config.

Known limitation: existing-secret (chart ships no Secret toggle).

## Why this is correct

- The chart version, source, namespace, release name, values, and capability profile are recorded in the render intent.
- The render variant is committed as YAML and contains 7 Kubernetes object(s).
- The installer package OCI ref points to the package users pull for this chart version.
- Render parity is recorded as passing for this preset config.

This is a claim about this recorded preset config. It is not a claim that every possible values file for this chart has been checked.

## Repeat it

Fast path with no ConfigHub account:

```sh
bash <(curl -fsSL https://confighub.github.io/helm-expt/site/sh/bitnami-zookeeper-13-8-7-ha/try.sh)
```

Fast path with a ConfigHub account:

```sh
bash <(curl -fsSL https://confighub.github.io/helm-expt/site/sh/bitnami-zookeeper-13-8-7-ha/confighub.sh)
```

The core render command is:

```sh
cub installer setup --pull oci://europe-west1-docker.pkg.dev/nth-fort-499605-q5/helm-expt/bitnami-zookeeper:13.8.7 --base ha --work-dir ./bitnami-zookeeper-13-8-7-ha --non-interactive --namespace default
```

After upload, create environment versions with `cub variant create` and move reviewed changes with `cub variant promote`. The walkthrough is [After Upload: Create A Variant And Promote Changes](../../../../docs/user/variants-after-upload.md).

## Preset details

| Item | Value |
| --- | --- |
| Chart | `bitnami/zookeeper@13.8.7` |
| Preset config | `ha` |
| Namespace | `default` |
| Release name | `zookeeper` |
| Values | [`recipes/bitnami/zookeeper/13.8.7/effective-values-ha.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/bitnami/zookeeper/13.8.7/effective-values-ha.yaml) |
| Render intent | [`data/helm-render-intents/intents/bitnami-zookeeper-13-8-7-ha.yaml`](https://github.com/confighub/helm-expt/blob/main/data/helm-render-intents/intents/bitnami-zookeeper-13-8-7-ha.yaml) |
| Render variant | [`recipes/bitnami/zookeeper/13.8.7/revisions/ha/r001/rendered/release-objects.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/bitnami/zookeeper/13.8.7/revisions/ha/r001/rendered/release-objects.yaml) |
| Package base | [`packages/bitnami/zookeeper/13.8.7/bases/ha`](https://github.com/confighub/helm-expt/tree/main/packages/bitnami/zookeeper/13.8.7/bases/ha) |
| Scripts | [try.sh](https://confighub.github.io/helm-expt/site/sh/bitnami-zookeeper-13-8-7-ha/try.sh) · [confighub.sh](https://confighub.github.io/helm-expt/site/sh/bitnami-zookeeper-13-8-7-ha/confighub.sh) |

## Prerequisites

| Kind | What | How to provide it |
| --- | --- | --- |
| None recorded | This preset does not record chart-specific prerequisites beyond a cluster and namespace. | - |

## Evidence

| Check | Status |
| --- | --- |
| Render parity | `yes` |
| ConfigHub scan/upload proof | `yes` |
| Local kind run | `no` |
| GitOps OCI live run | `watch` |
| Live Helm vs ConfigHub comparison | `watch` |
| Lifecycle routes | `0` |

## Limits

- Local kind evidence is no for this preset config.
- GitOps OCI live evidence is watch for this preset config.
- Known gap for this row: existing-secret (chart ships no Secret toggle).
- Resolve the named gap first: existing-secret (chart ships no Secret toggle).

## Source files

- Chart page: [https://confighub.github.io/helm-expt/site/charts/bitnami-zookeeper-13-8-7.html](https://confighub.github.io/helm-expt/site/charts/bitnami-zookeeper-13-8-7.html)
- Render intent: [`data/helm-render-intents/intents/bitnami-zookeeper-13-8-7-ha.yaml`](https://github.com/confighub/helm-expt/blob/main/data/helm-render-intents/intents/bitnami-zookeeper-13-8-7-ha.yaml)
- Rendered YAML: [`recipes/bitnami/zookeeper/13.8.7/revisions/ha/r001/rendered/release-objects.yaml`](https://github.com/confighub/helm-expt/blob/main/recipes/bitnami/zookeeper/13.8.7/revisions/ha/r001/rendered/release-objects.yaml)
- Package source: [`packages/bitnami/zookeeper/13.8.7/bases/ha`](https://github.com/confighub/helm-expt/tree/main/packages/bitnami/zookeeper/13.8.7/bases/ha)
- Generated scripts: [`site/sh/bitnami-zookeeper-13-8-7-ha`](https://github.com/confighub/helm-expt/tree/main/site/sh/bitnami-zookeeper-13-8-7-ha)
- Preset doctrine: [Helm Chart Presets And Values](../../../../docs/user/helm-presets-and-values.md)
