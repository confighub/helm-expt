# eks-inference-inference-workloads

<!-- Generated by npm run certified-bundles. Do not edit by hand. -->

This Space holds one certified bundle, ingested one Unit per file. Its source is literal yaml.

## What produced it

Producer: eks-inference. Contents: literal-config, 5 file(s).

## Whether it may ship as plain YAML

Lane: **born-flattened** (certified). Nothing renders here, so nothing is lost at render time.

Decided by: the producer's source for this component is literal YAML; nothing renders, so nothing is lost at render time

## What its routes owe

This bundle owes no route.

## How it was ingested

```sh
cub variant upload --component inference-workloads --variant base --granularity per-file oci://ghcr.io/confighub/configs/eks-inference/inference-workloads
```

The resolved digest is recorded on this Space as a `confighub.com/external-source` annotation, so the exact bytes installed here stay auditable.

