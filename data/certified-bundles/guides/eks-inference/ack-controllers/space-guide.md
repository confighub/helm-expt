# eks-inference-ack-controllers

<!-- Generated by npm run certified-bundles. Do not edit by hand. -->

This Space holds one certified bundle, ingested one Unit per file. Its source is helm chart.

## What produced it

Producer: eks-inference. Contents: rendered-config, 8 file(s).

## Whether it may ship as plain YAML

Lane: **flatten-with-routes** (certified). This bundle needs companion artifacts to deliver safely. They travel with it, and they are listed below.

Decided by: the strictest of 3 cited flattening-safety verdicts, recipes/aws-controllers-k8s/ec2-chart/1.18.4/publication/flattening-safety-verdict.yaml

## What its routes owe

This bundle ships the following routes. A delivery runtime executes them; they are not documentation.

- `data/certified-bundles/routes/eks-inference/ack-controllers/crd-ordering.yaml` discharges crd-ordering

## How it was ingested

```sh
cub variant upload --component ack-controllers --variant base --granularity per-file oci://ghcr.io/confighub/configs/eks-inference/ack-controllers
```

The resolved digest is recorded on this Space as a `confighub.com/external-source` annotation, so the exact bytes installed here stay auditable.

