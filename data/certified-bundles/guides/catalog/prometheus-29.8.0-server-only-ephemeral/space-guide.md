# catalog-prometheus-community-prometheus-29.8.0-server-only-ephemeral

<!-- Generated by npm run certified-bundles. Do not edit by hand. -->

This Space holds one certified bundle, ingested one Unit per file. It was produced from prometheus 29.8.0.

## What produced it

Producer: config-workshop-catalog. Contents: rendered-config, 1 file(s).

## Whether it may ship as plain YAML

Lane: **safe-to-flatten** (certified). Nothing this bundle carries is discharged at render time, so it delivers as it stands.

Decided by: the flattening-safety audit at recipes/prometheus-community/prometheus/29.8.0/publication/flattening-safety-verdict-server-only-ephemeral.yaml

## What its routes owe

This bundle owes no route.

## How it was ingested

```sh
cub variant upload --component catalog-prometheus-community-prometheus-29.8.0-server-only-ephemeral --variant base --granularity per-file <bundle>
```

The resolved digest is recorded on this Space as a `confighub.com/external-source` annotation, so the exact bytes installed here stay auditable.

