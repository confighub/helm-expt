# catalog-grafana-rollout-operator-0.49.0-no-crds

<!-- Generated by npm run certified-bundles. Do not edit by hand. -->

This Space holds one certified bundle, ingested one Unit per file. It was produced from rollout-operator 0.49.0.

## What produced it

Producer: config-workshop-catalog. Contents: rendered-config, 1 file(s).

## Whether it may ship as plain YAML

Lane: **flatten-with-routes** (certified). This bundle needs companion artifacts to deliver safely. They travel with it, and they are listed below.

Decided by: the flattening-safety audit at recipes/grafana/rollout-operator/0.49.0/publication/flattening-safety-verdict-no-crds.yaml

## What its routes owe

This bundle's lane requires companion artifacts and it ships none yet. Treat its ordering and lifecycle work as unresolved until a route arrives.

## How it was ingested

```sh
cub variant upload --component catalog-grafana-rollout-operator-0.49.0-no-crds --variant base --granularity per-file <bundle>
```

The resolved digest is recorded on this Space as a `confighub.com/external-source` annotation, so the exact bytes installed here stay auditable.

