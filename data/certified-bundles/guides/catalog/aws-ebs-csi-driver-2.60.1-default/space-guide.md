# catalog-aws-ebs-csi-driver-aws-ebs-csi-driver-2.60.1-default

<!-- Generated by npm run certified-bundles. Do not edit by hand. -->

This Space holds one certified bundle, ingested one Unit per file. It was produced from aws-ebs-csi-driver 2.60.1.

## What produced it

Producer: config-workshop-catalog. Contents: rendered-config, 1 file(s).

## Whether it may ship as plain YAML

Lane: **safe-to-flatten** (certified). Nothing this bundle carries is discharged at render time, so it delivers as it stands.

Decided by: the flattening-safety audit at recipes/aws-ebs-csi-driver/aws-ebs-csi-driver/2.60.1/publication/flattening-safety-verdict.yaml

## What its routes owe

This bundle owes no route.

## How it was ingested

```sh
cub variant upload --component catalog-aws-ebs-csi-driver-aws-ebs-csi-driver-2.60.1-default --variant base --granularity per-file <bundle>
```

The resolved digest is recorded on this Space as a `confighub.com/external-source` annotation, so the exact bytes installed here stay auditable.

