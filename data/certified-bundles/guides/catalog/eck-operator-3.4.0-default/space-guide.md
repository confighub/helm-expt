# catalog-elastic-eck-operator-3.4.0-default

<!-- Generated by npm run certified-bundles. Do not edit by hand. -->

This Space holds one certified bundle, ingested one Unit per file. It was produced from eck-operator 3.4.0.

## What produced it

Producer: config-workshop-catalog. Contents: rendered-config, 3 file(s).

## Whether it may ship as plain YAML

Lane: **flatten-with-routes** (certified). This bundle needs companion artifacts to deliver safely. They travel with it, and they are listed below.

Decided by: the flattening-safety audit at recipes/elastic/eck-operator/3.4.0/publication/flattening-safety-verdict.yaml

## What its routes owe

This bundle ships the following routes. A delivery runtime executes them; they are not documentation.

- `data/certified-bundles/routes/catalog/elastic-eck-operator-3.4.0-default/crd-ordering.yaml` discharges crd-ordering
- `data/certified-bundles/routes/catalog/elastic-eck-operator-3.4.0-default/prune-protection.yaml` discharges resource-policy-keep

## How it was ingested

```sh
cub variant upload --component catalog-elastic-eck-operator-3.4.0-default --variant base --granularity per-file <bundle>
```

The resolved digest is recorded on this Space as a `confighub.com/external-source` annotation, so the exact bytes installed here stay auditable.

