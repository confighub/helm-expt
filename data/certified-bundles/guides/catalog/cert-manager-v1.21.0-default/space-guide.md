# catalog-jetstack-cert-manager-v1.21.0-default

<!-- Generated by npm run certified-bundles. Do not edit by hand. -->

This Space holds one certified bundle, ingested one Unit per file. It was produced from cert-manager v1.21.0.

## What produced it

Producer: config-workshop-catalog. Contents: rendered-config, 2 file(s).

## Whether it may ship as plain YAML

Lane: **flatten-with-routes** (certified). This bundle needs companion artifacts to deliver safely. They travel with it, and they are listed below.

Decided by: the flattening-safety audit at recipes/jetstack/cert-manager/v1.21.0/publication/flattening-safety-verdict.yaml

## What its routes owe

This bundle ships the following route as a manual declaration for a human to follow; no delivery runtime execution is claimed.

- `data/certified-bundles/routes/catalog/jetstack-cert-manager-v1.21.0-default/lifecycle.yaml` discharges helm-hooks

## Notes

The default bundle does not own cert-manager's six CRDs. Extract only those CRDs from the v1.21.0 crds-enabled object set, server-side apply them, and wait for Established before applying this default bundle. Follow the exact manual lifecycle contract at examples/cert-manager-v121-default-lifecycle/contract.yaml and run the exact startup API-check payload at examples/cert-manager-v121-default-lifecycle/startupapicheck.yaml. The lifecycle route is a manual declaration for a human to follow, not a runtime execution claim or automatic GitOps execution; webhook caBundle remains controller-owned.

## How it was ingested

```sh
cub variant upload --component catalog-jetstack-cert-manager-v1.21.0-default --variant base --granularity per-file oci://europe-west1-docker.pkg.dev/nth-fort-499605-q5/helm-expt/bundles/catalog-jetstack-cert-manager-v1-21-0-default
```

The resolved digest is recorded on this Space as a `confighub.com/external-source` annotation, so the exact bytes installed here stay auditable.

