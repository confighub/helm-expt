# catalog-grafana-tempo-1.24.4-local-persistent

<!-- Generated by npm run certified-bundles. Do not edit by hand. -->

This Space holds one certified bundle, ingested one Unit per file. It was produced from tempo 1.24.4.

## What produced it

Producer: config-workshop-catalog. Contents: rendered-config, 1 file(s).

## Whether it may ship as plain YAML

Lane: **safe-to-flatten** (certified). Nothing this bundle carries is discharged at render time, so it delivers as it stands.

Decided by: the flattening-safety audit at recipes/grafana/tempo/1.24.4/publication/flattening-safety-verdict-local-persistent.yaml

## What its routes owe

This bundle owes no route.

## How it was ingested

```sh
cub variant upload --component catalog-grafana-tempo-1.24.4-local-persistent --variant base --granularity per-file <bundle>
```

The resolved digest is recorded on this Space as a `confighub.com/external-source` annotation, so the exact bytes installed here stay auditable.

