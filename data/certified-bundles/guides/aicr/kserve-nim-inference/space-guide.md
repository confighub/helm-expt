# aicr-kserve-nim-inference

<!-- Generated by npm run certified-bundles. Do not edit by hand. -->

This Space holds one certified bundle, ingested one Unit per file. Its source is retained upstream tree.

## What produced it

Producer: aicr. Contents: literal-config, 26 file(s).

## Whether it may ship as plain YAML

Lane: **born-flattened** (certified). Nothing renders here, so nothing is lost at render time.

Decided by: the source is literal YAML retained from upstream; nothing renders, so nothing is lost at render time

## What its routes owe

This bundle owes no route.

## How it was ingested

```sh
cub variant upload --component aicr-kserve-nim-inference --variant base --granularity per-file <bundle>
```

The resolved digest is recorded on this Space as a `confighub.com/external-source` annotation, so the exact bytes installed here stay auditable.

