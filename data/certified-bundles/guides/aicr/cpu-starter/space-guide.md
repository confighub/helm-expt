# aicr-cpu-starter

<!-- Generated by npm run certified-bundles. Do not edit by hand. -->

This Space holds one certified bundle, ingested one Unit per file. Its source is aicr entry.

## What produced it

Producer: aicr. Contents: rendered-config, 8 file(s).

## Whether it may ship as plain YAML

Lane: **flatten-with-routes** (certified). This bundle needs companion artifacts to deliver safely. They travel with it, and they are listed below.

Decided by: the platform-shape flattening verdict at data/aicr-flattening-verdicts/aicr-cpu-starter/flattening-safety-verdict.yaml

## What its routes owe

This bundle ships the following routes. A delivery runtime executes them; they are not documentation.

- `data/certified-bundles/routes/aicr/aicr-cpu-starter/sync-wave-ordering.yaml` discharges crd-ordering

## How it was ingested

```sh
cub variant upload --component aicr-cpu-starter --variant base --granularity per-file <bundle>
```

The resolved digest is recorded on this Space as a `confighub.com/external-source` annotation, so the exact bytes installed here stay auditable.

