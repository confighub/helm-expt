# Round two verdict: time, state, composition, and accountability questions

VERDICTS (pre-registered rules applied; house receipts spot-checked in the repo — drift.csv digest pairs, both republished witnesses, and `approverIdentityRecordedInReceipt: false` all verified as cited)

RACED
1. time-republish-forensic-1 — DISCRIMINATES (strong). 32 min (>10), and even a genuinely excellent forensic run ends at "cannot cryptographically confirm 9498a6f4 was ever served" and "the exact byte-diff is unrecoverable"; the house holds the locked receipt + 103-file witness. Near-fabrication flag: the agent's confident "the delta is confined to packaging metadata... chart logic unchanged" exceeds its evidence (it verified git-vs-current, not vetted-tarball-vs-current), though it hedged the stake correctly.
2. time-republish-sweep-2 — DISCRIMINATES (strong). Bare agent: CANNOT-DETERMINE, correctly reasoning superseded digests are overwritten in place with no transparency log; house: exactly two (goldilocks 10.3.0, vpa 4.11.0) with both digest pairs, verified in drift.csv. Note: even the agent's Fairwinds lead missed vpa 4.11.0 and flagged fairwinds-insights instead.
3. time-crd-upgrade-delta-3 — DISCRIMINATES (marginal, time-only). 12 min vs the 10-min line; the answer itself matched the receipt exactly (same 6 changed CRDs, same additive paths, no flips) and it staked production. Expect this to commoditize on a re-run.
4. time-destructive-render-diff-4 — DISCRIMINATES (weak, stake-only). Answer matched the committed renders down to the one added ConfigMap by name; 10 min; survives only because it declined to stake production (a principled default-values caveat) while the house would.
5. loki-minimal-converging-values — DISCRIMINATES (marginal, time-only). 14 min, needed a live kind cluster, but correct on both shapes and would stake — and it found the hard-anti-affinity Pending trap in more operational detail than the cited receipts. Arguably outperformed the house on content.
6. lookup-argo-vs-flux-drift — COMMODITY. 4 min, correct, staked. It asserted the Flux leg instead of running it (as the designer predicted) but the assertion matched the receipt, and the rules don't score proof-vs-assertion when the answer is right.
7. crd-ordering-delivery-paths — COMMODITY. 5 min, correct on the failure string, the fix, and the path matrix; added the --include-crds layer the question didn't ask for.
8. time-values-silent-keys-5 — COMMODITY. 9 min, correct (typo silently swallowed, identical objects); the random-password-per-render nuance is consistent with, not contradicting, the receipt's identical-SHA setup.
9. time-upstream-lag-7 — DISCRIMINATES (strong). Bare agent: CANNOT-DETERMINE and explicitly refused to fabricate the table; house: 7 of 20 with candidates, all 7 candidate-proof-complete, all gated on an explicit replacement decision. Cleanest proof that promotion/review state has no upstream source at all.
10. webhook-cert-staging-per-chart — DISCRIMINATES (moderate). 16 min (>10), and it silently answered for different artifacts than the fleet runs: kube-prometheus-stack 88.3.0 vs the proven 86.1.0/85.3.3, and the cowboysysop vertical-pod-autoscaler 11.1.1 instead of autoscaler/vertical-pod-autoscaler 0.9.0 — plausible-looking guidance unmoored from the environment's pins. Where charts coincided (ingress-nginx-admission, vpa-tls-secret, 10/2 CRD counts) it matched.

STRUCTURAL
11. time-edit-survives-upgrade-6 — STRUCTURAL (counts). helm rollback re-renders; recorded revisions don't exist in the Helm model — absent records in principle, and the house proof exists (runs/redis-upgrade-app-proof).
12. time-evidence-age-8 — STRUCTURAL (counts). A claim's age is invisible to anything that only sees the claim; you cannot date someone else's proof after the fact. Receipt-aging surface is committed.
13. kps-hidden-lifecycle-work — FAILS STRUCTURAL TEST. The whyHard is "hours of cluster work" — expense, not principle; a bare agent with clusters and 4 hours reproduces it. Misfiled: race it next round or drop it. (House evidence is fine; the classification isn't.)
14. template-vs-live-fleet-gap — FAILS STRUCTURAL TEST. Same defect: "hundreds of cluster-pair runs" is cost, not impossibility. The per-row answers are individually reproducible. Race a sampled subset or reframe around the point-in-time fleet record.
15. fleet-digest-accountability-1 — STRUCTURAL (counts, lean). The desired-state matrix and the reconciliation receipt binding it to observation are the platform's own records — absent authority for any outside agent, even one holding cluster credentials (it can observe, it cannot produce the standing desired-vs-observed binding).
16. rollback-history-1 — STRUCTURAL (counts, strongest of the set). The v0.12.0 state exists on no cluster; destroyed information, and the frozen historical surface plus the no-rewrite policy are exactly the answer.
17. approval-binding-1 — UNPROVEN. House's own receipt says approverIdentityRecordedInReceipt: false, apply was dry-run against OCI only, and the Space was deleted. The mechanism (hash-stable, head-bound, blocked-then-allowed) is proven; the question as asked — production, by an identified person — has no receipt on either side.
18. ai-edit-provenance-1 — STRUCTURAL (counts, with framing caveats). The proposal's contents and per-check verdicts are three committed records unreconstructable after the fact. Publish only with the record's own honesty notes attached: it is a deterministic fixture, not a real AI transcript, and "would have taken production down" is what the checks assert, not an observed outage.

COUNT
12 of 18 survive by the pre-registered rules (7 of 10 raced discriminate; 5 of 8 structural count). The robust core is 9: three raced survive on technicalities alone (12 and 14 minutes against a 10-minute line; one stake-refusal that was itself good judgment) and would likely flip to COMMODITY against a marginally faster or bolder agent. Two structural questions failed the principle test as cost-dressed-as-impossibility, and one is UNPROVEN by the house's own honesty. Against round one's 96.7%-commodity baseline: static questions were commodities; roughly half to two-thirds of TIME/STATE/ACCOUNTABILITY questions are not, and the ones that discriminate hardest are exactly the destroyed-information and catalog-internal-state questions.

FABRICATION WATCH
Zero outright receipt contradictions — notable and worth publishing as such. Two near-misses: (a) forensic-1's confident contents-unchanged claim outruns its evidence; (b) webhook-cert's wrong-chart drift (cowboysysop for autoscaler/vpa, 88.3.0 for 86.1.0) — correct-sounding answers about artifacts the fleet doesn't run, which is the failure mode a pinned catalog exists to prevent.

THREE SHARPEST DEMONSTRATIONS
1. time-republish-sweep-2 — the bare agent's own words concede the impossibility ("only in consumers' own pull logs, lockfiles, or caches"), it refuses to fabricate, and the house answers in one minute with both digest pairs from a verified sweep. Honest because the agent behaved impeccably and still could not answer.
2. time-republish-forensic-1 — the best-case bare agent: 32 minutes of superb public-source forensics (it even found the sync.sh root cause the receipts don't record) that still terminates at "unrecoverable" for the two things the on-call engineer actually needs — proof the vetted bytes existed and what changed inside. Retained-bytes-plus-witness answers both. Publish the agent's transcript alongside; its quality is what makes the gap credible.
3. time-upstream-lag-7 — the crispest statement of the thesis: "latest on Artifact Hub says nothing about review state." The agent could get half the answer from upstream and correctly identified the other half as having no source anywhere but the catalog's own promotion records.
(Honorable mention: webhook-cert's wrong-chart drift, if you want a vivid pins-matter exhibit.)

BUILD LIST (where the house failed its own test)
1. approval-binding-1 (the only outright NO-RECEIPT): run the approval flow in a real, retained production Space with approver identity recorded in the receipt, a non-dry-run apply, and the approved revision shown equal to live head. Until then the publishable claim is mechanism-only.
2. forensic-1 boundary the house itself named: no committed per-file diff of retained vs republished goldilocks bytes — build the file-level diff surface so "what changed inside" upgrades from structural-profile to enumerated files.
3. diff-4: no live upgrade receipt for 85.3.3→87.19.2 (only →86.1.0), and the committed renders are hookless — either record the two-major live upgrade or scope the question to render-level.
4. sweep-2 / lag-7 freshness: the drift sweep is point-in-time (2026-08-07) and the top-20 refresh snapshot is 2026-06-10 — both questions say "this year"/"now"; a recurring sweep/refresh lane is needed before publishing present-tense claims.
5. silent-keys-5: only the mechanism plus one synthetic case is proven; an exhaustive ignored-key surface per user values file (the thing the question literally asks for) is not committed.
6. Minor: webhook-cert table lacks an 85.3.3-default staged row; kps-hidden-lifecycle-work and template-vs-live-fleet-gap need re-filing as raced-empirical (with a sampled bare-agent arm) or their whyHard rewritten around a genuinely principled gap, not hours.

Key evidence paths: <repo>/data/upstream-drift/drift.csv, <repo>/runs/ai-change-review-live-proof/receipt.yaml (approverIdentityRecordedInReceipt: false), <repo>/data/flattening-safety/witnesses/fairwinds-stable-{goldilocks-10.3.0,vpa-4.11.0}-republished.yaml.