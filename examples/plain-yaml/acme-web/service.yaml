apiVersion: v1
kind: Service
metadata:
  name: acme-web
  namespace: acme-web
  labels:
    app.kubernetes.io/name: acme-web
spec:
  selector:
    app.kubernetes.io/name: acme-web
  ports:
    - name: http
      port: 80
      targetPort: http
