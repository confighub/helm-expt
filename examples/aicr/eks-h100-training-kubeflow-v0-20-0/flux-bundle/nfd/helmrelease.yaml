# Generated by AICR
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nfd
  namespace: flux-system
spec:
  interval: 10m
  targetNamespace: node-feature-discovery
  install:
    createNamespace: true
  chart:
    spec:
      chart: node-feature-discovery
      version: 0.19.0
      sourceRef:
        kind: HelmRepository
        name: kubernetes-sigs-github-io-node-feature-discovery-charts
        namespace: flux-system
      interval: 5m
  values:
    gc:
      enable: true
      tolerations:
        - operator: Exists
    master:
      enable: true
      tolerations:
        - operator: Exists
    topologyUpdater:
      createCRDs: true
      enable: true
      kubeletStateDir: ""
      resources:
        limits:
          memory: 256Mi
        requests:
          cpu: 50m
          memory: 128Mi
      tolerations:
        - operator: Exists
    worker:
      enable: true
      tolerations:
        - operator: Exists
