---
# Source: aicr-eks-h100-training-kubeflow-argocd/templates/aicr-stack.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: "aicr-stack"
  namespace: argocd
spec:
  project: default
  source:
    repoURL: "oci://europe-west1-docker.pkg.dev/nth-fort-499605-q5/helm-expt/aicr-eks-h100-training-kubeflow-argocd"
    targetRevision: "0.19.0"
    path: "."
    helm:
      valuesObject:
        deployer:
          destinationServer: https://kubernetes.default.svc
          includeRootApp: true
          namePrefix: ""
          project: default
        repoURL: oci://europe-west1-docker.pkg.dev/nth-fort-499605-q5/helm-expt
        targetRevision: ""
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      # ServerSideApply: required for charts whose CRDs exceed kubectl's
      # 262144-byte client-side annotation cap. See application.yaml.tmpl
      # in the upstream argocd deployer for the full rationale.
      - ServerSideApply=true
