apiVersion: "installer.confighub.com/v1alpha1"
kind: "Package"
metadata:
  name: "prometheus-community-prometheus-blackbox-exporter"
  version: "11.10.0"
spec:
  bases:
    -
      default: true
      description: "prometheus-community/prometheus-blackbox-exporter default variant rendered from prometheus-community/prometheus-blackbox-exporter@11.10.0"
      name: "default"
      path: "bases/default"
    -
      default: false
      description: "prometheus-community/prometheus-blackbox-exporter cluster-metrics-readonly useful base; aliases the default render with a named user start path"
      name: "cluster-metrics-readonly"
      path: "bases/cluster-metrics-readonly"
