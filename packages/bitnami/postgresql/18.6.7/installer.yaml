apiVersion: "installer.confighub.com/v1alpha1"
kind: "Package"
metadata:
  name: "bitnami-postgresql"
  version: "18.6.7"
spec:
  bases:
    -
      description: "postgresql static password demo variant rendered from bitnami/postgresql@18.6.7"
      name: "static-passwords"
      path: "bases/static-passwords"
    -
      description: "postgresql existing Secret default variant rendered from bitnami/postgresql@18.6.7"
      externalRequires:
        -
          kind: "ClusterFeature"
          name: "Secret postgresql/postgresql-auth key postgres-password"
          namespace: "postgresql"
          suggestedSource: "kubectl -n postgresql create secret generic postgresql-auth --from-literal=postgres-password=\"$(openssl rand -base64 32)\""
      name: "existing-secret"
      path: "bases/existing-secret"
      default: true
  collector:
    args:
      - "collector/target-facts.sh"
    command: "/bin/sh"
    description: "Records target-fact bindings and can live-check cluster-visible requirements."
  transformers:
    -
      description: "Set the namespace on every namespaced resource."
      invocations:
        -
          args:
            - "{{ .Namespace }}"
          name: "set-namespace"
      toolchain: "Kubernetes/YAML"
      whereResource: ""
