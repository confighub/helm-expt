apiVersion: "installer.confighub.com/v1alpha1"
kind: "Package"
metadata:
  name: "ingress-nginx-ingress-nginx"
  version: "4.15.1"
spec:
  bases:
    -
      name: "default"
      path: "bases/default"
      default: true
      description: "ingress-nginx default variant rendered from ingress-nginx/ingress-nginx@4.15.1"
    -
      name: "admission-disabled"
      path: "bases/admission-disabled"
      description: "ingress-nginx admission webhook disabled variant rendered from ingress-nginx/ingress-nginx@4.15.1"
  transformers:
    # F1 fix: honour `cub installer --namespace X` instead of the frozen upstream
    # literal — set metadata.namespace on every namespaced resource and rename
    # the Namespace object. Pattern: installer examples/hello-app/installer.yaml.
    - toolchain: "Kubernetes/YAML"
      whereResource: ""
      description: "Set the namespace on every namespaced resource."
      invocations:
        - name: "set-namespace"
          args: ["{{ .Namespace }}"]
