# argo-cd/argo-workflows 1.0.14 Installer Package

This package is generated from the next80 full proof artifacts.

Start with the [plain-English operations guide](../../../../docs/demo/hooks-crds/argo-workflows.md). It explains why this package exists, what setup work it replaces, how to run it, and what remains unproven.

```sh
npm run next80:generate
npm run next80:verify
npm run next80:verify-packages
```
