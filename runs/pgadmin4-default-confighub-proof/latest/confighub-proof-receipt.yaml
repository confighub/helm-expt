apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "pgadmin4-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.80"
  package:
    path: "packages/runix/pgadmin4/1.62.0"
    name: "runix-pgadmin4"
    chart: "runix/pgadmin4"
    chartVersion: "1.62.0"
    bases:
      - "default"
    selectedBase: "default"
    docVerified: true
    targetFactRequirements:
      []
  render:
    command: "cub installer setup --pull packages/runix/pgadmin4/1.62.0 --base default --work-dir .tmp/confighub-proof/pgadmin4-default --non-interactive --namespace default"
    result: "pass"
    manifestCount: 5
    separatedSecretCount: 1
    facts:
      {}
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/pgadmin4-default"
    result: "pass"
    manifestCount: 5
    separatedSecretCount: 1
  deterministicPackage:
    command: "cub installer package packages/runix/pgadmin4/1.62.0 -o .tmp/confighub-proof/pgadmin4-default-archives/pgadmin4-a.tgz"
    sha256: "a7ebe7db9ddd892a94b0038baf48ab9900c59f3f959a55a59599fb248724aef2"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/pgadmin4-default"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/pgadmin4-default --space helm-pgadmin4-default-confighub-proof --component Pgadmin4 --layer App --environment Demo --owner ConfigHubHelm --variant default --unit-label Component=Pgadmin4 --unit-label HelmChart=runix-pgadmin4 --unit-label HelmChartVersion=1.62.0 --unit-label Variant=default --unit-label Proof=pgadmin4-default-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=$HOME/.confighub/config.yaml"
    space: "helm-pgadmin4-default-confighub-proof"
    unitCount: 6
    kubernetesUnitCount: 5
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      - "v1/Secret default/pgadmin4"
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/pgadmin4-default"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/pgadmin4-default"
    result: "pass"
    summary: "No changes.\nImages in helm-pgadmin4-default-confighub-proof (post-render):\n      Deployment/pgadmin4 [pgadmin4] docker.io/dpage/pgadmin4:9.13"
  serverSideVariant:
    command: "cub variant create staging helm-pgadmin4-default-confighub-proof --environment Staging --region local --namespace default --space-pattern template:Pgadmin4-default-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-pgadmin4-default-confighub-proof"
    downstreamSpace: "Pgadmin4-default-staging"
    clonedUnitCount: 6
    namespace: "default"
  serverSidePromotion:
    receipt: "variant-promotion-receipt.yaml"
    result: "pass"
    reason: "server-side promotion passed through a changeset"
    changedUnitCaughtUp: "pass"
    addedUnitCloned: "pass"
    deletionHandling: "not-tested"
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "deployment-default-pgadmin4"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-pgadmin4-default-confighub-proof"
      - "Pgadmin4-default-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup ($HOME/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "rendered Secret resources are separated from ConfigHub Units and must be managed out-of-band."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
