apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "minio-operator-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.79"
  package:
    path: "packages/minio-operator/operator/7.1.1"
    name: "minio-operator-operator"
    chart: "minio-operator/operator"
    chartVersion: "7.1.1"
    bases:
      - "default"
    selectedBase: "default"
    docVerified: true
    targetFactRequirements:
      []
  render:
    command: "cub installer setup --pull packages/minio-operator/operator/7.1.1 --base default --work-dir .tmp/confighub-proof/minio-operator-default --non-interactive --namespace default"
    result: "pass"
    manifestCount: 9
    separatedSecretCount: 0
    facts:
      {}
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/minio-operator-default"
    result: "pass"
    manifestCount: 9
    separatedSecretCount: 0
  deterministicPackage:
    command: "cub installer package packages/minio-operator/operator/7.1.1 -o .tmp/confighub-proof/minio-operator-default-archives/minio-operator-a.tgz"
    sha256: "030f54948db823e7cfce72fbcfabcbf331c14562d1efa4e06006866094c8895c"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/minio-operator-default"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/minio-operator-default --space helm-minio-operator-default-confighub-proof --component MinioOperator --layer App --environment Demo --owner ConfigHubHelm --variant default --unit-label Component=MinioOperator --unit-label HelmChart=minio-operator-operator --unit-label HelmChartVersion=7.1.1 --unit-label Variant=default --unit-label Proof=minio-operator-default-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=/Users/alexis/.confighub/config.yaml"
    space: "helm-minio-operator-default-confighub-proof"
    unitCount: 10
    kubernetesUnitCount: 9
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      []
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/minio-operator-default"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/minio-operator-default"
    result: "pass"
    summary: "No changes.\nImages in helm-minio-operator-default-confighub-proof (post-render):\n      Deployment/minio-operator [operator] quay.io/minio/operator:v7.1.1"
  serverSideVariant:
    command: "cub variant create staging helm-minio-operator-default-confighub-proof --environment Staging --region local --space-name-pattern template:MinioOperator-default-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-minio-operator-default-confighub-proof"
    downstreamSpace: "MinioOperator-default-staging"
    clonedUnitCount: 10
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "deployment-default-minio-operator"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-minio-operator-default-confighub-proof"
      - "MinioOperator-default-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup (/Users/alexis/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
