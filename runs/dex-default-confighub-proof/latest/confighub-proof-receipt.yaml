apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "dex-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.79"
  package:
    path: "packages/dex/dex/0.24.0"
    name: "dex-dex"
    chart: "dex/dex"
    chartVersion: "0.24.0"
    bases:
      - "default"
    selectedBase: "default"
    docVerified: true
    targetFactRequirements:
      []
  render:
    command: "cub installer setup --pull packages/dex/dex/0.24.0 --base default --work-dir .tmp/confighub-proof/dex-default --non-interactive --namespace default"
    result: "pass"
    manifestCount: 8
    separatedSecretCount: 1
    facts:
      {}
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/dex-default"
    result: "pass"
    manifestCount: 8
    separatedSecretCount: 1
  deterministicPackage:
    command: "cub installer package packages/dex/dex/0.24.0 -o .tmp/confighub-proof/dex-default-archives/dex-a.tgz"
    sha256: "61f72819c33fc6b8c388808c061dc9686cbd6d96432fce8fe2b3eb319e968ab1"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/dex-default"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/dex-default --space helm-dex-default-confighub-proof --component Dex --layer App --environment Demo --owner ConfigHubHelm --variant default --unit-label Component=Dex --unit-label HelmChart=dex-dex --unit-label HelmChartVersion=0.24.0 --unit-label Variant=default --unit-label Proof=dex-default-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=/Users/alexis/.confighub/config.yaml"
    space: "helm-dex-default-confighub-proof"
    unitCount: 9
    kubernetesUnitCount: 8
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      - "v1/Secret default/dex"
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/dex-default"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/dex-default"
    result: "pass"
    summary: "No changes.\nImages in helm-dex-default-confighub-proof (post-render):\n      Deployment/dex [dex] ghcr.io/dexidp/dex:v2.44.0"
  serverSideVariant:
    command: "cub variant create staging helm-dex-default-confighub-proof --environment Staging --region local --space-name-pattern template:Dex-default-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-dex-default-confighub-proof"
    downstreamSpace: "Dex-default-staging"
    clonedUnitCount: 9
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "deployment-default-dex"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-dex-default-confighub-proof"
      - "Dex-default-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup (/Users/alexis/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "rendered Secret resources are separated from ConfigHub Units and must be managed out-of-band."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
