apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "rabbitmq-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.49"
  package:
    path: "packages/bitnami/rabbitmq/16.0.14"
    name: "bitnami-rabbitmq"
    chart: "bitnami/rabbitmq"
    chartVersion: "16.0.14"
    bases:
      - "generated-passwords"
      - "existing-secret"
    selectedBase: "generated-passwords"
    docVerified: true
    targetFactRequirements:
      -
        base: "existing-secret"
        requirements:
          -
            kind: "ClusterFeature"
            name: "Secret rabbitmq/rabbitmq-auth key rabbitmq-password"
            namespace: "rabbitmq"
            suggestedSource: "kubectl -n rabbitmq create secret generic rabbitmq-auth --from-literal=rabbitmq-password=<value>"
          -
            kind: "ClusterFeature"
            name: "Secret rabbitmq/rabbitmq-erlang-cookie key rabbitmq-erlang-cookie"
            namespace: "rabbitmq"
            suggestedSource: "kubectl -n rabbitmq create secret generic rabbitmq-erlang-cookie --from-literal=rabbitmq-erlang-cookie=<value>"
  render:
    command: "cub installer setup --pull packages/bitnami/rabbitmq/16.0.14 --base generated-passwords --work-dir .tmp/confighub-proof/rabbitmq-generated-passwords --non-interactive --namespace rabbitmq"
    result: "pass"
    manifestCount: 9
    separatedSecretCount: 2
    facts:
      targetFactChecks:
        base: "generated-passwords"
        mode: "not-required"
        result: "pass"
      targetFacts:
        requiredSecrets:
          []
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/rabbitmq-generated-passwords"
    result: "pass"
    manifestCount: 9
    separatedSecretCount: 2
  deterministicPackage:
    command: "cub installer package packages/bitnami/rabbitmq/16.0.14 -o .tmp/confighub-proof/rabbitmq-archives/rabbitmq-a.tgz"
    sha256: "00a46e601b59c275891f631e5e78cf573f634e47329cb51622c60dbe934db1e0"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/rabbitmq-generated-passwords"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/rabbitmq-generated-passwords --space helm-rabbitmq-confighub-proof --component RabbitMQ --layer App --environment Demo --owner ConfigHubHelm --variant generated-passwords --unit-label Component=RabbitMQ --unit-label HelmChart=bitnami-rabbitmq --unit-label HelmChartVersion=16.0.14 --unit-label Variant=generated-passwords --unit-label Proof=rabbitmq-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=/Users/alexis/.confighub/config.yaml"
    space: "helm-rabbitmq-confighub-proof"
    unitCount: 10
    kubernetesUnitCount: 9
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      - "v1/Secret rabbitmq/rabbitmq-config"
      - "v1/Secret rabbitmq/rabbitmq"
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/rabbitmq-generated-passwords"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/rabbitmq-generated-passwords"
    result: "pass"
    summary: "No changes.\nImages in helm-rabbitmq-confighub-proof (post-render):\n  init  StatefulSet/rabbitmq [prepare-plugins-dir] docker.io/bitnami/rabbitmq:4.1.3-debian-12-r1\n      StatefulSet/rabbitmq [rabbitmq] docker.io/bitnami/rabbitmq:4.1.3-debian-12-r1"
  serverSideVariant:
    command: "cub variant create staging helm-rabbitmq-confighub-proof --environment Staging --region local --space-name-pattern template:{{.SourceEntitySlug}}-{{.Labels.Variant}} --allow-exists"
    result: "pass"
    upstreamSpace: "helm-rabbitmq-confighub-proof"
    downstreamSpace: "helm-rabbitmq-confighub-proof-staging"
    clonedUnitCount: 10
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "statefulset-rabbitmq-rabbitmq"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-rabbitmq-confighub-proof"
      - "helm-rabbitmq-confighub-proof-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup (/Users/alexis/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "rendered Secret resources are separated from ConfigHub Units and must be managed out-of-band."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
