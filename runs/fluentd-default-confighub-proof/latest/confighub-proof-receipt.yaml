apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "fluentd-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.79"
  package:
    path: "packages/fluent/fluentd/0.5.3"
    name: "fluent-fluentd"
    chart: "fluent/fluentd"
    chartVersion: "0.5.3"
    bases:
      - "default"
    selectedBase: "default"
    docVerified: true
    targetFactRequirements:
      []
  render:
    command: "cub installer setup --pull packages/fluent/fluentd/0.5.3 --base default --work-dir .tmp/confighub-proof/fluentd-default --non-interactive --namespace default"
    result: "pass"
    manifestCount: 11
    separatedSecretCount: 0
    facts:
      {}
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/fluentd-default"
    result: "pass"
    manifestCount: 11
    separatedSecretCount: 0
  deterministicPackage:
    command: "cub installer package packages/fluent/fluentd/0.5.3 -o .tmp/confighub-proof/fluentd-default-archives/fluentd-a.tgz"
    sha256: "73e36b924e73bb87db5c7c1fbac81cb593724cbde6618c7cd6c20aef3a2ce8c9"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/fluentd-default"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/fluentd-default --space helm-fluentd-default-confighub-proof --component Fluentd --layer App --environment Demo --owner ConfigHubHelm --variant default --unit-label Component=Fluentd --unit-label HelmChart=fluent-fluentd --unit-label HelmChartVersion=0.5.3 --unit-label Variant=default --unit-label Proof=fluentd-default-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=/Users/alexis/.confighub/config.yaml"
    space: "helm-fluentd-default-confighub-proof"
    unitCount: 12
    kubernetesUnitCount: 11
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      []
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/fluentd-default"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/fluentd-default"
    result: "pass"
    summary: "No changes.\nImages in helm-fluentd-default-confighub-proof (post-render):\n      DaemonSet/fluentd [fluentd] fluent/fluentd-kubernetes-daemonset:v1.17.1-debian-elasticsearch7-1.2"
  serverSideVariant:
    command: "cub variant create staging helm-fluentd-default-confighub-proof --environment Staging --region local --space-name-pattern template:Fluentd-default-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-fluentd-default-confighub-proof"
    downstreamSpace: "Fluentd-default-staging"
    clonedUnitCount: 12
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "daemonset-fluentd"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-fluentd-default-confighub-proof"
      - "Fluentd-default-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup (/Users/alexis/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
