apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "redis-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    v0.1.71"
      cubServerVersion: "v0.1.74"
  package:
    path: "packages/bitnami/redis/25.5.3"
    name: "bitnami-redis"
    chart: "bitnami/redis"
    chartVersion: "25.5.3"
    bases:
      - "default"
      - "reuse-existing-secret"
    selectedBase: "default"
    docVerified: true
    targetFactRequirements:
      -
        base: "reuse-existing-secret"
        requirements:
          -
            kind: "ClusterFeature"
            name: "Secret redis/redis-existing-secret key redis-password"
            namespace: "redis"
            suggestedSource: "kubectl -n redis create secret generic redis-existing-secret --from-literal=redis-password=<value>"
  render:
    command: "cub installer setup --pull packages/bitnami/redis/25.5.3 --base default --work-dir .tmp/confighub-proof/redis-default --non-interactive --namespace redis"
    result: "pass"
    manifestCount: 14
    separatedSecretCount: 1
    facts:
      targetFactChecks:
        base: "default"
        mode: "not-required"
        result: "pass"
      targetFacts:
        requiredSecrets:
          []
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/redis-default"
    result: "pass"
    manifestCount: 14
    separatedSecretCount: 1
  deterministicPackage:
    command: "cub installer package packages/bitnami/redis/25.5.3 -o .tmp/confighub-proof/redis-archives/redis-a.tgz"
    sha256: "932093f05f7b1b264bf2579c3bfbcf975467e0830c5d77f992b08be2b796273a"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/redis-default"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/redis-default --space helm-redis-confighub-proof --component Redis --layer App --environment Demo --owner ConfigHubHelm --variant default --unit-label Component=Redis --unit-label HelmChart=bitnami-redis --unit-label HelmChartVersion=25.5.3 --unit-label Variant=default --unit-label Proof=redis-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=$HOME/.confighub/config.yaml"
    space: "helm-redis-confighub-proof"
    unitCount: 15
    kubernetesUnitCount: 14
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      - "v1/Secret redis/redis"
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/redis-default"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/redis-default"
    result: "pass"
    summary: "No changes.\nImages in helm-redis-confighub-proof (post-render):\n      StatefulSet/redis-master [redis] registry-1.docker.io/bitnami/redis:latest\n      StatefulSet/redis-replicas [redis] registry-1.docker.io/bitnami/redis:latest"
  serverSideVariant:
    command: "cub variant create staging helm-redis-confighub-proof --environment Staging --region local --space-name-pattern template:{{.Labels.Component}}-{{.Labels.Variant}} --allow-exists"
    result: "pass"
    upstreamSpace: "helm-redis-confighub-proof"
    downstreamSpace: "Redis-staging"
    clonedUnitCount: 15
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "statefulset-redis-redis-master"
  retention:
    liveSpacesDeletedAfterProof: false
    spaces:
      - "helm-redis-confighub-proof"
      - "Redis-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup ($HOME/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "rendered Secret resources are separated from ConfigHub Units and must be managed out-of-band."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
