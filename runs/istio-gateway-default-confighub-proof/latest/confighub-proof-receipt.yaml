apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "istio-gateway-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.80"
  package:
    path: "packages/istio/gateway/1.30.0"
    name: "istio-gateway"
    chart: "istio/gateway"
    chartVersion: "1.30.0"
    bases:
      - "default"
      - "controller-default-reviewed"
    selectedBase: "default"
    docVerified: true
    targetFactRequirements:
      []
  render:
    command: "cub installer setup --pull packages/istio/gateway/1.30.0 --base default --work-dir .tmp/confighub-proof/istio-gateway-default --non-interactive --namespace default"
    result: "pass"
    manifestCount: 7
    separatedSecretCount: 0
    facts:
      {}
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/istio-gateway-default"
    result: "pass"
    manifestCount: 7
    separatedSecretCount: 0
  deterministicPackage:
    command: "cub installer package packages/istio/gateway/1.30.0 -o .tmp/confighub-proof/istio-gateway-default-archives/istio-gateway-a.tgz"
    sha256: "2235b01ac2590d4fdc8a99da29bd735cd053470a01e230cefea14d79ffee2270"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/istio-gateway-default"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/istio-gateway-default --space helm-istio-gateway-default-confighub-proof --component IstioGateway --layer App --environment Demo --owner ConfigHubHelm --variant default --unit-label Component=IstioGateway --unit-label HelmChart=istio-gateway --unit-label HelmChartVersion=1.30.0 --unit-label Variant=default --unit-label Proof=istio-gateway-default-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=$HOME/.confighub/config.yaml"
    space: "helm-istio-gateway-default-confighub-proof"
    unitCount: 8
    kubernetesUnitCount: 7
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      []
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/istio-gateway-default"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/istio-gateway-default"
    result: "pass"
    summary: "No changes.\nImages in helm-istio-gateway-default-confighub-proof (post-render):\n      Deployment/gateway [istio-proxy] auto"
  serverSideVariant:
    command: "cub variant create staging helm-istio-gateway-default-confighub-proof --environment Staging --region local --namespace default --space-pattern template:IstioGateway-default-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-istio-gateway-default-confighub-proof"
    downstreamSpace: "IstioGateway-default-staging"
    clonedUnitCount: 8
    namespace: "default"
  serverSidePromotion:
    receipt: "variant-promotion-receipt.yaml"
    result: "pass"
    reason: "server-side promotion passed through a changeset"
    changedUnitCaughtUp: "pass"
    addedUnitCloned: "pass"
    deletionHandling: "not-tested"
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "deployment-default-gateway"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-istio-gateway-default-confighub-proof"
      - "IstioGateway-default-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup ($HOME/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
