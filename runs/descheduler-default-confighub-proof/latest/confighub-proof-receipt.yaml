apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "descheduler-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.79"
  package:
    path: "packages/descheduler/descheduler/0.36.0"
    name: "descheduler-descheduler"
    chart: "descheduler/descheduler"
    chartVersion: "0.36.0"
    bases:
      - "default"
    selectedBase: "default"
    docVerified: true
    targetFactRequirements:
      []
  render:
    command: "cub installer setup --pull packages/descheduler/descheduler/0.36.0 --base default --work-dir .tmp/confighub-proof/descheduler-default --non-interactive --namespace default"
    result: "pass"
    manifestCount: 6
    separatedSecretCount: 0
    facts:
      {}
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/descheduler-default"
    result: "pass"
    manifestCount: 6
    separatedSecretCount: 0
  deterministicPackage:
    command: "cub installer package packages/descheduler/descheduler/0.36.0 -o .tmp/confighub-proof/descheduler-default-archives/descheduler-a.tgz"
    sha256: "ade98781bd54405308961ca0d1b29646d425e6e3624819cee5c095418ea30490"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/descheduler-default"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/descheduler-default --space helm-descheduler-default-confighub-proof --component Descheduler --layer App --environment Demo --owner ConfigHubHelm --variant default --unit-label Component=Descheduler --unit-label HelmChart=descheduler-descheduler --unit-label HelmChartVersion=0.36.0 --unit-label Variant=default --unit-label Proof=descheduler-default-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=/Users/alexis/.confighub/config.yaml"
    space: "helm-descheduler-default-confighub-proof"
    unitCount: 7
    kubernetesUnitCount: 6
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      []
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/descheduler-default"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/descheduler-default"
    result: "pass"
    summary: "No changes.\nImages in helm-descheduler-default-confighub-proof (post-render):\n      CronJob/descheduler [descheduler] registry.k8s.io/descheduler/descheduler:v0.36.0"
  serverSideVariant:
    command: "cub variant create staging helm-descheduler-default-confighub-proof --environment Staging --region local --space-name-pattern template:Descheduler-default-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-descheduler-default-confighub-proof"
    downstreamSpace: "Descheduler-default-staging"
    clonedUnitCount: 7
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "clusterrolebinding-descheduler"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-descheduler-default-confighub-proof"
      - "Descheduler-default-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup (/Users/alexis/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
