apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubSafeOpsReceipt"
metadata:
  name: "descheduler-safe-ops-20260527"
spec:
  observedAt: "2026-05-27"
  context:
    organization: "Kubara"
    server: "https://hub.confighub.com"
    space: "helm-descheduler-default-confighub-proof"
    selector: "Labels.Proof = 'descheduler-default-confighub-proof'"
  subject:
    chart: "descheduler/descheduler"
    chartVersion: "0.36.0"
    packagePath: "packages/descheduler/descheduler/0.36.0"
    variant: "default"
  changeset:
    slug: "descheduler-safe-ops-20260527"
    createResult: "pass"
    updateResult: "pass"
    description: "descheduler safe-ops proof: approve reviewed revisions, dry-run apply only"
    labels:
      Proof: "descheduler-default-confighub-proof"
      Lane: "safe-ops"
    annotations:
      proof.confighub.com/scope: "local-test"
      proof.confighub.com/live-apply: "false"
  approval:
    command: "cub unit approve clusterrolebinding-descheduler --space helm-descheduler-default-confighub-proof --revision HeadRevisionNum --verbose --wait"
    result: "pass"
    representativeUnit: "clusterrolebinding-descheduler"
    output: "Unit clusterrolebinding-descheduler (cb55bbc6-ca2e-43ef-ad60-bb19e8f2e334) has been approved\nAwaiting triggers..."
  applyDryRun:
    command: "cub unit apply --space helm-descheduler-default-confighub-proof --where \"Labels.Proof = 'descheduler-default-confighub-proof'\" --dry-run --wait --timeout 2m"
    result: "blocked-no-target"
    expected: true
    message: "Failed: HTTP 422 for req sMYowVpdfrubdXRyAPleUfYNJcremoES: cannot invoke action on a unit without a target\nDetails:\n  failed to apply entity"
  cancel:
    command: "cub unit cancel --space helm-descheduler-default-confighub-proof --where \"Labels.Proof = 'descheduler-default-confighub-proof'\""
    result: "pass"
    message: "No units found matching the filter"
  safetyResult: "pass"
  interpretation: "The reviewed Units are visible in ConfigHub, but without a target ConfigHub makes no live deployment claim and blocks apply at the operation boundary."
