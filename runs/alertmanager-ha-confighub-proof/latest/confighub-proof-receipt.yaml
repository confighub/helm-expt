apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "alertmanager-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.80"
  package:
    path: "packages/prometheus-community/alertmanager/1.37.0"
    name: "prometheus-community-alertmanager"
    chart: "prometheus-community/alertmanager"
    chartVersion: "1.37.0"
    bases:
      - "default"
      - "ha"
    selectedBase: "ha"
    docVerified: true
    targetFactRequirements:
      []
  render:
    command: "cub installer setup --pull packages/prometheus-community/alertmanager/1.37.0 --base ha --work-dir .tmp/confighub-proof/alertmanager-ha --non-interactive --namespace default"
    result: "pass"
    manifestCount: 6
    separatedSecretCount: 0
    facts:
      {}
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/alertmanager-ha"
    result: "pass"
    manifestCount: 6
    separatedSecretCount: 0
  deterministicPackage:
    command: "cub installer package packages/prometheus-community/alertmanager/1.37.0 -o .tmp/confighub-proof/alertmanager-ha-archives/alertmanager-a.tgz"
    sha256: "beec8632d1ef811a83b81ea07825946022bb2ade2129b3727d18bce2c5ccc47e"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/alertmanager-ha"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/alertmanager-ha --space helm-alertmanager-ha-confighub-proof --component Alertmanager --layer App --environment Demo --owner ConfigHubHelm --variant ha --unit-label Component=Alertmanager --unit-label HelmChart=prometheus-community-alertmanager --unit-label HelmChartVersion=1.37.0 --unit-label Variant=ha --unit-label Proof=alertmanager-ha-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=$HOME/.confighub/config.yaml"
    space: "helm-alertmanager-ha-confighub-proof"
    unitCount: 7
    kubernetesUnitCount: 6
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      []
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/alertmanager-ha"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/alertmanager-ha"
    result: "pass"
    summary: "Plan: 0 to add, 2 to change, 0 to delete.\nSpace helm-alertmanager-ha-confighub-proof:\n  ~ service-default-alertmanager-headless\n      New changes from update from $HOME/code/helm-expt-claude-clone/.tmp/confighub-proof/alertmanager-ha/out/manifests/service-default-alertmanager-headless.yaml:\n      Resource: v1/Service default/alertmanager-headless\n        - [Delete] spec.ports.?port=9094;@1  (#2)\n          name: cluster-tcp\n          port: 9094\n          protocol: TCP\n          targetPort: clusterpeer-tcp"
  serverSideVariant:
    command: "cub variant create staging helm-alertmanager-ha-confighub-proof --environment Staging --region local --namespace default --space-pattern template:Alertmanager-ha-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-alertmanager-ha-confighub-proof"
    downstreamSpace: "Alertmanager-ha-staging"
    clonedUnitCount: 7
    namespace: "default"
  serverSidePromotion:
    receipt: "variant-promotion-receipt.yaml"
    result: "pass"
    reason: "server-side promotion passed through a changeset"
    changedUnitCaughtUp: "pass"
    addedUnitCloned: "pass"
    deletionHandling: "not-tested"
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "statefulset-default-alertmanager"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-alertmanager-ha-confighub-proof"
      - "Alertmanager-ha-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup ($HOME/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
