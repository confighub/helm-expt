apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "trust-manager-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.79"
  package:
    path: "packages/jetstack/trust-manager/v0.22.1"
    name: "jetstack-trust-manager"
    chart: "jetstack/trust-manager"
    chartVersion: "v0.22.1"
    bases:
      - "default"
      - "no-crds"
    selectedBase: "default"
    docVerified: true
    targetFactRequirements:
      []
  render:
    command: "cub installer setup --pull packages/jetstack/trust-manager/v0.22.1 --base default --work-dir .tmp/confighub-proof/trust-manager-default --non-interactive --namespace default"
    result: "pass"
    manifestCount: 15
    separatedSecretCount: 0
    facts:
      {}
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/trust-manager-default"
    result: "pass"
    manifestCount: 15
    separatedSecretCount: 0
  deterministicPackage:
    command: "cub installer package packages/jetstack/trust-manager/v0.22.1 -o .tmp/confighub-proof/trust-manager-archives/trust-manager-a.tgz"
    sha256: "e0f077d0158b34447fbc29a05f0cb94eb7d63127f8ac60d2a9806c54d5b4e9ce"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/trust-manager-default"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/trust-manager-default --space helm-trust-manager-confighub-proof --component TrustManager --layer App --environment Demo --owner ConfigHubHelm --variant default --unit-label Component=TrustManager --unit-label HelmChart=jetstack-trust-manager --unit-label HelmChartVersion=v0.22.1 --unit-label Variant=default --unit-label Proof=trust-manager-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=$HOME/.confighub/config.yaml"
    space: "helm-trust-manager-confighub-proof"
    unitCount: 16
    kubernetesUnitCount: 15
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      []
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/trust-manager-default"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/trust-manager-default"
    result: "pass"
    summary: "No changes.\nImages in helm-trust-manager-confighub-proof (post-render):\n  init  Deployment/trust-manager [cert-manager-package-debian] quay.io/jetstack/trust-pkg-debian-bookworm:20230311-deb12u1.6\n      Deployment/trust-manager [trust-manager] quay.io/jetstack/trust-manager:v0.22.1"
  serverSideVariant:
    command: "cub variant create staging helm-trust-manager-confighub-proof --environment Staging --region local --space-name-pattern template:{{.Labels.Component}}-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-trust-manager-confighub-proof"
    downstreamSpace: "TrustManager-staging"
    clonedUnitCount: 16
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "deployment-default-trust-manager"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-trust-manager-confighub-proof"
      - "TrustManager-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup ($HOME/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
