apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "metrics-server-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.80"
  package:
    path: "packages/metrics-server/metrics-server/3.13.0"
    name: "metrics-server-metrics-server"
    chart: "metrics-server/metrics-server"
    chartVersion: "3.13.0"
    bases:
      - "default"
      - "external-tls-ca"
    selectedBase: "external-tls-ca"
    docVerified: true
    targetFactRequirements:
      -
        base: "external-tls-ca"
        requirements:
          -
            kind: "ClusterFeature"
            name: "Secret kube-system/metrics-server-tls keys tls.crt,tls.key"
            namespace: "kube-system"
            suggestedSource: "kubectl -n kube-system create secret generic metrics-server-tls --from-literal=tls.crt=<value> --from-literal=tls.key=<value>"
  render:
    command: "cub installer setup --pull packages/metrics-server/metrics-server/3.13.0 --base external-tls-ca --work-dir .tmp/confighub-proof/metrics-server-external-tls-ca --non-interactive --namespace kube-system"
    result: "pass"
    manifestCount: 10
    separatedSecretCount: 0
    facts:
      targetFactChecks:
        base: "external-tls-ca"
        mode: "record"
        result: "recorded"
      targetFacts:
        requiredCRDs:
          []
        requiredObjectStores:
          []
        requiredSecrets:
          -
            keys:
              - "tls.crt"
              - "tls.key"
            name: "metrics-server-tls"
            namespace: "kube-system"
            purpose: "metrics-server serving certificate"
        requiredTopology: null
        requiredValues:
          -
            installerInput: "apiServiceCaBundle"
            installerInputFormat: "base64-pem"
            path: "apiService.caBundle"
            purpose: "APIService CA bundle that validates kube-system/metrics-server-tls"
            regularHelmFormat: "pem"
            source: "metrics-server-tls-ca"
            stage: "pre-render"
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/metrics-server-external-tls-ca"
    result: "pass"
    manifestCount: 10
    separatedSecretCount: 0
  deterministicPackage:
    command: "cub installer package packages/metrics-server/metrics-server/3.13.0 -o .tmp/confighub-proof/metrics-server-external-tls-ca-archives/metrics-server-a.tgz"
    sha256: "10533c3e9e3d36c42145a97a5faf5b878db95aee76a3335e70671237265ca864"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/metrics-server-external-tls-ca"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/metrics-server-external-tls-ca --space helm-metrics-server-external-tls-ca-confighub-proof --component MetricsServer --layer App --environment Demo --owner ConfigHubHelm --variant external-tls-ca --unit-label Component=MetricsServer --unit-label HelmChart=metrics-server-metrics-server --unit-label HelmChartVersion=3.13.0 --unit-label Variant=external-tls-ca --unit-label Proof=metrics-server-external-tls-ca-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=$HOME/.confighub/config.yaml"
    space: "helm-metrics-server-external-tls-ca-confighub-proof"
    unitCount: 11
    kubernetesUnitCount: 10
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      []
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/metrics-server-external-tls-ca"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/metrics-server-external-tls-ca"
    result: "pass"
    summary: "No changes.\nImages in helm-metrics-server-external-tls-ca-confighub-proof (post-render):\n      Deployment/metrics-server [metrics-server] registry.k8s.io/metrics-server/metrics-server:v0.8.0"
  serverSideVariant:
    command: "cub variant create staging helm-metrics-server-external-tls-ca-confighub-proof --environment Staging --region local --namespace kube-system --space-pattern template:MetricsServer-external-tls-ca-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-metrics-server-external-tls-ca-confighub-proof"
    downstreamSpace: "MetricsServer-external-tls-ca-staging"
    clonedUnitCount: 11
    namespace: "kube-system"
  serverSidePromotion:
    receipt: "variant-promotion-receipt.yaml"
    result: "pass"
    reason: "server-side promotion passed through a changeset"
    changedUnitCaughtUp: "pass"
    addedUnitCloned: "pass"
    deletionHandling: "not-tested"
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "deployment-kube-system-metrics-server"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-metrics-server-external-tls-ca-confighub-proof"
      - "MetricsServer-external-tls-ca-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup ($HOME/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
