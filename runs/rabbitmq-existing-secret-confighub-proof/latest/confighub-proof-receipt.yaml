apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "rabbitmq-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.80"
  package:
    path: "packages/bitnami/rabbitmq/16.0.14"
    name: "bitnami-rabbitmq"
    chart: "bitnami/rabbitmq"
    chartVersion: "16.0.14"
    bases:
      - "generated-passwords"
      - "existing-secret"
    selectedBase: "existing-secret"
    docVerified: true
    targetFactRequirements:
      -
        base: "existing-secret"
        requirements:
          -
            kind: "ClusterFeature"
            name: "Secret rabbitmq/rabbitmq-auth key rabbitmq-password"
            namespace: "rabbitmq"
            suggestedSource: "kubectl -n rabbitmq create secret generic rabbitmq-auth --from-literal=rabbitmq-password=<value>"
          -
            kind: "ClusterFeature"
            name: "Secret rabbitmq/rabbitmq-erlang-cookie key rabbitmq-erlang-cookie"
            namespace: "rabbitmq"
            suggestedSource: "kubectl -n rabbitmq create secret generic rabbitmq-erlang-cookie --from-literal=rabbitmq-erlang-cookie=<value>"
  render:
    command: "cub installer setup --pull packages/bitnami/rabbitmq/16.0.14 --base existing-secret --work-dir .tmp/confighub-proof/rabbitmq-existing-secret --non-interactive --namespace rabbitmq"
    result: "pass"
    manifestCount: 9
    separatedSecretCount: 1
    facts:
      targetFactChecks:
        base: "existing-secret"
        mode: "record"
        result: "recorded"
      targetFacts:
        requiredCRDs:
          []
        requiredObjectStores:
          []
        requiredSecrets:
          -
            keys:
              - "rabbitmq-password"
            name: "rabbitmq-auth"
            namespace: "rabbitmq"
            purpose: "RabbitMQ administrator password"
          -
            keys:
              - "rabbitmq-erlang-cookie"
            name: "rabbitmq-erlang-cookie"
            namespace: "rabbitmq"
            purpose: "RabbitMQ Erlang cookie for node clustering"
        requiredTopology: null
        requiredValues:
          []
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/rabbitmq-existing-secret"
    result: "pass"
    manifestCount: 9
    separatedSecretCount: 1
  deterministicPackage:
    command: "cub installer package packages/bitnami/rabbitmq/16.0.14 -o .tmp/confighub-proof/rabbitmq-existing-secret-archives/rabbitmq-a.tgz"
    sha256: "42887c216015befac045652b1e0615e54f1eebaeb313c7f57f6293ee225f5b22"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/rabbitmq-existing-secret"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/rabbitmq-existing-secret --space helm-rabbitmq-existing-secret-confighub-proof --component RabbitMQ --layer App --environment Demo --owner ConfigHubHelm --variant existing-secret --unit-label Component=RabbitMQ --unit-label HelmChart=bitnami-rabbitmq --unit-label HelmChartVersion=16.0.14 --unit-label Variant=existing-secret --unit-label Proof=rabbitmq-existing-secret-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=/Users/alexis/.confighub/config.yaml"
    space: "helm-rabbitmq-existing-secret-confighub-proof"
    unitCount: 10
    kubernetesUnitCount: 9
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      - "v1/Secret rabbitmq/rabbitmq-config"
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/rabbitmq-existing-secret"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/rabbitmq-existing-secret"
    result: "pass"
    summary: "No changes.\nImages in helm-rabbitmq-existing-secret-confighub-proof (post-render):\n  init  StatefulSet/rabbitmq [prepare-plugins-dir] docker.io/bitnamilegacy/rabbitmq:4.1.3-debian-12-r1\n      StatefulSet/rabbitmq [rabbitmq] docker.io/bitnamilegacy/rabbitmq:4.1.3-debian-12-r1"
  serverSideVariant:
    command: "cub variant create staging helm-rabbitmq-existing-secret-confighub-proof --environment Staging --region local --namespace rabbitmq --space-pattern template:RabbitMQ-existing-secret-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-rabbitmq-existing-secret-confighub-proof"
    downstreamSpace: "RabbitMQ-existing-secret-staging"
    clonedUnitCount: 10
    namespace: "rabbitmq"
  serverSidePromotion:
    receipt: "variant-promotion-receipt.yaml"
    result: "pass"
    reason: "server-side promotion passed through a changeset"
    changedUnitCaughtUp: "pass"
    addedUnitCloned: "pass"
    deletionHandling: "not-tested"
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "statefulset-rabbitmq-rabbitmq"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-rabbitmq-existing-secret-confighub-proof"
      - "RabbitMQ-existing-secret-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup (/Users/alexis/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "rendered Secret resources are separated from ConfigHub Units and must be managed out-of-band."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
