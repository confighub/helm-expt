apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "grafana-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.79"
  package:
    path: "packages/grafana/grafana/10.5.15"
    name: "grafana-grafana"
    chart: "grafana/grafana"
    chartVersion: "10.5.15"
    bases:
      - "generated-passwords"
      - "existing-secret-ingress"
    selectedBase: "generated-passwords"
    docVerified: true
    targetFactRequirements:
      -
        base: "existing-secret-ingress"
        requirements:
          -
            kind: "ClusterFeature"
            name: "Secret grafana/grafana-admin keys admin-user,admin-password"
            namespace: "grafana"
            suggestedSource: "kubectl -n grafana create secret generic grafana-admin --from-literal=admin-user=<value> --from-literal=admin-password=<value>"
  render:
    command: "cub installer setup --pull packages/grafana/grafana/10.5.15 --base generated-passwords --work-dir .tmp/confighub-proof/grafana-generated-passwords --non-interactive --namespace grafana"
    result: "pass"
    manifestCount: 9
    separatedSecretCount: 1
    facts:
      targetFactChecks:
        base: "generated-passwords"
        mode: "not-required"
        result: "pass"
      targetFacts:
        requiredCRDs:
          []
        requiredObjectStores:
          []
        requiredSecrets:
          []
        requiredTopology: null
        requiredValues:
          []
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/grafana-generated-passwords"
    result: "pass"
    manifestCount: 9
    separatedSecretCount: 1
  deterministicPackage:
    command: "cub installer package packages/grafana/grafana/10.5.15 -o .tmp/confighub-proof/grafana-archives/grafana-a.tgz"
    sha256: "f63f67e9c48f503bbbc36452905876c4a64ffa0bb0abf123ae2cb020286df0a9"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/grafana-generated-passwords"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/grafana-generated-passwords --space helm-grafana-confighub-proof --component Grafana --layer App --environment Demo --owner ConfigHubHelm --variant generated-passwords --unit-label Component=Grafana --unit-label HelmChart=grafana-grafana --unit-label HelmChartVersion=10.5.15 --unit-label Variant=generated-passwords --unit-label Proof=grafana-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=/Users/alexis/.confighub/config.yaml"
    space: "helm-grafana-confighub-proof"
    unitCount: 10
    kubernetesUnitCount: 9
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      - "v1/Secret grafana/grafana"
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/grafana-generated-passwords"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/grafana-generated-passwords"
    result: "pass"
    summary: "Plan: 0 to add, 1 to change, 0 to delete.\nSpace helm-grafana-confighub-proof:\n  ~ deployment-grafana-grafana\n      New changes from update from /Users/alexis/code/helm-expt/.tmp/confighub-proof/grafana-generated-passwords/out/manifests/deployment-grafana-grafana.yaml:\n      Resource: apps/v1/Deployment grafana/grafana\n        - [Delete] spec.template.spec.containers.?name=grafana;@0.ports.?containerPort=9094;@1  (#2)\n          containerPort: 9094\n          name: gossip-tcp\n          protocol: TCP\n        ~ [Update] spec.template.spec.containers.?name=grafana;@0.ports.?containerPort=9094;@1.name  (#2)"
  serverSideVariant:
    command: "cub variant create staging helm-grafana-confighub-proof --environment Staging --region local --namespace grafana --space-pattern template:{{.Labels.Component}}-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-grafana-confighub-proof"
    downstreamSpace: "Grafana-staging"
    clonedUnitCount: 10
    namespace: "grafana"
  serverSidePromotion:
    receipt: "variant-promotion-receipt.yaml"
    result: "watch"
    reason: "server-side promotion mechanics passed, but changeset-bound promote failed and required the no-changeset fallback"
    changedUnitCaughtUp: "pass"
    addedUnitCloned: "pass"
    deletionHandling: "not-tested"
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "deployment-grafana-grafana"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-grafana-confighub-proof"
      - "Grafana-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup (/Users/alexis/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "rendered Secret resources are separated from ConfigHub Units and must be managed out-of-band."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
