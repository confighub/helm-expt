apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "terraform-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.80"
  package:
    path: "packages/hashicorp/terraform/1.1.2"
    name: "hashicorp-terraform"
    chart: "hashicorp/terraform"
    chartVersion: "1.1.2"
    bases:
      - "default"
      - "no-crds"
    selectedBase: "default"
    docVerified: true
    targetFactRequirements:
      []
  render:
    command: "cub installer setup --pull packages/hashicorp/terraform/1.1.2 --base default --work-dir .tmp/confighub-proof/terraform-default --non-interactive --namespace default"
    result: "pass"
    manifestCount: 6
    separatedSecretCount: 0
    facts:
      {}
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/terraform-default"
    result: "pass"
    manifestCount: 6
    separatedSecretCount: 0
  deterministicPackage:
    command: "cub installer package packages/hashicorp/terraform/1.1.2 -o .tmp/confighub-proof/terraform-default-archives/terraform-a.tgz"
    sha256: "737667648f89276c77a2268dd67e52b33ed27b88add655c7aea2232f12ab8ef0"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/terraform-default"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/terraform-default --space helm-terraform-default-confighub-proof --component Terraform --layer App --environment Demo --owner ConfigHubHelm --variant default --unit-label Component=Terraform --unit-label HelmChart=hashicorp-terraform --unit-label HelmChartVersion=1.1.2 --unit-label Variant=default --unit-label Proof=terraform-default-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=/Users/alexis/.confighub/config.yaml"
    space: "helm-terraform-default-confighub-proof"
    unitCount: 7
    kubernetesUnitCount: 6
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      []
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/terraform-default"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/terraform-default"
    result: "pass"
    summary: "No changes.\nImages in helm-terraform-default-confighub-proof (post-render):\n      Deployment/terraform-terraform-sync-workspace [terraform-sync-workspace] hashicorp/terraform-k8s:1.1.2"
  serverSideVariant:
    command: "cub variant create staging helm-terraform-default-confighub-proof --environment Staging --region local --namespace default --space-pattern template:Terraform-default-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-terraform-default-confighub-proof"
    downstreamSpace: "Terraform-default-staging"
    clonedUnitCount: 7
    namespace: "default"
  serverSidePromotion:
    receipt: "variant-promotion-receipt.yaml"
    result: "pass"
    reason: "server-side promotion passed through a changeset"
    changedUnitCaughtUp: "pass"
    addedUnitCloned: "pass"
    deletionHandling: "not-tested"
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "deployment-default-terraform-terraform-sync-workspace"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-terraform-default-confighub-proof"
      - "Terraform-default-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup (/Users/alexis/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
