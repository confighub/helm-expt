apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "psmdb-operator-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.79"
  package:
    path: "packages/percona/psmdb-operator/1.22.0"
    name: "percona-psmdb-operator"
    chart: "percona/psmdb-operator"
    chartVersion: "1.22.0"
    bases:
      - "default"
      - "no-crds"
    selectedBase: "default"
    docVerified: true
    targetFactRequirements:
      []
  render:
    command: "cub installer setup --pull packages/percona/psmdb-operator/1.22.0 --base default --work-dir .tmp/confighub-proof/psmdb-operator-default --non-interactive --namespace default"
    result: "pass"
    manifestCount: 8
    separatedSecretCount: 0
    facts:
      {}
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/psmdb-operator-default"
    result: "pass"
    manifestCount: 8
    separatedSecretCount: 0
  deterministicPackage:
    command: "cub installer package packages/percona/psmdb-operator/1.22.0 -o .tmp/confighub-proof/psmdb-operator-archives/psmdb-operator-a.tgz"
    sha256: "29e16c7492a6e7b679cc4d756ea198c719da6cf787f61b75d8264592f47583d8"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/psmdb-operator-default"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/psmdb-operator-default --space helm-psmdb-operator-confighub-proof --component PerconaPSMDBOperator --layer App --environment Demo --owner ConfigHubHelm --variant default --unit-label Component=PerconaPSMDBOperator --unit-label HelmChart=percona-psmdb-operator --unit-label HelmChartVersion=1.22.0 --unit-label Variant=default --unit-label Proof=psmdb-operator-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=$HOME/.confighub/config.yaml"
    space: "helm-psmdb-operator-confighub-proof"
    unitCount: 9
    kubernetesUnitCount: 8
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      []
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/psmdb-operator-default"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/psmdb-operator-default"
    result: "pass"
    summary: "No changes.\nImages in helm-psmdb-operator-confighub-proof (post-render):\n      Deployment/psmdb-operator [psmdb-operator] percona/percona-server-mongodb-operator:1.22.0"
  serverSideVariant:
    command: "cub variant create staging helm-psmdb-operator-confighub-proof --environment Staging --region local --space-name-pattern template:{{.Labels.Component}}-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-psmdb-operator-confighub-proof"
    downstreamSpace: "PerconaPSMDBOperator-staging"
    clonedUnitCount: 9
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "deployment-default-psmdb-operator"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-psmdb-operator-confighub-proof"
      - "PerconaPSMDBOperator-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup ($HOME/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
