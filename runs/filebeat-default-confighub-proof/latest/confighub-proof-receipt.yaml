apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "filebeat-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.80"
  package:
    path: "packages/elastic/filebeat/8.5.1"
    name: "elastic-filebeat"
    chart: "elastic/filebeat"
    chartVersion: "8.5.1"
    bases:
      - "default"
      - "node-or-cluster-collector"
    selectedBase: "default"
    docVerified: true
    targetFactRequirements:
      []
  render:
    command: "cub installer setup --pull packages/elastic/filebeat/8.5.1 --base default --work-dir .tmp/confighub-proof/filebeat-default --non-interactive --namespace default"
    result: "pass"
    manifestCount: 8
    separatedSecretCount: 0
    facts:
      {}
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/filebeat-default"
    result: "pass"
    manifestCount: 8
    separatedSecretCount: 0
  deterministicPackage:
    command: "cub installer package packages/elastic/filebeat/8.5.1 -o .tmp/confighub-proof/filebeat-default-archives/filebeat-a.tgz"
    sha256: "32748d9cac7d65bcf6929d943932f65f6861c8e6aab63ffb7824c43b89da669c"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/filebeat-default"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/filebeat-default --space helm-filebeat-default-confighub-proof --component Filebeat --layer App --environment Demo --owner ConfigHubHelm --variant default --unit-label Component=Filebeat --unit-label HelmChart=elastic-filebeat --unit-label HelmChartVersion=8.5.1 --unit-label Variant=default --unit-label Proof=filebeat-default-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=/Users/alexis/.confighub/config.yaml"
    space: "helm-filebeat-default-confighub-proof"
    unitCount: 9
    kubernetesUnitCount: 8
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      []
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/filebeat-default"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/filebeat-default"
    result: "pass"
    summary: "No changes.\nImages in helm-filebeat-default-confighub-proof (post-render):\n      DaemonSet/filebeat-filebeat [filebeat] docker.elastic.co/beats/filebeat:8.5.1"
  serverSideVariant:
    command: "cub variant create staging helm-filebeat-default-confighub-proof --environment Staging --region local --namespace default --space-pattern template:Filebeat-default-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-filebeat-default-confighub-proof"
    downstreamSpace: "Filebeat-default-staging"
    clonedUnitCount: 9
    namespace: "default"
  serverSidePromotion:
    receipt: "variant-promotion-receipt.yaml"
    result: "pass"
    reason: "server-side promotion passed through a changeset"
    changedUnitCaughtUp: "pass"
    addedUnitCloned: "pass"
    deletionHandling: "not-tested"
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "configmap-filebeat-filebeat-daemonset-config"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-filebeat-default-confighub-proof"
      - "Filebeat-default-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup (/Users/alexis/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
