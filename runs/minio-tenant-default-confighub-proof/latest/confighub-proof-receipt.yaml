apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "minio-tenant-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.79"
  package:
    path: "packages/minio-operator/tenant/7.1.1"
    name: "minio-operator-tenant"
    chart: "minio-operator/tenant"
    chartVersion: "7.1.1"
    bases:
      - "default"
    selectedBase: "default"
    docVerified: true
    targetFactRequirements:
      []
  render:
    command: "cub installer setup --pull packages/minio-operator/tenant/7.1.1 --base default --work-dir .tmp/confighub-proof/minio-tenant-default --non-interactive --namespace default"
    result: "pass"
    manifestCount: 2
    separatedSecretCount: 1
    facts:
      {}
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/minio-tenant-default"
    result: "pass"
    manifestCount: 2
    separatedSecretCount: 1
  deterministicPackage:
    command: "cub installer package packages/minio-operator/tenant/7.1.1 -o .tmp/confighub-proof/minio-tenant-default-archives/minio-tenant-a.tgz"
    sha256: "7a10ca850b10f4b0d7c766397449b88636eb6f798ddf606da6d89b0b2b4e3d42"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/minio-tenant-default"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/minio-tenant-default --space helm-minio-tenant-default-confighub-proof --component MinioTenant --layer App --environment Demo --owner ConfigHubHelm --variant default --unit-label Component=MinioTenant --unit-label HelmChart=minio-operator-tenant --unit-label HelmChartVersion=7.1.1 --unit-label Variant=default --unit-label Proof=minio-tenant-default-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=/Users/alexis/.confighub/config.yaml"
    space: "helm-minio-tenant-default-confighub-proof"
    unitCount: 3
    kubernetesUnitCount: 2
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      - "v1/Secret myminio-env-configuration"
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/minio-tenant-default"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/minio-tenant-default"
    result: "pass"
    summary: "No changes."
  serverSideVariant:
    command: "cub variant create staging helm-minio-tenant-default-confighub-proof --environment Staging --region local --space-name-pattern template:MinioTenant-default-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-minio-tenant-default-confighub-proof"
    downstreamSpace: "MinioTenant-default-staging"
    clonedUnitCount: 3
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "namespace-default"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-minio-tenant-default-confighub-proof"
      - "MinioTenant-default-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup (/Users/alexis/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "rendered Secret resources are separated from ConfigHub Units and must be managed out-of-band."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
