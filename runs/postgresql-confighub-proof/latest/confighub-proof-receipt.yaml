apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "postgresql-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.79"
  package:
    path: "packages/bitnami/postgresql/18.6.7"
    name: "bitnami-postgresql"
    chart: "bitnami/postgresql"
    chartVersion: "18.6.7"
    bases:
      - "generated-passwords"
      - "existing-secret"
    selectedBase: "generated-passwords"
    docVerified: true
    targetFactRequirements:
      -
        base: "existing-secret"
        requirements:
          -
            kind: "ClusterFeature"
            name: "Secret postgresql/postgresql-auth key postgres-password"
            namespace: "postgresql"
            suggestedSource: "kubectl -n postgresql create secret generic postgresql-auth --from-literal=postgres-password=<value>"
  render:
    command: "cub installer setup --pull packages/bitnami/postgresql/18.6.7 --base generated-passwords --work-dir .tmp/confighub-proof/postgresql-generated-passwords --non-interactive --namespace postgresql"
    result: "pass"
    manifestCount: 7
    separatedSecretCount: 1
    facts:
      targetFactChecks:
        base: "generated-passwords"
        mode: "not-required"
        result: "pass"
      targetFacts:
        requiredCRDs:
          []
        requiredSecrets:
          []
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/postgresql-generated-passwords"
    result: "pass"
    manifestCount: 7
    separatedSecretCount: 1
  deterministicPackage:
    command: "cub installer package packages/bitnami/postgresql/18.6.7 -o .tmp/confighub-proof/postgresql-archives/postgresql-a.tgz"
    sha256: "68f5561ebc036c2b46785a1f00497f279bbfa818323187f86d7d63bb73475a9f"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/postgresql-generated-passwords"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/postgresql-generated-passwords --space helm-postgresql-confighub-proof --component PostgreSQL --layer App --environment Demo --owner ConfigHubHelm --variant generated-passwords --unit-label Component=PostgreSQL --unit-label HelmChart=bitnami-postgresql --unit-label HelmChartVersion=18.6.7 --unit-label Variant=generated-passwords --unit-label Proof=postgresql-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=/Users/alexis/.confighub/config.yaml"
    space: "helm-postgresql-confighub-proof"
    unitCount: 8
    kubernetesUnitCount: 7
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      - "v1/Secret postgresql/postgresql"
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/postgresql-generated-passwords"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/postgresql-generated-passwords"
    result: "pass"
    summary: "No changes.\nImages in helm-postgresql-confighub-proof (post-render):\n      StatefulSet/postgresql [postgresql] registry-1.docker.io/bitnami/postgresql@sha256:2d3c068e58f89e0e29c6d41ffa3d00ff253e87a9426662c3ef11d2a0942ee653"
  serverSideVariant:
    command: "cub variant create staging helm-postgresql-confighub-proof --environment Staging --region local --space-name-pattern template:{{.Labels.Component}}-{{.Labels.Variant}} --allow-exists"
    result: "pass"
    upstreamSpace: "helm-postgresql-confighub-proof"
    downstreamSpace: "PostgreSQL-staging"
    clonedUnitCount: 8
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "statefulset-postgresql-postgresql"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-postgresql-confighub-proof"
      - "PostgreSQL-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup (/Users/alexis/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "rendered Secret resources are separated from ConfigHub Units and must be managed out-of-band."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
