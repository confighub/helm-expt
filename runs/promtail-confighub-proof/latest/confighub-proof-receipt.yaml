apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "promtail-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.79"
  package:
    path: "packages/grafana/promtail/6.17.1"
    name: "grafana-promtail"
    chart: "grafana/promtail"
    chartVersion: "6.17.1"
    bases:
      - "default"
    selectedBase: "default"
    docVerified: true
    targetFactRequirements:
      []
  render:
    command: "cub installer setup --pull packages/grafana/promtail/6.17.1 --base default --work-dir .tmp/confighub-proof/promtail-default --non-interactive --namespace default"
    result: "pass"
    manifestCount: 5
    separatedSecretCount: 1
    facts:
      {}
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/promtail-default"
    result: "pass"
    manifestCount: 5
    separatedSecretCount: 1
  deterministicPackage:
    command: "cub installer package packages/grafana/promtail/6.17.1 -o .tmp/confighub-proof/promtail-archives/promtail-a.tgz"
    sha256: "9188545be328ef5274745f362eede9cf1610a38ca13ade9a254f613a5bee4f62"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/promtail-default"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/promtail-default --space helm-promtail-confighub-proof --component Promtail --layer App --environment Demo --owner ConfigHubHelm --variant default --unit-label Component=Promtail --unit-label HelmChart=grafana-promtail --unit-label HelmChartVersion=6.17.1 --unit-label Variant=default --unit-label Proof=promtail-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=$HOME/.confighub/config.yaml"
    space: "helm-promtail-confighub-proof"
    unitCount: 6
    kubernetesUnitCount: 5
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      - "v1/Secret default/promtail"
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/promtail-default"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/promtail-default"
    result: "pass"
    summary: "No changes.\nImages in helm-promtail-confighub-proof (post-render):\n      DaemonSet/promtail [promtail] docker.io/grafana/promtail:3.5.1"
  serverSideVariant:
    command: "cub variant create staging helm-promtail-confighub-proof --environment Staging --region local --space-name-pattern template:{{.Labels.Component}}-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-promtail-confighub-proof"
    downstreamSpace: "Promtail-staging"
    clonedUnitCount: 6
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "daemonset-default-promtail"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-promtail-confighub-proof"
      - "Promtail-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup ($HOME/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "rendered Secret resources are separated from ConfigHub Units and must be managed out-of-band."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
