apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubSafeOpsReceipt"
metadata:
  name: "redis-safe-ops-20260527"
spec:
  observedAt: "2026-05-27"
  context:
    organization: "Kubara"
    server: "https://hub.confighub.com"
    space: "helm-redis-reuse-existing-secret-confighub-proof"
    selector: "Labels.Proof = 'redis-reuse-existing-secret-confighub-proof'"
  subject:
    chart: "bitnami/redis"
    chartVersion: "25.5.3"
    packagePath: "packages/bitnami/redis/25.5.3"
    variant: "reuse-existing-secret"
  changeset:
    slug: "redis-safe-ops-20260527"
    createResult: "pass"
    updateResult: "pass"
    description: "Redis safe-ops proof: approve reviewed revisions, dry-run apply only"
    labels:
      Proof: "redis-reuse-existing-secret-confighub-proof"
      Lane: "safe-ops"
    annotations:
      proof.confighub.com/scope: "local-test"
      proof.confighub.com/live-apply: "false"
  approval:
    command: "cub unit approve statefulset-redis-redis-master --space helm-redis-reuse-existing-secret-confighub-proof --revision HeadRevisionNum --verbose --wait"
    result: "pass"
    representativeUnit: "statefulset-redis-redis-master"
    output: "Unit statefulset-redis-redis-master (7b28dcf5-4c4a-47f4-954a-2f041bde359c) has been approved\nAwaiting triggers..."
  applyDryRun:
    command: "cub unit apply --space helm-redis-reuse-existing-secret-confighub-proof --where \"Labels.Proof = 'redis-reuse-existing-secret-confighub-proof'\" --dry-run --wait --timeout 2m"
    result: "blocked-no-target"
    expected: true
    message: "Failed: HTTP 422 for req shDoNorLZOKgIsnggGEmGbNYMQSjrTtP: cannot invoke action on a unit without a target\nDetails:\n  failed to apply entity"
  cancel:
    command: "cub unit cancel --space helm-redis-reuse-existing-secret-confighub-proof --where \"Labels.Proof = 'redis-reuse-existing-secret-confighub-proof'\""
    result: "pass"
    message: "No units found matching the filter"
  safetyResult: "pass"
  interpretation: "The reviewed Units are visible in ConfigHub, but without a target ConfigHub makes no live deployment claim and blocks apply at the operation boundary."
