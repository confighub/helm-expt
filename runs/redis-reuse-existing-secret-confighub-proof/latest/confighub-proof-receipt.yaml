apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "redis-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.79"
  package:
    path: "packages/bitnami/redis/25.5.3"
    name: "bitnami-redis"
    chart: "bitnami/redis"
    chartVersion: "25.5.3"
    bases:
      - "default"
      - "reuse-existing-secret"
    selectedBase: "reuse-existing-secret"
    docVerified: true
    targetFactRequirements:
      -
        base: "reuse-existing-secret"
        requirements:
          -
            kind: "ClusterFeature"
            name: "Secret redis/redis-existing-secret key redis-password"
            namespace: "redis"
            suggestedSource: "kubectl -n redis create secret generic redis-existing-secret --from-literal=redis-password=<value>"
  render:
    command: "cub installer setup --pull packages/bitnami/redis/25.5.3 --base reuse-existing-secret --work-dir .tmp/confighub-proof/redis-reuse-existing-secret --non-interactive --namespace redis"
    result: "pass"
    manifestCount: 14
    separatedSecretCount: 0
    facts:
      targetFactChecks:
        base: "reuse-existing-secret"
        mode: "record"
        result: "recorded"
      targetFacts:
        requiredCRDs:
          []
        requiredSecrets:
          -
            keys:
              - "redis-password"
            name: "redis-existing-secret"
            namespace: "redis"
            purpose: "Redis authentication password"
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/redis-reuse-existing-secret"
    result: "pass"
    manifestCount: 14
    separatedSecretCount: 0
  deterministicPackage:
    command: "cub installer package packages/bitnami/redis/25.5.3 -o .tmp/confighub-proof/redis-reuse-existing-secret-archives/redis-a.tgz"
    sha256: "6ed8a0a3af8bcddfd18bb13c0004fd20a21c81412f2a999fffee2d79819d9444"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/redis-reuse-existing-secret"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/redis-reuse-existing-secret --space helm-redis-reuse-existing-secret-confighub-proof --component Redis --layer App --environment Demo --owner ConfigHubHelm --variant reuse-existing-secret --unit-label Component=Redis --unit-label HelmChart=bitnami-redis --unit-label HelmChartVersion=25.5.3 --unit-label Variant=reuse-existing-secret --unit-label Proof=redis-reuse-existing-secret-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=/Users/alexis/.confighub/config.yaml"
    space: "helm-redis-reuse-existing-secret-confighub-proof"
    unitCount: 15
    kubernetesUnitCount: 14
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      []
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/redis-reuse-existing-secret"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/redis-reuse-existing-secret"
    result: "pass"
    summary: "No changes.\nImages in helm-redis-reuse-existing-secret-confighub-proof (post-render):\n      StatefulSet/redis-master [redis] registry-1.docker.io/bitnami/redis@sha256:6e7a020f1f6504698a7272c58783bdc2c23588c49febbae5aca1bb8dfa10af25\n      StatefulSet/redis-replicas [redis] registry-1.docker.io/bitnami/redis@sha256:6e7a020f1f6504698a7272c58783bdc2c23588c49febbae5aca1bb8dfa10af25"
  serverSideVariant:
    command: "cub variant create staging helm-redis-reuse-existing-secret-confighub-proof --environment Staging --region local --space-name-pattern template:Redis-reuse-existing-secret-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-redis-reuse-existing-secret-confighub-proof"
    downstreamSpace: "Redis-reuse-existing-secret-staging"
    clonedUnitCount: 15
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "statefulset-redis-redis-master"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-redis-reuse-existing-secret-confighub-proof"
      - "Redis-reuse-existing-secret-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup (/Users/alexis/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
