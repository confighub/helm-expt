apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "tempo-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.79"
  package:
    path: "packages/grafana/tempo/1.24.4"
    name: "grafana-tempo"
    chart: "grafana/tempo"
    chartVersion: "1.24.4"
    bases:
      - "local-persistent"
      - "s3-query-observability"
    selectedBase: "s3-query-observability"
    docVerified: true
    targetFactRequirements:
      -
        base: "s3-query-observability"
        requirements:
          -
            kind: "ClusterFeature"
            name: "S3-compatible object store tempo/tempo-object-store"
            namespace: "tempo"
            suggestedSource: "create or bind an S3-compatible endpoint, bucket, and credentials before apply"
          -
            kind: "ClusterFeature"
            name: "Secret tempo/tempo-s3-credentials keys access_key,secret_key"
            namespace: "tempo"
            suggestedSource: "kubectl -n tempo create secret generic tempo-s3-credentials --from-literal=access_key=<value> --from-literal=secret_key=<value>"
          -
            kind: "ClusterFeature"
            name: "CRD servicemonitors.monitoring.coreos.com"
            namespace: ""
            suggestedSource: "kubectl apply -f <crd-manifest.yaml>"
  render:
    command: "cub installer setup --pull packages/grafana/tempo/1.24.4 --base s3-query-observability --work-dir .tmp/confighub-proof/tempo-s3-query-observability --non-interactive --namespace tempo"
    result: "pass"
    manifestCount: 9
    separatedSecretCount: 0
    facts:
      targetFactChecks:
        base: "s3-query-observability"
        mode: "record"
        result: "recorded"
      targetFacts:
        requiredCRDs:
          -
            deliveryLanes:
              - "regularHelm"
              - "cubInstallerApply"
              - "configHubKubectlApply"
              - "configHubOciArgo"
            name: "servicemonitors.monitoring.coreos.com"
            purpose: "Prometheus Operator ServiceMonitor CRD required by Tempo's ServiceMonitor object"
            sourcePath: "../../../prometheus-community/kube-prometheus-stack/85.3.3/revisions/default/r001/rendered/release-objects.yaml"
            sourceVariant: "prometheus-community/kube-prometheus-stack@85.3.3/default"
        requiredSecrets:
          -
            keys:
              - "access_key"
              - "secret_key"
            name: "tempo-s3-credentials"
            namespace: "tempo"
            purpose: "S3 access credentials referenced by Tempo environment variables"
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/tempo-s3-query-observability"
    result: "pass"
    manifestCount: 9
    separatedSecretCount: 0
  deterministicPackage:
    command: "cub installer package packages/grafana/tempo/1.24.4 -o .tmp/confighub-proof/tempo-s3-query-observability-archives/tempo-a.tgz"
    sha256: "c619b2293a9429e60c12080f196e3880307082f553995360eaaea14afdfa6bcd"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/tempo-s3-query-observability"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/tempo-s3-query-observability --space helm-tempo-s3-query-observability-confighub-proof --component Tempo --layer App --environment Demo --owner ConfigHubHelm --variant s3-query-observability --unit-label Component=Tempo --unit-label HelmChart=grafana-tempo --unit-label HelmChartVersion=1.24.4 --unit-label Variant=s3-query-observability --unit-label Proof=tempo-s3-query-observability-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=/Users/alexis/.confighub/config.yaml"
    space: "helm-tempo-s3-query-observability-confighub-proof"
    unitCount: 10
    kubernetesUnitCount: 9
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      []
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/tempo-s3-query-observability"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/tempo-s3-query-observability"
    result: "pass"
    summary: "No changes.\nImages in helm-tempo-s3-query-observability-confighub-proof (post-render):\n      StatefulSet/tempo [tempo] docker.io/grafana/tempo:2.9.0\n      StatefulSet/tempo [tempo-query] grafana/tempo-query:2.9.0"
  serverSideVariant:
    command: "cub variant create staging helm-tempo-s3-query-observability-confighub-proof --environment Staging --region local --space-name-pattern template:Tempo-s3-query-observability-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-tempo-s3-query-observability-confighub-proof"
    downstreamSpace: "Tempo-s3-query-observability-staging"
    clonedUnitCount: 10
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "statefulset-tempo-tempo"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-tempo-s3-query-observability-confighub-proof"
      - "Tempo-s3-query-observability-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup (/Users/alexis/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
