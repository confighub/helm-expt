apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "vault-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.80"
  package:
    path: "packages/hashicorp/vault/0.32.0"
    name: "hashicorp-vault"
    chart: "hashicorp/vault"
    chartVersion: "0.32.0"
    bases:
      - "dev-mode"
      - "default"
      - "ha-raft-ui"
    selectedBase: "default"
    docVerified: true
    targetFactRequirements:
      -
        base: "ha-raft-ui"
        requirements:
          -
            kind: "ClusterFeature"
            name: "minimum schedulable nodes 3"
            namespace: ""
            suggestedSource: "use a target with at least 3 schedulable nodes before applying this base"
  render:
    command: "cub installer setup --pull packages/hashicorp/vault/0.32.0 --base default --work-dir .tmp/confighub-proof/vault-default --non-interactive --namespace vault"
    result: "pass"
    manifestCount: 13
    separatedSecretCount: 0
    facts:
      targetFactChecks:
        base: "default"
        mode: "not-required"
        result: "pass"
      targetFacts:
        requiredCRDs:
          []
        requiredObjectStores:
          []
        requiredSecrets:
          []
        requiredTopology: null
        requiredValues:
          []
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/vault-default"
    result: "pass"
    manifestCount: 13
    separatedSecretCount: 0
  deterministicPackage:
    command: "cub installer package packages/hashicorp/vault/0.32.0 -o .tmp/confighub-proof/vault-default-archives/vault-a.tgz"
    sha256: "bda474fba0b628af687c22cab27c3f0f8c17db543d39b7fc4b67db1ee27bc654"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/vault-default"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/vault-default --space helm-vault-default-confighub-proof --component Vault --layer App --environment Demo --owner ConfigHubHelm --variant default --unit-label Component=Vault --unit-label HelmChart=hashicorp-vault --unit-label HelmChartVersion=0.32.0 --unit-label Variant=default --unit-label Proof=vault-default-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=/Users/alexis/.confighub/config.yaml"
    space: "helm-vault-default-confighub-proof"
    unitCount: 14
    kubernetesUnitCount: 13
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      []
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/vault-default"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/vault-default"
    result: "pass"
    summary: "No changes.\nImages in helm-vault-default-confighub-proof (post-render):\n      Deployment/vault-agent-injector [sidecar-injector] hashicorp/vault-k8s:1.7.2\n      StatefulSet/vault [vault] hashicorp/vault:1.21.2"
  serverSideVariant:
    command: "cub variant create staging helm-vault-default-confighub-proof --environment Staging --region local --namespace vault --space-pattern template:Vault-default-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-vault-default-confighub-proof"
    downstreamSpace: "Vault-default-staging"
    clonedUnitCount: 14
    namespace: "vault"
  serverSidePromotion:
    receipt: "variant-promotion-receipt.yaml"
    result: "pass"
    reason: "server-side promotion passed through a changeset"
    changedUnitCaughtUp: "pass"
    addedUnitCloned: "pass"
    deletionHandling: "not-tested"
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "statefulset-vault-vault"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-vault-default-confighub-proof"
      - "Vault-default-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup (/Users/alexis/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
