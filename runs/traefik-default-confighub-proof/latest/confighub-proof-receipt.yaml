apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "traefik-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.79"
  package:
    path: "packages/traefik/traefik/40.2.0"
    name: "traefik-traefik"
    chart: "traefik/traefik"
    chartVersion: "40.2.0"
    bases:
      - "default"
      - "no-crds"
    selectedBase: "default"
    docVerified: true
    targetFactRequirements:
      []
  render:
    command: "cub installer setup --pull packages/traefik/traefik/40.2.0 --base default --work-dir .tmp/confighub-proof/traefik-default --non-interactive --namespace default"
    result: "pass"
    manifestCount: 32
    separatedSecretCount: 0
    facts:
      {}
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/traefik-default"
    result: "pass"
    manifestCount: 32
    separatedSecretCount: 0
  deterministicPackage:
    command: "cub installer package packages/traefik/traefik/40.2.0 -o .tmp/confighub-proof/traefik-default-archives/traefik-a.tgz"
    sha256: "5d9c0bdf6306b0c196fbee35312d89a6df807bbbae4c64f49f831b8f8959eaa9"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/traefik-default"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/traefik-default --space helm-traefik-default-confighub-proof --component Traefik --layer App --environment Demo --owner ConfigHubHelm --variant default --unit-label Component=Traefik --unit-label HelmChart=traefik-traefik --unit-label HelmChartVersion=40.2.0 --unit-label Variant=default --unit-label Proof=traefik-default-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=$HOME/.confighub/config.yaml"
    space: "helm-traefik-default-confighub-proof"
    unitCount: 33
    kubernetesUnitCount: 32
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      []
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/traefik-default"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/traefik-default"
    result: "pass"
    summary: "No changes.\nImages in helm-traefik-default-confighub-proof (post-render):\n      Deployment/traefik [traefik] docker.io/traefik:v3.7.1"
  serverSideVariant:
    command: "cub variant create staging helm-traefik-default-confighub-proof --environment Staging --region local --space-name-pattern template:Traefik-default-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-traefik-default-confighub-proof"
    downstreamSpace: "Traefik-default-staging"
    clonedUnitCount: 33
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "deployment-default-traefik"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-traefik-default-confighub-proof"
      - "Traefik-default-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup ($HOME/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
