apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubSafeOpsReceipt"
metadata:
  name: "grafana-safe-ops-20260527"
spec:
  observedAt: "2026-05-27"
  context:
    organization: "Kubara"
    server: "https://hub.confighub.com"
    space: "helm-grafana-existing-secret-ingress-confighub-proof"
    selector: "Labels.Proof = 'grafana-existing-secret-ingress-confighub-proof'"
  subject:
    chart: "grafana/grafana"
    chartVersion: "10.5.15"
    packagePath: "packages/grafana/grafana/10.5.15"
    variant: "existing-secret-ingress"
  changeset:
    slug: "grafana-safe-ops-20260527"
    createResult: "pass"
    updateResult: "pass"
    description: "Grafana safe-ops proof: approve reviewed revisions, dry-run apply only"
    labels:
      Proof: "grafana-existing-secret-ingress-confighub-proof"
      Lane: "safe-ops"
    annotations:
      proof.confighub.com/scope: "local-test"
      proof.confighub.com/live-apply: "false"
  approval:
    command: "cub unit approve deployment-grafana-grafana --space helm-grafana-existing-secret-ingress-confighub-proof --revision HeadRevisionNum --verbose --wait"
    result: "pass"
    representativeUnit: "deployment-grafana-grafana"
    output: "Unit deployment-grafana-grafana (3d46ab32-bad1-4f79-a595-a31911083460) has been approved\nAwaiting triggers..."
  applyDryRun:
    command: "cub unit apply --space helm-grafana-existing-secret-ingress-confighub-proof --where \"Labels.Proof = 'grafana-existing-secret-ingress-confighub-proof'\" --dry-run --wait --timeout 2m"
    result: "blocked-no-target"
    expected: true
    message: "Failed: HTTP 422 for req hLgJCceJpoUzIOcMVkzELLuyVtwmYieU: cannot invoke action on a unit without a target\nDetails:\n  failed to apply entity"
  cancel:
    command: "cub unit cancel --space helm-grafana-existing-secret-ingress-confighub-proof --where \"Labels.Proof = 'grafana-existing-secret-ingress-confighub-proof'\""
    result: "pass"
    message: "No units found matching the filter"
  safetyResult: "pass"
  interpretation: "The reviewed Units are visible in ConfigHub, but without a target ConfigHub makes no live deployment claim and blocks apply at the operation boundary."
