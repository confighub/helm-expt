apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "grafana-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.79"
  package:
    path: "packages/grafana/grafana/10.5.15"
    name: "grafana-grafana"
    chart: "grafana/grafana"
    chartVersion: "10.5.15"
    bases:
      - "generated-passwords"
      - "existing-secret-ingress"
    selectedBase: "existing-secret-ingress"
    docVerified: true
    targetFactRequirements:
      -
        base: "existing-secret-ingress"
        requirements:
          -
            kind: "ClusterFeature"
            name: "Secret grafana/grafana-admin keys admin-user,admin-password"
            namespace: "grafana"
            suggestedSource: "kubectl -n grafana create secret generic grafana-admin --from-literal=admin-user=<value> --from-literal=admin-password=<value>"
  render:
    command: "cub installer setup --pull packages/grafana/grafana/10.5.15 --base existing-secret-ingress --work-dir .tmp/confighub-proof/grafana-existing-secret-ingress --non-interactive --namespace grafana"
    result: "pass"
    manifestCount: 10
    separatedSecretCount: 0
    facts:
      targetFactChecks:
        base: "existing-secret-ingress"
        mode: "record"
        result: "recorded"
      targetFacts:
        requiredCRDs:
          []
        requiredSecrets:
          -
            keys:
              - "admin-user"
              - "admin-password"
            name: "grafana-admin"
            namespace: "grafana"
            purpose: "Grafana administrator username and password"
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/grafana-existing-secret-ingress"
    result: "pass"
    manifestCount: 10
    separatedSecretCount: 0
  deterministicPackage:
    command: "cub installer package packages/grafana/grafana/10.5.15 -o .tmp/confighub-proof/grafana-existing-secret-ingress-archives/grafana-a.tgz"
    sha256: "991475b6180fec02029dbead55e98f18202b46083f71b11d50b11f33734e44a4"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/grafana-existing-secret-ingress"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/grafana-existing-secret-ingress --space helm-grafana-existing-secret-ingress-confighub-proof --component Grafana --layer App --environment Demo --owner ConfigHubHelm --variant existing-secret-ingress --unit-label Component=Grafana --unit-label HelmChart=grafana-grafana --unit-label HelmChartVersion=10.5.15 --unit-label Variant=existing-secret-ingress --unit-label Proof=grafana-existing-secret-ingress-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=/Users/alexis/.confighub/config.yaml"
    space: "helm-grafana-existing-secret-ingress-confighub-proof"
    unitCount: 11
    kubernetesUnitCount: 10
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      []
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/grafana-existing-secret-ingress"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/grafana-existing-secret-ingress"
    result: "pass"
    summary: "Plan: 0 to add, 1 to change, 0 to delete.\nSpace helm-grafana-existing-secret-ingress-confighub-proof:\n  ~ deployment-grafana-grafana\n      New changes from update from /Users/alexis/code/helm-expt-claude-clone/.tmp/confighub-proof/grafana-existing-secret-ingress/out/manifests/deployment-grafana-grafana.yaml:\n      Resource: apps/v1/Deployment grafana/grafana\n        - [Delete] spec.template.spec.containers.?name=grafana;@0.ports.?containerPort=9094;@1  (#2)\n          containerPort: 9094\n          name: gossip-tcp\n          protocol: TCP\n        ~ [Update] spec.template.spec.containers.?name=grafana;@0.ports.?containerPort=9094;@1.name  (#2)"
  serverSideVariant:
    command: "cub variant create staging helm-grafana-existing-secret-ingress-confighub-proof --environment Staging --region local --space-name-pattern template:Grafana-existing-secret-ingress-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-grafana-existing-secret-ingress-confighub-proof"
    downstreamSpace: "Grafana-existing-secret-ingress-staging"
    clonedUnitCount: 11
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "deployment-grafana-grafana"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-grafana-existing-secret-ingress-confighub-proof"
      - "Grafana-existing-secret-ingress-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup (/Users/alexis/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
