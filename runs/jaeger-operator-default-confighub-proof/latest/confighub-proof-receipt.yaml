apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "jaeger-operator-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.79"
  package:
    path: "packages/jaegertracing/jaeger-operator/2.57.0"
    name: "jaegertracing-jaeger-operator"
    chart: "jaegertracing/jaeger-operator"
    chartVersion: "2.57.0"
    bases:
      - "default"
      - "no-crds"
    selectedBase: "default"
    docVerified: true
    targetFactRequirements:
      []
  render:
    command: "cub installer setup --pull packages/jaegertracing/jaeger-operator/2.57.0 --base default --work-dir .tmp/confighub-proof/jaeger-operator-default --non-interactive --namespace default"
    result: "pass"
    manifestCount: 12
    separatedSecretCount: 0
    facts:
      {}
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/jaeger-operator-default"
    result: "pass"
    manifestCount: 12
    separatedSecretCount: 0
  deterministicPackage:
    command: "cub installer package packages/jaegertracing/jaeger-operator/2.57.0 -o .tmp/confighub-proof/jaeger-operator-default-archives/jaeger-operator-a.tgz"
    sha256: "98780a2b5ca0546b5824def5ada44accfdd3af6d228b5991bef4db542f4c2e1d"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/jaeger-operator-default"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/jaeger-operator-default --space helm-jaeger-operator-default-confighub-proof --component JaegerOperator --layer App --environment Demo --owner ConfigHubHelm --variant default --unit-label Component=JaegerOperator --unit-label HelmChart=jaegertracing-jaeger-operator --unit-label HelmChartVersion=2.57.0 --unit-label Variant=default --unit-label Proof=jaeger-operator-default-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=/Users/alexis/.confighub/config.yaml"
    space: "helm-jaeger-operator-default-confighub-proof"
    unitCount: 13
    kubernetesUnitCount: 12
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      []
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/jaeger-operator-default"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/jaeger-operator-default"
    result: "pass"
    summary: "No changes.\nImages in helm-jaeger-operator-default-confighub-proof (post-render):\n      Deployment/jaeger-operator [jaeger-operator] jaegertracing/jaeger-operator:1.61.0"
  serverSideVariant:
    command: "cub variant create staging helm-jaeger-operator-default-confighub-proof --environment Staging --region local --space-name-pattern template:JaegerOperator-default-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-jaeger-operator-default-confighub-proof"
    downstreamSpace: "JaegerOperator-default-staging"
    clonedUnitCount: 13
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "deployment-default-jaeger-operator"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-jaeger-operator-default-confighub-proof"
      - "JaegerOperator-default-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup (/Users/alexis/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
