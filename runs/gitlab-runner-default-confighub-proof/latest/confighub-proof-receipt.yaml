apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "gitlab-runner-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.79"
  package:
    path: "packages/gitlab/gitlab-runner/0.89.0"
    name: "gitlab-gitlab-runner"
    chart: "gitlab/gitlab-runner"
    chartVersion: "0.89.0"
    bases:
      - "default"
    selectedBase: "default"
    docVerified: true
    targetFactRequirements:
      []
  render:
    command: "cub installer setup --pull packages/gitlab/gitlab-runner/0.89.0 --base default --work-dir .tmp/confighub-proof/gitlab-runner-default --non-interactive --namespace default"
    result: "pass"
    manifestCount: 3
    separatedSecretCount: 0
    facts:
      {}
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/gitlab-runner-default"
    result: "pass"
    manifestCount: 3
    separatedSecretCount: 0
  deterministicPackage:
    command: "cub installer package packages/gitlab/gitlab-runner/0.89.0 -o .tmp/confighub-proof/gitlab-runner-default-archives/gitlab-runner-a.tgz"
    sha256: "e14db565ea0b5e569dcb29d5854158da33e0c003bb46a61af64596dc14232d4e"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/gitlab-runner-default"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/gitlab-runner-default --space helm-gitlab-runner-default-confighub-proof --component GitlabRunner --layer App --environment Demo --owner ConfigHubHelm --variant default --unit-label Component=GitlabRunner --unit-label HelmChart=gitlab-gitlab-runner --unit-label HelmChartVersion=0.89.0 --unit-label Variant=default --unit-label Proof=gitlab-runner-default-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=/Users/alexis/.confighub/config.yaml"
    space: "helm-gitlab-runner-default-confighub-proof"
    unitCount: 4
    kubernetesUnitCount: 3
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      []
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/gitlab-runner-default"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/gitlab-runner-default"
    result: "pass"
    summary: "No changes.\nImages in helm-gitlab-runner-default-confighub-proof (post-render):\n      Deployment/gitlab-runner [gitlab-runner] registry.gitlab.com/gitlab-org/gitlab-runner:alpine-v19.0.0"
  serverSideVariant:
    command: "cub variant create staging helm-gitlab-runner-default-confighub-proof --environment Staging --region local --space-name-pattern template:GitlabRunner-default-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-gitlab-runner-default-confighub-proof"
    downstreamSpace: "GitlabRunner-default-staging"
    clonedUnitCount: 4
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "deployment-default-gitlab-runner"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-gitlab-runner-default-confighub-proof"
      - "GitlabRunner-default-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup (/Users/alexis/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
