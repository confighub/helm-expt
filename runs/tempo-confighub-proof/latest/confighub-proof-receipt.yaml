apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "tempo-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.49"
  package:
    path: "packages/grafana/tempo/1.24.4"
    name: "grafana-tempo"
    chart: "grafana/tempo"
    chartVersion: "1.24.4"
    bases:
      - "local-persistent"
      - "s3-query-observability"
    selectedBase: "local-persistent"
    docVerified: true
    targetFactRequirements:
      -
        base: "s3-query-observability"
        requirements:
          -
            kind: "ClusterFeature"
            name: "Secret tempo/tempo-s3-credentials keys access_key,secret_key"
            namespace: "tempo"
            suggestedSource: "kubectl -n tempo create secret generic tempo-s3-credentials --from-literal=access_key=<value> --from-literal=secret_key=<value>"
  render:
    command: "cub install setup --pull packages/grafana/tempo/1.24.4 --base local-persistent --work-dir .tmp/confighub-proof/tempo-local-persistent --non-interactive --namespace tempo"
    result: "pass"
    manifestCount: 5
    separatedSecretCount: 0
    facts:
      targetFactChecks:
        base: "local-persistent"
        mode: "not-required"
        result: "pass"
      targetFacts:
        requiredSecrets:
          []
  rerender:
    command: "cub install render --work-dir .tmp/confighub-proof/tempo-local-persistent"
    result: "pass"
    manifestCount: 5
    separatedSecretCount: 0
  deterministicPackage:
    command: "cub install package packages/grafana/tempo/1.24.4 -o .tmp/confighub-proof/tempo-archives/tempo-a.tgz"
    sha256: "1d0726aae56de97cd3c9f501dcffdbc5746b46de731accf95bedaf90b3c38911"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub install vet --work-dir .tmp/confighub-proof/tempo-local-persistent"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub install upload --work-dir .tmp/confighub-proof/tempo-local-persistent --space helm-tempo-confighub-proof --component Tempo --layer App --environment Demo --owner ConfigHubHelm --variant local-persistent --unit-label Component=Tempo --unit-label HelmChart=grafana-tempo --unit-label HelmChartVersion=1.24.4 --unit-label Variant=local-persistent --unit-label Proof=tempo-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=/Users/alexis/.confighub/config.yaml"
    space: "helm-tempo-confighub-proof"
    unitCount: 6
    kubernetesUnitCount: 5
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      []
  plan:
    preUpload:
      command: "cub install plan --work-dir .tmp/confighub-proof/tempo-local-persistent"
      result: "expected-missing-upload-state"
    command: "cub install plan --work-dir .tmp/confighub-proof/tempo-local-persistent"
    result: "pass"
    summary: "No changes.\nImages in helm-tempo-confighub-proof (post-render):\n      StatefulSet/tempo [tempo] docker.io/grafana/tempo:2.9.0"
  serverSideVariant:
    command: "cub variant create staging helm-tempo-confighub-proof --environment Staging --region local --space-name-pattern template:{{.SourceEntitySlug}}-{{.Labels.Variant}} --allow-exists"
    result: "pass"
    upstreamSpace: "helm-tempo-confighub-proof"
    downstreamSpace: "helm-tempo-confighub-proof-staging"
    clonedUnitCount: 6
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "statefulset-tempo-tempo"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-tempo-confighub-proof"
      - "helm-tempo-confighub-proof-staging"
  observedFriction:
    - "cub install upload needs explicit CUB_CONFIG in this local setup (/Users/alexis/.confighub/config.yaml)."
    - "pre-upload cub install plan is expected to fail until upload state exists."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
