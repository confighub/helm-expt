apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "vertical-pod-autoscaler-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.79"
  package:
    path: "packages/autoscaler/vertical-pod-autoscaler/0.9.0"
    name: "autoscaler-vertical-pod-autoscaler"
    chart: "autoscaler/vertical-pod-autoscaler"
    chartVersion: "0.9.0"
    bases:
      - "default"
      - "no-crds"
    selectedBase: "default"
    docVerified: true
    targetFactRequirements:
      -
        base: "default"
        requirements:
          -
            kind: "ClusterFeature"
            name: "Secret default/vpa-tls-certs keys ca,cert,key"
            namespace: "default"
            suggestedSource: "Run the chart certgen lifecycle action or stage a valid vpa-tls-certs Secret before waiting for the admission controller"
      -
        base: "no-crds"
        requirements:
          -
            kind: "ClusterFeature"
            name: "Secret default/vpa-tls-certs keys ca,cert,key"
            namespace: "default"
            suggestedSource: "Run the chart certgen lifecycle action or stage a valid vpa-tls-certs Secret before waiting for the admission controller"
  render:
    command: "cub installer setup --pull packages/autoscaler/vertical-pod-autoscaler/0.9.0 --base default --work-dir .tmp/confighub-proof/vertical-pod-autoscaler-default --non-interactive --namespace default"
    result: "pass"
    manifestCount: 40
    separatedSecretCount: 0
    facts:
      targetFactChecks:
        base: "default"
        mode: "record"
        result: "recorded"
      targetFacts:
        requiredCRDs:
          []
        requiredSecrets:
          -
            deliveryLanes:
              - "regularHelm"
              - "cubInstallerApply"
              - "configHubKubectlApply"
              - "configHubOciArgo"
            keys:
              - "ca"
              - "cert"
              - "key"
            name: "vpa-tls-certs"
            namespace: "default"
            purpose: "Webhook serving certificate Secret mounted by the VPA admission controller; Helm normally creates this via the certgen hook"
            suggestedSource: "Run the chart certgen lifecycle action or stage a valid vpa-tls-certs Secret before waiting for the admission controller"
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/vertical-pod-autoscaler-default"
    result: "pass"
    manifestCount: 40
    separatedSecretCount: 0
  deterministicPackage:
    command: "cub installer package packages/autoscaler/vertical-pod-autoscaler/0.9.0 -o .tmp/confighub-proof/vertical-pod-autoscaler-default-archives/vertical-pod-autoscaler-a.tgz"
    sha256: "d6f9c9b6c8ac5ec53e4dda67c74949a452cafe6b64352828cd2570f4c96500f7"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/vertical-pod-autoscaler-default"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/vertical-pod-autoscaler-default --space helm-vertical-pod-autoscaler-default-confighub-proof --component VerticalPodAutoscaler --layer App --environment Demo --owner ConfigHubHelm --variant default --unit-label Component=VerticalPodAutoscaler --unit-label HelmChart=autoscaler-vertical-pod-autoscaler --unit-label HelmChartVersion=0.9.0 --unit-label Variant=default --unit-label Proof=vertical-pod-autoscaler-default-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=/Users/alexis/.confighub/config.yaml"
    space: "helm-vertical-pod-autoscaler-default-confighub-proof"
    unitCount: 41
    kubernetesUnitCount: 40
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      []
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/vertical-pod-autoscaler-default"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/vertical-pod-autoscaler-default"
    result: "pass"
    summary: "No changes.\nImages in helm-vertical-pod-autoscaler-default-confighub-proof (post-render):\n      Deployment/vertical-pod-autoscaler-admission-controller [admission-controller] registry.k8s.io/autoscaling/vpa-admission-controller:1.6.0\n      Deployment/vertical-pod-autoscaler-recommender [recommender] registry.k8s.io/autoscaling/vpa-recommender:1.6.0\n      Deployment/vertical-pod-autoscaler-updater [updater] registry.k8s.io/autoscaling/vpa-updater:1.6.0"
  serverSideVariant:
    command: "cub variant create staging helm-vertical-pod-autoscaler-default-confighub-proof --environment Staging --region local --space-name-pattern template:VerticalPodAutoscaler-default-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-vertical-pod-autoscaler-default-confighub-proof"
    downstreamSpace: "VerticalPodAutoscaler-default-staging"
    clonedUnitCount: 41
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "deployment-default-vertical-pod-autoscaler-recommender"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-vertical-pod-autoscaler-default-confighub-proof"
      - "VerticalPodAutoscaler-default-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup (/Users/alexis/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
