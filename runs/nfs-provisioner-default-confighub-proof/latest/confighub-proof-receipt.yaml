apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "nfs-provisioner-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.80"
  package:
    path: "packages/nfs-subdir-external-provisioner/nfs-subdir-external-provisioner/4.0.18"
    name: "nfs-subdir-external-provisioner-nfs-subdir-external-provisioner"
    chart: "nfs-subdir-external-provisioner/nfs-subdir-external-provisioner"
    chartVersion: "4.0.18"
    bases:
      - "default"
    selectedBase: "default"
    docVerified: true
    targetFactRequirements:
      []
  render:
    command: "cub installer setup --pull packages/nfs-subdir-external-provisioner/nfs-subdir-external-provisioner/4.0.18 --base default --work-dir .tmp/confighub-proof/nfs-provisioner-default --non-interactive --namespace default"
    result: "pass"
    manifestCount: 8
    separatedSecretCount: 0
    facts:
      {}
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/nfs-provisioner-default"
    result: "pass"
    manifestCount: 8
    separatedSecretCount: 0
  deterministicPackage:
    command: "cub installer package packages/nfs-subdir-external-provisioner/nfs-subdir-external-provisioner/4.0.18 -o .tmp/confighub-proof/nfs-provisioner-default-archives/nfs-provisioner-a.tgz"
    sha256: "c34aaf40e578bf4557a032c6869ac3f3001d1489b23d140f972c7b3004e5f6de"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/nfs-provisioner-default"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/nfs-provisioner-default --space helm-nfs-provisioner-default-confighub-proof --component NfsProvisioner --layer App --environment Demo --owner ConfigHubHelm --variant default --unit-label Component=NfsProvisioner --unit-label HelmChart=nfs-subdir-external-provisioner-nfs-subdir-external-provisioner --unit-label HelmChartVersion=4.0.18 --unit-label Variant=default --unit-label Proof=nfs-provisioner-default-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=$HOME/.confighub/config.yaml"
    space: "helm-nfs-provisioner-default-confighub-proof"
    unitCount: 9
    kubernetesUnitCount: 8
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      []
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/nfs-provisioner-default"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/nfs-provisioner-default"
    result: "pass"
    summary: "No changes.\nImages in helm-nfs-provisioner-default-confighub-proof (post-render):\n      Deployment/nfs-subdir-external-provisioner [nfs-subdir-external-provisioner] registry.k8s.io/sig-storage/nfs-subdir-external-provisioner:v4.0.2"
  serverSideVariant:
    command: "cub variant create staging helm-nfs-provisioner-default-confighub-proof --environment Staging --region local --namespace default --space-pattern template:NfsProvisioner-default-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-nfs-provisioner-default-confighub-proof"
    downstreamSpace: "NfsProvisioner-default-staging"
    clonedUnitCount: 9
    namespace: "default"
  serverSidePromotion:
    receipt: "variant-promotion-receipt.yaml"
    result: "pass"
    reason: "server-side promotion passed through a changeset"
    changedUnitCaughtUp: "pass"
    addedUnitCloned: "pass"
    deletionHandling: "not-tested"
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "deployment-nfs-subdir-external-provisioner"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-nfs-provisioner-default-confighub-proof"
      - "NfsProvisioner-default-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup ($HOME/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
