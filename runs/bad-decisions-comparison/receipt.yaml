apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "BadDecisionsComparisonReceipt"
metadata:
  name: "bad-decisions-cmp-20260621"
spec:
  observedAt: "2026-06-21T14:21:33.764Z"
  decisions: 18
  chartsCompared: 10
  cases: 180
  helm:
    rejected: 2
    leaked: 60
    absorbed: 118
  timing:
    helmBeforeCluster: 2
    helmAtApiOnly: 60
    helmNever: 118
    cubAtReview: 180
  unclassified: 0
  result: "pass"
  claim: "Same bad decisions, asked WHEN you find out. Raw Helm surfaces only render-rejected decisions before the cluster; leaked values reach the live k8s API at apply and absorbed values silently succeed. Config-as-data renders the same output but surfaces every outcome — change, empty no-op, or render error — as a reviewable diff before apply: 100% at review, 0 silent. (cub does not reject more; the difference is visibility and timing.)"
  charts:
    -
      slug: "bitnami/nginx"
      version: "25.0.0"
      status: "compared"
      cases:
        -
          decision: "replicas-negative"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "replicas-zero"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "replicas-absurd"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "replicas-string"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "image-pullpolicy-bad"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "cpu-limit-nonsense"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "mem-limit-negative"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "service-type-bad"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "service-port-overflow"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "service-port-negative"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "bool-as-string"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "grace-negative"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "probe-negative"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "priorityclass-missing"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "nodeselector-impossible"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "storageclass-missing"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "typo-key-noop"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "deep-typo-key-noop"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
    -
      slug: "bitnami/redis"
      version: "27.0.0"
      status: "compared"
      cases:
        -
          decision: "replicas-negative"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "replicas-zero"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "replicas-absurd"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "replicas-string"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "image-pullpolicy-bad"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "cpu-limit-nonsense"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "mem-limit-negative"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "service-type-bad"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "service-port-overflow"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "service-port-negative"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "bool-as-string"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "grace-negative"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "probe-negative"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "priorityclass-missing"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "nodeselector-impossible"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "storageclass-missing"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "typo-key-noop"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "deep-typo-key-noop"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
    -
      slug: "bitnami/mysql"
      version: "14.0.3"
      status: "compared"
      cases:
        -
          decision: "replicas-negative"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "replicas-zero"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "replicas-absurd"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "replicas-string"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "image-pullpolicy-bad"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "cpu-limit-nonsense"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "mem-limit-negative"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "service-type-bad"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "service-port-overflow"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "service-port-negative"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "bool-as-string"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "grace-negative"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "probe-negative"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "priorityclass-missing"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "nodeselector-impossible"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "storageclass-missing"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "typo-key-noop"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "deep-typo-key-noop"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
    -
      slug: "bitnami/mongodb"
      version: "19.1.0"
      status: "compared"
      cases:
        -
          decision: "replicas-negative"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "replicas-zero"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "replicas-absurd"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "replicas-string"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "image-pullpolicy-bad"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "cpu-limit-nonsense"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "mem-limit-negative"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "service-type-bad"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "service-port-overflow"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "service-port-negative"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "bool-as-string"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "grace-negative"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "probe-negative"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "priorityclass-missing"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "nodeselector-impossible"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "storageclass-missing"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "typo-key-noop"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "deep-typo-key-noop"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
    -
      slug: "bitnami/rabbitmq"
      version: "16.0.14"
      status: "compared"
      cases:
        -
          decision: "replicas-negative"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "replicas-zero"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "replicas-absurd"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "replicas-string"
          helm: "rejected"
          helmWhen: "render (before cluster)"
          cub: "render-error"
          cubWhen: "review (author catches it)"
        -
          decision: "image-pullpolicy-bad"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "cpu-limit-nonsense"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "mem-limit-negative"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "service-type-bad"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "service-port-overflow"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "service-port-negative"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "bool-as-string"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "grace-negative"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "probe-negative"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "priorityclass-missing"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "nodeselector-impossible"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "storageclass-missing"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "typo-key-noop"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "deep-typo-key-noop"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
    -
      slug: "fluent/fluent-bit"
      version: "0.57.6"
      status: "compared"
      cases:
        -
          decision: "replicas-negative"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "replicas-zero"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "replicas-absurd"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "replicas-string"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "image-pullpolicy-bad"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "cpu-limit-nonsense"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "mem-limit-negative"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "service-type-bad"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "service-port-overflow"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "service-port-negative"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "bool-as-string"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "grace-negative"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "probe-negative"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "priorityclass-missing"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "nodeselector-impossible"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "storageclass-missing"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "typo-key-noop"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "deep-typo-key-noop"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
    -
      slug: "prometheus-community/prometheus"
      version: "29.9.0"
      status: "compared"
      cases:
        -
          decision: "replicas-negative"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "replicas-zero"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "replicas-absurd"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "replicas-string"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "image-pullpolicy-bad"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "cpu-limit-nonsense"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "mem-limit-negative"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "service-type-bad"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "service-port-overflow"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "service-port-negative"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "bool-as-string"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "grace-negative"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "probe-negative"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "priorityclass-missing"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "nodeselector-impossible"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "storageclass-missing"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "typo-key-noop"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "deep-typo-key-noop"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
    -
      slug: "grafana/grafana"
      version: "10.5.15"
      status: "compared"
      cases:
        -
          decision: "replicas-negative"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "replicas-zero"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "replicas-absurd"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "replicas-string"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "image-pullpolicy-bad"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "cpu-limit-nonsense"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "mem-limit-negative"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "service-type-bad"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "service-port-overflow"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "service-port-negative"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "bool-as-string"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "grace-negative"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "probe-negative"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "priorityclass-missing"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "nodeselector-impossible"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "storageclass-missing"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "typo-key-noop"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "deep-typo-key-noop"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
    -
      slug: "ingress-nginx/ingress-nginx"
      version: "4.15.1"
      status: "compared"
      cases:
        -
          decision: "replicas-negative"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "replicas-zero"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "replicas-absurd"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "replicas-string"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "image-pullpolicy-bad"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "cpu-limit-nonsense"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "mem-limit-negative"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "service-type-bad"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "service-port-overflow"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "service-port-negative"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "bool-as-string"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "grace-negative"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "probe-negative"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "priorityclass-missing"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "nodeselector-impossible"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "storageclass-missing"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "typo-key-noop"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "deep-typo-key-noop"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
    -
      slug: "metrics-server/metrics-server"
      version: "3.13.0"
      status: "compared"
      cases:
        -
          decision: "replicas-negative"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "replicas-zero"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "replicas-absurd"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "replicas-string"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "image-pullpolicy-bad"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "cpu-limit-nonsense"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "mem-limit-negative"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "service-type-bad"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "service-port-overflow"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "service-port-negative"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "bool-as-string"
          helm: "rejected"
          helmWhen: "render (before cluster)"
          cub: "render-error"
          cubWhen: "review (author catches it)"
        -
          decision: "grace-negative"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "probe-negative"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "priorityclass-missing"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "nodeselector-impossible"
          helm: "leaked"
          helmWhen: "live k8s API (at apply, in cluster)"
          cub: "visible-in-config"
          cubWhen: "review (the value is in the config you review)"
        -
          decision: "storageclass-missing"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "typo-key-noop"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
        -
          decision: "deep-typo-key-noop"
          helm: "absorbed"
          helmWhen: "never (silent success)"
          cub: "visible-noop-edit"
          cubWhen: "review (a visible no-op edit, not a silent success)"
