apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "postgresql-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.79"
  package:
    path: "packages/bitnami/postgresql/18.6.7"
    name: "bitnami-postgresql"
    chart: "bitnami/postgresql"
    chartVersion: "18.6.7"
    bases:
      - "generated-passwords"
      - "existing-secret"
    selectedBase: "existing-secret"
    docVerified: true
    targetFactRequirements:
      -
        base: "existing-secret"
        requirements:
          -
            kind: "ClusterFeature"
            name: "Secret postgresql/postgresql-auth key postgres-password"
            namespace: "postgresql"
            suggestedSource: "kubectl -n postgresql create secret generic postgresql-auth --from-literal=postgres-password=<value>"
  render:
    command: "cub installer setup --pull packages/bitnami/postgresql/18.6.7 --base existing-secret --work-dir .tmp/confighub-proof/postgresql-existing-secret --non-interactive --namespace postgresql"
    result: "pass"
    manifestCount: 7
    separatedSecretCount: 0
    facts:
      targetFactChecks:
        base: "existing-secret"
        mode: "record"
        result: "recorded"
      targetFacts:
        requiredCRDs:
          []
        requiredSecrets:
          -
            keys:
              - "postgres-password"
            name: "postgresql-auth"
            namespace: "postgresql"
            purpose: "PostgreSQL administrator password"
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/postgresql-existing-secret"
    result: "pass"
    manifestCount: 7
    separatedSecretCount: 0
  deterministicPackage:
    command: "cub installer package packages/bitnami/postgresql/18.6.7 -o .tmp/confighub-proof/postgresql-existing-secret-archives/postgresql-a.tgz"
    sha256: "6eb1e6f44ed42268e41c8fe7cf8c94fb1d68016ef06ecf242451378c189af30e"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/postgresql-existing-secret"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/postgresql-existing-secret --space helm-postgresql-existing-secret-confighub-proof --component PostgreSQL --layer App --environment Demo --owner ConfigHubHelm --variant existing-secret --unit-label Component=PostgreSQL --unit-label HelmChart=bitnami-postgresql --unit-label HelmChartVersion=18.6.7 --unit-label Variant=existing-secret --unit-label Proof=postgresql-existing-secret-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=/Users/alexis/.confighub/config.yaml"
    space: "helm-postgresql-existing-secret-confighub-proof"
    unitCount: 8
    kubernetesUnitCount: 7
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      []
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/postgresql-existing-secret"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/postgresql-existing-secret"
    result: "pass"
    summary: "No changes.\nImages in helm-postgresql-existing-secret-confighub-proof (post-render):\n      StatefulSet/postgresql [postgresql] registry-1.docker.io/bitnami/postgresql@sha256:2d3c068e58f89e0e29c6d41ffa3d00ff253e87a9426662c3ef11d2a0942ee653"
  serverSideVariant:
    command: "cub variant create staging helm-postgresql-existing-secret-confighub-proof --environment Staging --region local --space-name-pattern template:PostgreSQL-existing-secret-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-postgresql-existing-secret-confighub-proof"
    downstreamSpace: "PostgreSQL-existing-secret-staging"
    clonedUnitCount: 8
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "statefulset-postgresql-postgresql"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-postgresql-existing-secret-confighub-proof"
      - "PostgreSQL-existing-secret-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup (/Users/alexis/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
