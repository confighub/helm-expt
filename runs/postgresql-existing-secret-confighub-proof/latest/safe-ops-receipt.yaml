apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubSafeOpsReceipt"
metadata:
  name: "postgresql-safe-ops-20260527"
spec:
  observedAt: "2026-05-27"
  context:
    organization: "Kubara"
    server: "https://hub.confighub.com"
    space: "helm-postgresql-existing-secret-confighub-proof"
    selector: "Labels.Proof = 'postgresql-existing-secret-confighub-proof'"
  subject:
    chart: "bitnami/postgresql"
    chartVersion: "18.6.7"
    packagePath: "packages/bitnami/postgresql/18.6.7"
    variant: "existing-secret"
  changeset:
    slug: "postgresql-safe-ops-20260527"
    createResult: "pass"
    updateResult: "pass"
    description: "PostgreSQL safe-ops proof: approve reviewed revisions, dry-run apply only"
    labels:
      Proof: "postgresql-existing-secret-confighub-proof"
      Lane: "safe-ops"
    annotations:
      proof.confighub.com/scope: "local-test"
      proof.confighub.com/live-apply: "false"
  approval:
    command: "cub unit approve statefulset-postgresql-postgresql --space helm-postgresql-existing-secret-confighub-proof --revision HeadRevisionNum --verbose --wait"
    result: "pass"
    representativeUnit: "statefulset-postgresql-postgresql"
    output: "Unit statefulset-postgresql-postgresql (5c2faceb-e83f-4eb0-b2bc-ab0325b87749) has been approved\nAwaiting triggers..."
  applyDryRun:
    command: "cub unit apply --space helm-postgresql-existing-secret-confighub-proof --where \"Labels.Proof = 'postgresql-existing-secret-confighub-proof'\" --dry-run --wait --timeout 2m"
    result: "blocked-no-target"
    expected: true
    message: "Failed: HTTP 422 for req gwrsBkTDCrUeJnqJsPrRLjiTAMxWDlwU: cannot invoke action on a unit without a target\nDetails:\n  failed to apply entity"
  cancel:
    command: "cub unit cancel --space helm-postgresql-existing-secret-confighub-proof --where \"Labels.Proof = 'postgresql-existing-secret-confighub-proof'\""
    result: "pass"
    message: "No units found matching the filter"
  safetyResult: "pass"
  interpretation: "The reviewed Units are visible in ConfigHub, but without a target ConfigHub makes no live deployment claim and blocks apply at the operation boundary."
