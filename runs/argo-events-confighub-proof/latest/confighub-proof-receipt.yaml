apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "argo-events-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.79"
  package:
    path: "packages/argo-cd/argo-events/2.4.21"
    name: "argo-cd-argo-events"
    chart: "argo-cd/argo-events"
    chartVersion: "2.4.21"
    bases:
      - "default"
      - "no-crds"
    selectedBase: "default"
    docVerified: true
    targetFactRequirements:
      []
  render:
    command: "cub installer setup --pull packages/argo-cd/argo-events/2.4.21 --base default --work-dir .tmp/confighub-proof/argo-events-default --non-interactive --namespace default"
    result: "pass"
    manifestCount: 10
    separatedSecretCount: 0
    facts:
      {}
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/argo-events-default"
    result: "pass"
    manifestCount: 10
    separatedSecretCount: 0
  deterministicPackage:
    command: "cub installer package packages/argo-cd/argo-events/2.4.21 -o .tmp/confighub-proof/argo-events-archives/argo-events-a.tgz"
    sha256: "305f63669cfe2d1fb456b90bb025043320cb214ec3e023db16547106b6454848"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/argo-events-default"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/argo-events-default --space helm-argo-events-confighub-proof --component ArgoEvents --layer App --environment Demo --owner ConfigHubHelm --variant default --unit-label Component=ArgoEvents --unit-label HelmChart=argo-cd-argo-events --unit-label HelmChartVersion=2.4.21 --unit-label Variant=default --unit-label Proof=argo-events-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=/Users/alexis/.confighub/config.yaml"
    space: "helm-argo-events-confighub-proof"
    unitCount: 11
    kubernetesUnitCount: 10
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      []
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/argo-events-default"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/argo-events-default"
    result: "pass"
    summary: "No changes.\nImages in helm-argo-events-confighub-proof (post-render):\n      Deployment/argo-events-controller-manager [controller-manager] quay.io/argoproj/argo-events:v1.9.10"
  serverSideVariant:
    command: "cub variant create staging helm-argo-events-confighub-proof --environment Staging --region local --space-name-pattern template:{{.Labels.Component}}-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-argo-events-confighub-proof"
    downstreamSpace: "ArgoEvents-staging"
    clonedUnitCount: 11
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "deployment-default-argo-events-controller-manager"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-argo-events-confighub-proof"
      - "ArgoEvents-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup (/Users/alexis/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
