apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "nginx-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    v0.1.71"
      cubServerVersion: "v0.1.74"
  package:
    path: "packages/bitnami/nginx/24.0.2"
    name: "bitnami-nginx"
    chart: "bitnami/nginx"
    chartVersion: "24.0.2"
    bases:
      - "http-clusterip"
      - "existing-tls-ingress"
    selectedBase: "http-clusterip"
    docVerified: true
    targetFactRequirements:
      -
        base: "existing-tls-ingress"
        requirements:
          -
            kind: "ClusterFeature"
            name: "Secret nginx/nginx-backend-tls keys tls.crt,tls.key,ca.crt"
            namespace: "nginx"
            suggestedSource: "kubectl -n nginx create secret generic nginx-backend-tls --from-literal=tls.crt=<value> --from-literal=tls.key=<value> --from-literal=ca.crt=<value>"
          -
            kind: "ClusterFeature"
            name: "Secret nginx/nginx-ingress-tls keys tls.crt,tls.key"
            namespace: "nginx"
            suggestedSource: "kubectl -n nginx create secret generic nginx-ingress-tls --from-literal=tls.crt=<value> --from-literal=tls.key=<value>"
  render:
    command: "cub installer setup --pull packages/bitnami/nginx/24.0.2 --base http-clusterip --work-dir .tmp/confighub-proof/nginx-http-clusterip --non-interactive --namespace nginx"
    result: "pass"
    manifestCount: 6
    separatedSecretCount: 0
    facts:
      targetFactChecks:
        base: "http-clusterip"
        mode: "not-required"
        result: "pass"
      targetFacts:
        requiredSecrets:
          []
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/nginx-http-clusterip"
    result: "pass"
    manifestCount: 6
    separatedSecretCount: 0
  deterministicPackage:
    command: "cub installer package packages/bitnami/nginx/24.0.2 -o .tmp/confighub-proof/nginx-archives/nginx-a.tgz"
    sha256: "31e3efed39908c535f981e59a2267e792a0d1a46a8f996c7dcc12b2c5b892e78"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/nginx-http-clusterip"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/nginx-http-clusterip --space helm-nginx-confighub-proof --component NGINX --layer App --environment Demo --owner ConfigHubHelm --variant http-clusterip --unit-label Component=NGINX --unit-label HelmChart=bitnami-nginx --unit-label HelmChartVersion=24.0.2 --unit-label Variant=http-clusterip --unit-label Proof=nginx-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=/Users/alexis/.confighub/config.yaml"
    space: "helm-nginx-confighub-proof"
    unitCount: 7
    kubernetesUnitCount: 6
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      []
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/nginx-http-clusterip"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/nginx-http-clusterip"
    result: "pass"
    summary: "No changes.\nImages in helm-nginx-confighub-proof (post-render):\n  init  Deployment/nginx [preserve-logs-symlinks] registry-1.docker.io/bitnami/nginx:latest\n      Deployment/nginx [nginx] registry-1.docker.io/bitnami/nginx:latest"
  serverSideVariant:
    command: "cub variant create staging helm-nginx-confighub-proof --environment Staging --region local --space-name-pattern template:{{.Labels.Component}}-{{.Labels.Variant}} --allow-exists"
    result: "pass"
    upstreamSpace: "helm-nginx-confighub-proof"
    downstreamSpace: "NGINX-staging"
    clonedUnitCount: 7
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "deployment-nginx-nginx"
  retention:
    liveSpacesDeletedAfterProof: false
    spaces:
      - "helm-nginx-confighub-proof"
      - "NGINX-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup (/Users/alexis/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
