apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "mysql-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.79"
  package:
    path: "packages/bitnami/mysql/14.0.3"
    name: "bitnami-mysql"
    chart: "bitnami/mysql"
    chartVersion: "14.0.3"
    bases:
      - "static-passwords"
      - "existing-secret"
    selectedBase: "static-passwords"
    docVerified: true
    targetFactRequirements:
      -
        base: "existing-secret"
        requirements:
          -
            kind: "ClusterFeature"
            name: "Secret mysql/mysql-auth keys mysql-root-password,mysql-password,mysql-replication-password"
            namespace: "mysql"
            suggestedSource: "kubectl -n mysql create secret generic mysql-auth --from-literal=mysql-root-password=<value> --from-literal=mysql-password=<value> --from-literal=mysql-replication-password=<value>"
  render:
    command: "cub installer setup --pull packages/bitnami/mysql/14.0.3 --base static-passwords --work-dir .tmp/confighub-proof/mysql-static-passwords --non-interactive --namespace mysql"
    result: "pass"
    manifestCount: 8
    separatedSecretCount: 1
    facts:
      targetFactChecks:
        base: "static-passwords"
        mode: "not-required"
        result: "pass"
      targetFacts:
        requiredCRDs:
          []
        requiredObjectStores:
          []
        requiredSecrets:
          []
        requiredTopology: null
        requiredValues:
          []
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/mysql-static-passwords"
    result: "pass"
    manifestCount: 8
    separatedSecretCount: 1
  deterministicPackage:
    command: "cub installer package packages/bitnami/mysql/14.0.3 -o .tmp/confighub-proof/mysql-archives/mysql-a.tgz"
    sha256: "141a6b01c3e7b0276e22b8df64d3076e2849d0ae4270d3759e938d218d120a43"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/mysql-static-passwords"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/mysql-static-passwords --space helm-mysql-confighub-proof --component MySQL --layer App --environment Demo --owner ConfigHubHelm --variant static-passwords --unit-label Component=MySQL --unit-label HelmChart=bitnami-mysql --unit-label HelmChartVersion=14.0.3 --unit-label Variant=static-passwords --unit-label Proof=mysql-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=/Users/alexis/.confighub/config.yaml"
    space: "helm-mysql-confighub-proof"
    unitCount: 9
    kubernetesUnitCount: 8
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      - "v1/Secret mysql/mysql"
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/mysql-static-passwords"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/mysql-static-passwords"
    result: "pass"
    summary: "No changes.\nImages in helm-mysql-confighub-proof (post-render):\n  init  StatefulSet/mysql [preserve-logs-symlinks] docker.io/bitnamilegacy/mysql:9.4.0-debian-12-r1\n      StatefulSet/mysql [mysql] docker.io/bitnamilegacy/mysql:9.4.0-debian-12-r1"
  serverSideVariant:
    command: "cub variant create staging helm-mysql-confighub-proof --environment Staging --region local --namespace mysql --space-pattern template:{{.Labels.Component}}-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-mysql-confighub-proof"
    downstreamSpace: "MySQL-staging"
    clonedUnitCount: 9
    namespace: "mysql"
  serverSidePromotion:
    receipt: "variant-promotion-receipt.yaml"
    result: "watch"
    reason: "server-side promotion mechanics passed, but changeset-bound promote failed and required the no-changeset fallback"
    changedUnitCaughtUp: "pass"
    addedUnitCloned: "pass"
    deletionHandling: "not-tested"
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "statefulset-mysql-mysql"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-mysql-confighub-proof"
      - "MySQL-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup (/Users/alexis/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "rendered Secret resources are separated from ConfigHub Units and must be managed out-of-band."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
