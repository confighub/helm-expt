apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "mysql-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.49"
  package:
    path: "packages/bitnami/mysql/14.0.3"
    name: "bitnami-mysql"
    chart: "bitnami/mysql"
    chartVersion: "14.0.3"
    bases:
      - "generated-passwords"
      - "existing-secret"
    selectedBase: "generated-passwords"
    docVerified: true
    targetFactRequirements:
      -
        base: "existing-secret"
        requirements:
          -
            kind: "ClusterFeature"
            name: "Secret mysql/mysql-auth keys mysql-root-password,mysql-password,mysql-replication-password"
            namespace: "mysql"
            suggestedSource: "kubectl -n mysql create secret generic mysql-auth --from-literal=mysql-root-password=<value> --from-literal=mysql-password=<value> --from-literal=mysql-replication-password=<value>"
  render:
    command: "cub installer setup --pull packages/bitnami/mysql/14.0.3 --base generated-passwords --work-dir .tmp/confighub-proof/mysql-generated-passwords --non-interactive --namespace mysql"
    result: "pass"
    manifestCount: 8
    separatedSecretCount: 1
    facts:
      targetFactChecks:
        base: "generated-passwords"
        mode: "not-required"
        result: "pass"
      targetFacts:
        requiredSecrets:
          []
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/mysql-generated-passwords"
    result: "pass"
    manifestCount: 8
    separatedSecretCount: 1
  deterministicPackage:
    command: "cub installer package packages/bitnami/mysql/14.0.3 -o .tmp/confighub-proof/mysql-archives/mysql-a.tgz"
    sha256: "8534d63c67cbc647c8ab6dab1899dc008838e9cb679561a7c79d7083e5405890"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/mysql-generated-passwords"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/mysql-generated-passwords --space helm-mysql-confighub-proof --component MySQL --layer App --environment Demo --owner ConfigHubHelm --variant generated-passwords --unit-label Component=MySQL --unit-label HelmChart=bitnami-mysql --unit-label HelmChartVersion=14.0.3 --unit-label Variant=generated-passwords --unit-label Proof=mysql-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=/Users/alexis/.confighub/config.yaml"
    space: "helm-mysql-confighub-proof"
    unitCount: 9
    kubernetesUnitCount: 8
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      - "v1/Secret mysql/mysql"
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/mysql-generated-passwords"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/mysql-generated-passwords"
    result: "pass"
    summary: "No changes.\nImages in helm-mysql-confighub-proof (post-render):\n  init  StatefulSet/mysql [preserve-logs-symlinks] docker.io/bitnami/mysql:9.4.0-debian-12-r1\n      StatefulSet/mysql [mysql] docker.io/bitnami/mysql:9.4.0-debian-12-r1"
  serverSideVariant:
    command: "cub variant create staging helm-mysql-confighub-proof --environment Staging --region local --space-name-pattern template:{{.SourceEntitySlug}}-{{.Labels.Variant}} --allow-exists"
    result: "pass"
    upstreamSpace: "helm-mysql-confighub-proof"
    downstreamSpace: "helm-mysql-confighub-proof-staging"
    clonedUnitCount: 9
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "statefulset-mysql-mysql"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-mysql-confighub-proof"
      - "helm-mysql-confighub-proof-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup (/Users/alexis/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "rendered Secret resources are separated from ConfigHub Units and must be managed out-of-band."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
