apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "logstash-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.79"
  package:
    path: "packages/elastic/logstash/8.5.1"
    name: "elastic-logstash"
    chart: "elastic/logstash"
    chartVersion: "8.5.1"
    bases:
      - "default"
      - "ha"
    selectedBase: "ha"
    docVerified: true
    targetFactRequirements:
      []
  render:
    command: "cub installer setup --pull packages/elastic/logstash/8.5.1 --base ha --work-dir .tmp/confighub-proof/logstash-ha --non-interactive --namespace default"
    result: "pass"
    manifestCount: 4
    separatedSecretCount: 0
    facts:
      {}
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/logstash-ha"
    result: "pass"
    manifestCount: 4
    separatedSecretCount: 0
  deterministicPackage:
    command: "cub installer package packages/elastic/logstash/8.5.1 -o .tmp/confighub-proof/logstash-archives/logstash-a.tgz"
    sha256: "bc30315bed50eda7f5684ff7bb3bfa43daca0f9ebac8f0634f1ce0cdd311ff56"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/logstash-ha"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/logstash-ha --space helm-logstash-confighub-proof --component Logstash --layer App --environment Demo --owner ConfigHubHelm --variant ha --unit-label Component=Logstash --unit-label HelmChart=elastic-logstash --unit-label HelmChartVersion=8.5.1 --unit-label Variant=ha --unit-label Proof=logstash-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=/Users/alexis/.confighub/config.yaml"
    space: "helm-logstash-confighub-proof"
    unitCount: 5
    kubernetesUnitCount: 4
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      []
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/logstash-ha"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/logstash-ha"
    result: "pass"
    summary: "No changes.\nImages in helm-logstash-confighub-proof (post-render):\n      StatefulSet/logstash-logstash [logstash] docker.elastic.co/logstash/logstash:8.5.1"
  serverSideVariant:
    command: "cub variant create staging helm-logstash-confighub-proof --environment Staging --region local --space-name-pattern template:{{.Labels.Component}}-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-logstash-confighub-proof"
    downstreamSpace: "Logstash-staging"
    clonedUnitCount: 5
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "statefulset-logstash-logstash"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-logstash-confighub-proof"
      - "Logstash-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup (/Users/alexis/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
