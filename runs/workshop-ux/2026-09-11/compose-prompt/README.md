# Corrected copyable task regression

Two fresh gpt-5.6-luna agents received the exact corrected Compose assistant
task, with separate pinned plugin installations and fresh compose-ai directories.
They were given the task, so this is guided regression testing, not independent
navigation or evidence that users discover it. No completion-time claim is made.

Both saved the 184-object baseline, moved the complete workspace, retained the
replica-only candidate, preserved an unsupported v1beta1 refusal, then copied
the failed directory to recovered, restored only v1 and ran a new recovery
certification. Both saved the required reports. The reviewer confirmed refused
and recovered statuses, the unmodified failed API, and byte equality of the
recovered component with the intended pre-refusal candidate.

Both pass this exact local task. Their reports distinguish a fresh recovery
command from a previous successful receipt, even when deterministic output
hashes are identical. Both retain target prerequisites and make no deployment,
authority, readiness or health claim. There was no coaching during execution.

The source Guide hash, setup scope, reports, command logs, original/retained
manifests and complete archived output files are adjacent. The plugin source
is excluded except for the trial's compose-ai outputs. This closes the final
prompt-edit regression check; first-use and unassisted mixed-history acceptance
remain separate in #1897.
