# Refusal recovery retest with explicit CLI and report paths

Fresh participants M5-C/D used the homepage mission links and installed cub,
with explicit absolute report paths. Both preserved served-API refusals and
the provided handoff. M5-C again called the earlier good candidate recovery;
its log shows no fresh recovery check after refusal. That is incomplete,
correctness 0 for its unsupported recovery claim, despite correct static limits. M5-D certified and rendered a
separate good copy after the refusal and passes the bounded local recovery
mission, correctness 2 and evidence understanding 2. No coaching was supplied.

The repeat establishes that navigation wording alone did not resolve the
confusion. The inherited bundle included baseline and prior replica-change
receipts. A separate clean-baseline cohort isolates that setup difference;
do not overwrite these reports or attribute later success solely to the site.

Review also found that the Guide's human sequence includes recovery but the
copyable assistant task ends at refusal. Complete that task with an explicit
post-refusal copy, API restoration and new recovery receipt, and gate that
requirement separately. These trials predate that final task edit and do not
establish that the changed prompt itself passed participant testing.

Workspaces, readable receipts, reports, logs and transfer/file manifests are
retained, with local paths anonymized. Prior good receipts are not relabeled
as later recovery evidence. All claims are local/static; setup was supplied.
