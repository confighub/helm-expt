The default configuration is bitnami/redis 25.5.3, revision r001, rendered into 14 Kubernetes objects in namespace `redis` with release name `redis`. Pull the immutable installer package `oci://europe-west1-docker.pkg.dev/nth-fort-499605-q5/helm-expt/bitnami-redis:25.5.3@sha256:a216ce212424e05b341ef5000f1798e6014b72b8bc3dce9f315285871037af2a`. The default base needs no separate install prerequisite; the alternate reuse-existing-secret base needs Secret `redis/redis-existing-secret` key `redis-password`.

Settings come from the pinned chart/version, `recipes/bitnami/redis/25.5.3/effective-values.yaml`, namespace and release name, and capability profile `k8s-1.30-default` (digest `sha256:c1f8a4eb20154228a391f2a61565160634fbeb5dcf1079065543dd0a2ff3dfbf`). Later environment edits belong in ConfigHub Unit revision history or a derived ConfigHub variant; they should not be silently folded into the catalog base. The chart also performs lookups, capability branching, and generated credentials, so plain YAML flattening is unsafe.

Checked: package publication, chart contents, Helm render parity, supplied-values diagnostics, local Kubernetes run, GitOps/OCI delivery, Helm/ConfigHub live parity, and ConfigHub promotion. The catalog scan reports 4 advisory findings (2 critical and 2 warnings), including sensitive environment material and StatefulSet PVC orphaning risk. Untested/not checked: hooks/CRDs/setup are not applicable for this base, separate-cluster comparison is missing, upstream bytes behind the version string were not checked, and this exact default has no recorded destination acceptance or post-deployment health/rollback result. A fresh two-cluster run is the next evidence gap.

This helped me decide that the catalog gives a reproducible, inspectable starting point and render evidence, but not a production approval for my destination. Next, choose a target, check its APIs, storage class, policies, credentials, controllers and hardware facts, then record the target-specific result. If changing Helm inputs, make a new base variant; if changing fields for one environment after rendering, put the edit in ConfigHub Unit revision history/derived variant and review overlap before promotion.
