I found the Catalog entry for `bitnami/redis` version `25.5.3`, selected the `default` preset, and saved the complete lookup envelope in `record.json`. The Catalog SHA-256 is `3948d0a09658264a210c1fae4661977f0c93d4a3792ce1716483f04dad214dcd`; the selected base record SHA-256 is `2287c65b89f99be7b6d9ebfbc43a29edc415bd99b719920453a0d2ebd8f32c6b`. The record also preserves the revision digest `b72dbea967a115c140b00b124ac7facf66178d27383041e0938b11df75ab58d4`, canonical object-set digest `175caf404c4a005708398d2facd696a8500ef4280c47c682b7bae6273a91272e`, and immutable installer package digest `sha256:a216ce212424e05b341ef5000f1798e6014b72b8bc3dce9f315285871037af2a`.

The default configuration renders 14 Kubernetes objects in namespace `redis`, from chart values recorded in `recipes/bitnami/redis/25.5.3/effective-values.yaml`, using release name `redis`, Helm chart `bitnami/redis@25.5.3`, and Kubernetes 1.30 capability profile. The image is pinned to `registry-1.docker.io/bitnami/redis@sha256:6e7a020f1f6504698a7272c58783bdc2c23588c49febbae5aca1bb8dfa10af25`. The exact public installer package is the OCI reference recorded above.

The page recommends `reuse-existing-secret` for a safer starting point because the normal default writes password material into a rendered Secret. The default preset has no separately declared install-time prerequisite in its recorded scope; it has no hook or CRD route. Settings come from the chart's recorded Helm values and render context at build time. Catalog base edits are absent; later environment-specific changes belong in ConfigHub Unit revision history or a derived variant, with overlap reviewed.

Checked: source and render records are available; Helm render parity passed; ConfigHub scan and local-kind evidence are recorded; GitOps/live dual parity and variant promotion are recorded. The page reports 14 objects and the shared check result has 4 advisory findings (2 critical and 2 warnings), including direct sensitive environment storage and possible PVC orphaning during StatefulSet lifecycle operations. Untested for this exact default record: destination acceptance, post-deployment success, hooks/CRD readiness, target Secrets or cloud services, admission behavior, workload health, rollback, and two-cluster kind evidence. Historical support scope is constrained to the recorded vanilla-kind target, namespace `redis`, ConfigHub OCI delivery, and Argo CD.

This helped me decide that the default is a renderable, evidence-backed starting point, but needs a production suitability decision and target-specific live check before use. For a safer credential path, start from the recorded `reuse-existing-secret` base and supply `redis/redis-existing-secret` key `redis-password`. The next edit belongs in a new reviewed base variant when changing Helm values, or in a ConfigHub derived variant/Unit revision when changing an environment after rendering; do not edit the generated Catalog page or rendered YAML directly.

What did this help you decide and what next?

It helped decide which exact immutable package and preset to inspect, what the render guarantees, and which operational questions remain open. Next, review the full record and rendered objects, choose the credential strategy, declare the target's storage and other facts, then run the exact destination and post-deployment checks for the intended environment.
