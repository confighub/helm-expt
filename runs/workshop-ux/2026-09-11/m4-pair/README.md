# Match mission results and handoff limitation

Two fresh gpt-5.6-luna agents used supplied pinned plugin checkouts and the
locked local candidate. Both produced candidate, mismatch and unknown receipts
for two GPUs, one GPU and absent GPU facts respectively. Both describe the
snapshot as supplied evidence, with liveChecked false and no scheduling,
free-capacity or inference proof. Neither received coaching.

M4-A used the plugin entry point directly. An initial path error was followed
by successful execution. Its first attempted missing-fact edit accidentally
kept the GPU value and therefore returned candidate. It preserved that receipt
as unknown.json, corrected the copied input and saved unknown-final.json with
status unknown and exit 3. Original facts remain available and match the
intermediate candidate's input identity. This is recoverable participant
friction, not permission to relabel the earlier receipt.

M4-B used the three documented cases and saved their receipts and reports
under trial/. The reports are present in the retained workspace archive, but
not at the explicitly required trial-root result.md/trial-log.md paths.
The external root-file observer therefore has no completed handoff for M4-B.
Do not move its reports and call the original handoff compliant.

The core comparisons and evidence explanations are correct for both. M4-A
passes the bounded supplied-facts mission through the direct plugin entry
point; M4-B has a noncompliant handoff (correctness 1, evidence understanding 2).
This is not two full `cub` Guide passes. A further unassisted handoff test is
needed before declaring that path complete. Setup was supplied and participant
logs do not replace an external tool transcript.

Adjacent manifests hash original and retained outputs; archives include all
inputs, receipts, errors and reports, excluding CLI config and plugin source.
Absolute local prefixes are anonymized. Earlier intermediate failures remain.
No live fleet, browser, human or hosted API result is claimed.
