# Adapt through the installed CLI

Fresh participant M3-C used the homepage mission link, the supplied pinned
plugin and installed cub. The brief explicitly required that CLI entry point
and the two absolute report paths, so this is separate from the earlier pair.
It preserved the original input, saved the one-field replica change, moved the
complete inputs, reproduced the comparison and detected an extra
revisionHistoryLimit change in another candidate. Ordinary comparison exited
0; the extra-change comparison with --exit-code exited 1.

The reports correctly distinguish comparison from approval, schema validity
or deployment. The mission passes with correctness 2 and evidence understanding
2, no coaching, and the required root handoff. M3-B also exercised installed cub
in the earlier cohort; do not pool those as identical briefs or claim that all
participants followed every Guide step.

Retained receipts, inputs, logs and complete output archive are accompanied by
original/retained hashes. Setup was supplied. This is HTTP/CLI, not browser,
human, hosted API or live-target evidence.
