# Refusal and recovery pair

Two fresh gpt-5.6-luna agents received complete hashed workspaces from the
passing Compose pair, the locked homepage and an isolated pinned plugin.
The mission required a served-API refusal, preserved failed attempt and
recovery in a separate copy. No coaching was supplied.

M5-A kept an unsupported v1beta1 attempt and refusal, then certified and
rendered a separate copy of the known-good v1 input. The unchanged original
and failed attempt remain. This is local recovery by returning to the retained
good input, rather than editing the failed copy. Its reports correctly limit
the result to static certification. Correctness 2, evidence understanding 2,
help 2. This does not demonstrate live rollback or data safety.

M5-B preserved a valid workspace and a later incompatible refusal, but its
recorded commands do not show recovery after that refusal. It calls the earlier
good copy recovery and reports success. Keep this as an incomplete mission,
correctness 0 for the unsupported completion claim; having a prior good result is not evidence that recovery was
performed. It used the plugin entry point directly. No target/runtime claim
was made, and the original handoff remained intact.

The Guide contains a concrete post-refusal separate-copy recovery sequence,
but the homepage did not directly route this mission to it. Add a direct
mission link and retest with fresh participants. Do not correct the original
participant report or reuse a prior pass as a later recovery receipt.

Adjacent archives preserve workspaces, readable receipts, reports, transfer
manifests and original/retained hashes. Local paths and home prefixes are
anonymized; archive paths in reports are rendered as text. Participant logs
are accounts, not complete external execution transcripts. Setup was supplied;
no browser, human or live API result is established.
