# Match through cub with complete handoffs

Two fresh participants used the new homepage mission links, the pinned plugin
and installed cub. The brief now explicitly required that entry point and the
absolute result.md/trial-log.md paths. Both saved the reports correctly and
preserved candidate/mismatch/unknown receipts with exits 0/1/3. Both retained
original workload and Node facts and separate altered copies. Both explanations
distinguish sufficient supplied facts, insufficient facts and missing facts,
without claiming scheduling, free GPUs or inference.

Both pass the bounded mission: correctness 2, evidence understanding 2 and help
2. M4-C encountered overwrite protection on a repeated output name and retained
a fresh result; this is recoverable friction. M4-D reported no such error.
Neither was coached. Do not pool this changed brief with the earlier pair,
whose entry-point and handoff limitations remain recorded.

The reviewer inspected all three statuses, source hashes and live-check limits.
Setup was supplied and not a first-use installation trial. Adjacent receipts,
reports, original/retained manifests and compressed workspaces retain the
evidence. Browser, human and hosted API results remain separate.
