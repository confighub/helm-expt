# Compose routing retest

Two fresh gpt-5.6-luna agents, low reasoning, received the same Compose mission
as the [first pair](../m2-first-pair/README.md), with a new locked local site
candidate and separately installed pinned plugins. Both found the GitOps shop
Guide, saved `kubara-gitops-shop` with 184 objects, and explained target
prerequisites and untested reconciliation/application health. Both pass this
local mission: correctness 2, evidence understanding 2, help 2 and mission
understanding 2. There was no coaching. Setup itself was supplied, not tested.

M2-D first tried an unavailable plugin-local CLI path, then recovered using the
installed CLI. Record that as recoverable command-path friction. M2-C reported
no blocking setup issue. Both followed the additional Guide steps: replica
edit, incompatible API refusal and separate recovery. Those receipts supplement
the Compose result; they are not independent refusal-mission participants.

The reviewer checked both 184-object baseline and resumed receipts, unchanged
baseline renders, retained `certified: false` refusals, unsupported v1beta1 in
the failed copy, and successful v1 recovery in a different copy. Baseline render
SHA-256 is `e9d86c644b65a8fca0936415cfb4bc16caa43db5e894b6694746abe1fff8a4a6`
in both trials. Target availability remains not-checked. Plugin tracked inputs
remained unchanged. A same-agent move is not independent session continuation.

Final required reports were first externally observed 117.3 seconds after
dispatch for M2-C and 81.9 seconds for M2-D, and then validated. These include
the extra Guide steps and polling overhead. They are not first-use setup or
population performance benchmarks.

The candidate revision, model, dispatch and external observations are retained.
Each trial contains readable receipts and reports, a complete compressed
workspace excluding plugin/config, and original/retained file hashes. Absolute
trial paths were made relative and home prefixes anonymized. No failed result
was corrected into success. Browser/human/live API testing remains separate.
