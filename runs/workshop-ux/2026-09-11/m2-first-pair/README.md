# First Compose mission pair

Two fresh gpt-5.6-luna agents, low reasoning, received the homepage and this
mission: build a local platform with GitOps services and a shop app using the
retained Kubara selection; preserve the materialized configuration and explain
real-target requirements. Each had an isolated, pinned plugin supplied. This
was an HTTP/CLI trial against candidate `16c90e0942272a4cf31f75570eb1f513b3f5eee3`.

Both chose the platform page and followed the 135-object `kubara-shop-platform`
example. Its certification is valid, but that selection omits the requested
GitOps services. The existing Compose Guide selects `kubara-gitops-shop` with
184 objects. Neither participant completed the requested mission. Both instead
continued into the replica-change and API-refusal demonstrations.

Both saved baseline, candidate and refusal artifacts, correctly named target
and application-health limits, and needed no coaching. Score correctness 0
for the wrong requested composition, evidence understanding 2 and help 2.
Their next steps require adding GitOps wiring rather than recognizing the
retained GitOps selection. Do not count these as Compose passes or as complete
refusal/recovery trials: they did not recover in a new copy.

The reviewer inspected both `platform/result.json` files (certified true,
135 objects), output paths and reports; plugin tracked files remained unchanged.
Retained reports describe static ordering checks, not observed application or
controller behavior. No new live evidence is established.

The first executable route on the platform page did not identify the
GitOps-plus-app choice. Replace its generic local-platform notice with an
explicit route naming that mission and the retained selection. Keep the valid
135-object example intact. Hold further mission expansion until a fresh pair
uses the requested selection successfully.

Each trial directory includes the original reports, readable JSON receipts,
and a compressed complete workspace, excluding plugin checkout and CLI config.
Manifests hash original and retained files. Absolute trial prefixes in text
were made relative; rendered content is otherwise retained. Dispatch and
external file observations are adjacent. These failed handoff arrivals are not
successful completion times. Setup is excluded and no population claim is made.
