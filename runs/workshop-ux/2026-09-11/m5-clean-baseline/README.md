# Refusal and recovery from a clean supplied baseline

Fresh participants M5-E/F received newly materialized 184-object baselines
without the earlier replica-change candidate or its receipts. The homepage,
installed-CLI and report requirements matched M5-C/D. This isolates a setup
difference; it is not the same inherited-history cohort.

Both preserved an unsupported v1beta1 attempt with an exit-1 refusal, then
certified a separate v1 recovery copy and rendered it successfully. Both kept
the original baseline and failed attempt. M5-E returned to a separate known-good
copy; M5-F added a separate test ExternalSecret, increasing its trial to 185
objects, and restored that object's API in a recovery copy. This differs from
the Guide's edit of the existing shop resource, while still exercising the
stated unsupported-API mission. Both describe local composition
only, not live rollback, data safety, target availability or health. Both pass:
correctness 2, evidence understanding 2 and help 2. No coaching was supplied.

This supports the clean-baseline local task. It does not erase the mixed-history
failures, prove navigation alone caused improvement, or validate the later
assistant-task text change. The decisive refusal finding is buried in a large
JSON result; M5-E recorded that as friction. A concise refusal summary remains
a useful improvement to evaluate separately.

Complete compressed workspaces, readable receipts/reports, setup, dispatch,
external observations and original/retained file hashes are adjacent. CLI
config, plugin checkout and extra plugin-copy directories are excluded from
archives; the stack refers only to retained component files. Local prefixes are
anonymized. Setup was provided, with baseline creation outside the trial clock.
