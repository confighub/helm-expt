apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "logstash-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.80"
  package:
    path: "packages/elastic/logstash/8.5.1"
    name: "elastic-logstash"
    chart: "elastic/logstash"
    chartVersion: "8.5.1"
    bases:
      - "default"
      - "ha"
    selectedBase: "default"
    docVerified: true
    targetFactRequirements:
      -
        base: "ha"
        requirements:
          -
            kind: "ClusterFeature"
            name: "minimum schedulable nodes 3"
            namespace: ""
            suggestedSource: "use a target with at least 3 schedulable nodes before applying this base"
  render:
    command: "cub installer setup --pull packages/elastic/logstash/8.5.1 --base default --work-dir .tmp/confighub-proof/logstash-default --non-interactive --namespace default"
    result: "pass"
    manifestCount: 4
    separatedSecretCount: 0
    facts:
      targetFactChecks:
        base: "default"
        mode: "not-required"
        result: "pass"
      targetFacts:
        requiredCRDs:
          []
        requiredObjectStores:
          []
        requiredSecrets:
          []
        requiredTopology: null
        requiredValues:
          []
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/logstash-default"
    result: "pass"
    manifestCount: 4
    separatedSecretCount: 0
  deterministicPackage:
    command: "cub installer package packages/elastic/logstash/8.5.1 -o .tmp/confighub-proof/logstash-default-archives/logstash-a.tgz"
    sha256: "83975cfa57b676362b21dc888072d70ae569ab4503ca2e72d170d8e5a19e2bd3"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/logstash-default"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/logstash-default --space helm-logstash-default-confighub-proof --component Logstash --layer App --environment Demo --owner ConfigHubHelm --variant default --unit-label Component=Logstash --unit-label HelmChart=elastic-logstash --unit-label HelmChartVersion=8.5.1 --unit-label Variant=default --unit-label Proof=logstash-default-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=/Users/alexis/.confighub/config.yaml"
    space: "helm-logstash-default-confighub-proof"
    unitCount: 5
    kubernetesUnitCount: 4
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      []
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/logstash-default"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/logstash-default"
    result: "pass"
    summary: "No changes.\nImages in helm-logstash-default-confighub-proof (post-render):\n      StatefulSet/logstash-logstash [logstash] docker.elastic.co/logstash/logstash:8.5.1"
  serverSideVariant:
    command: "cub variant create staging helm-logstash-default-confighub-proof --environment Staging --region local --namespace default --space-pattern template:Logstash-default-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-logstash-default-confighub-proof"
    downstreamSpace: "Logstash-default-staging"
    clonedUnitCount: 5
    namespace: "default"
  serverSidePromotion:
    receipt: "variant-promotion-receipt.yaml"
    result: "pass"
    reason: "server-side promotion passed through a changeset"
    changedUnitCaughtUp: "pass"
    addedUnitCloned: "pass"
    deletionHandling: "not-tested"
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "statefulset-logstash-logstash"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-logstash-default-confighub-proof"
      - "Logstash-default-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup (/Users/alexis/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
