apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "trivy-operator-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.79"
  package:
    path: "packages/aqua/trivy-operator/0.32.1"
    name: "aqua-trivy-operator"
    chart: "aqua/trivy-operator"
    chartVersion: "0.32.1"
    bases:
      - "default"
      - "no-crds"
    selectedBase: "default"
    docVerified: true
    targetFactRequirements:
      []
  render:
    command: "cub installer setup --pull packages/aqua/trivy-operator/0.32.1 --base default --work-dir .tmp/confighub-proof/trivy-operator-default --non-interactive --namespace default"
    result: "pass"
    manifestCount: 32
    separatedSecretCount: 2
    facts:
      {}
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/trivy-operator-default"
    result: "pass"
    manifestCount: 32
    separatedSecretCount: 2
  deterministicPackage:
    command: "cub installer package packages/aqua/trivy-operator/0.32.1 -o .tmp/confighub-proof/trivy-operator-archives/trivy-operator-a.tgz"
    sha256: "debcc8623ee9a61303bdb7df297c62f46ba17dc85355b7d35f0bd5257e000ef5"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/trivy-operator-default"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/trivy-operator-default --space helm-trivy-operator-confighub-proof --component TrivyOperator --layer App --environment Demo --owner ConfigHubHelm --variant default --unit-label Component=TrivyOperator --unit-label HelmChart=aqua-trivy-operator --unit-label HelmChartVersion=0.32.1 --unit-label Variant=default --unit-label Proof=trivy-operator-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=/Users/alexis/.confighub/config.yaml"
    space: "helm-trivy-operator-confighub-proof"
    unitCount: 33
    kubernetesUnitCount: 32
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      - "v1/Secret default/trivy-operator-trivy-config"
      - "v1/Secret default/trivy-operator"
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/trivy-operator-default"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/trivy-operator-default"
    result: "pass"
    summary: "No changes.\nImages in helm-trivy-operator-confighub-proof (post-render):\n      Deployment/trivy-operator [trivy-operator] mirror.gcr.io/aquasec/trivy-operator:0.30.1"
  serverSideVariant:
    command: "cub variant create staging helm-trivy-operator-confighub-proof --environment Staging --region local --space-name-pattern template:{{.Labels.Component}}-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-trivy-operator-confighub-proof"
    downstreamSpace: "TrivyOperator-staging"
    clonedUnitCount: 33
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "deployment-default-trivy-operator"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-trivy-operator-confighub-proof"
      - "TrivyOperator-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup (/Users/alexis/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "rendered Secret resources are separated from ConfigHub Units and must be managed out-of-band."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
