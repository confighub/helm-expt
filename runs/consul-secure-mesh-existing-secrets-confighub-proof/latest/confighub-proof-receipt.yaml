apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "consul-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.79"
  package:
    path: "packages/hashicorp/consul/2.0.0"
    name: "hashicorp-consul"
    chart: "hashicorp/consul"
    chartVersion: "2.0.0"
    bases:
      - "default-control-plane"
      - "secure-mesh-existing-secrets"
    selectedBase: "secure-mesh-existing-secrets"
    docVerified: true
    targetFactRequirements:
      -
        base: "secure-mesh-existing-secrets"
        requirements:
          -
            kind: "ClusterFeature"
            name: "Secret consul/consul-ca-cert key tls.crt"
            namespace: "consul"
            suggestedSource: "kubectl -n consul create secret generic consul-ca-cert --from-literal=tls.crt=<value>"
          -
            kind: "ClusterFeature"
            name: "Secret consul/consul-server-cert keys tls.crt,tls.key"
            namespace: "consul"
            suggestedSource: "kubectl -n consul create secret generic consul-server-cert --from-literal=tls.crt=<value> --from-literal=tls.key=<value>"
          -
            kind: "ClusterFeature"
            name: "Secret consul/consul-gossip-encryption-key key key"
            namespace: "consul"
            suggestedSource: "kubectl -n consul create secret generic consul-gossip-encryption-key --from-literal=key=<value>"
          -
            kind: "ClusterFeature"
            name: "Secret consul/consul-bootstrap-acl-token key token"
            namespace: "consul"
            suggestedSource: "kubectl -n consul create secret generic consul-bootstrap-acl-token --from-literal=token=<value>"
  render:
    command: "cub installer setup --pull packages/hashicorp/consul/2.0.0 --base secure-mesh-existing-secrets --work-dir .tmp/confighub-proof/consul-secure-mesh-existing-secrets --non-interactive --namespace consul"
    result: "pass"
    manifestCount: 97
    separatedSecretCount: 1
    facts:
      targetFactChecks:
        base: "secure-mesh-existing-secrets"
        mode: "record"
        result: "recorded"
      targetFacts:
        requiredCRDs:
          []
        requiredObjectStores:
          []
        requiredSecrets:
          -
            keys:
              - "tls.crt"
            name: "consul-ca-cert"
            namespace: "consul"
            purpose: "Consul TLS CA certificate"
          -
            keys:
              - "tls.crt"
              - "tls.key"
            name: "consul-server-cert"
            namespace: "consul"
            purpose: "Consul server TLS certificate and private key"
          -
            keys:
              - "key"
            name: "consul-gossip-encryption-key"
            namespace: "consul"
            purpose: "Consul gossip encryption key"
          -
            keys:
              - "token"
            name: "consul-bootstrap-acl-token"
            namespace: "consul"
            purpose: "Consul ACL bootstrap token"
        requiredTopology:
          deliveryLanes:
            - "regularHelm"
            - "cubInstallerApply"
          minimumSchedulableNodes: 3
          purpose: "Consul secure mesh renders three server replicas with anti-affinity and gateway workloads."
        requiredValues:
          []
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/consul-secure-mesh-existing-secrets"
    result: "pass"
    manifestCount: 97
    separatedSecretCount: 1
  deterministicPackage:
    command: "cub installer package packages/hashicorp/consul/2.0.0 -o .tmp/confighub-proof/consul-secure-mesh-existing-secrets-archives/consul-a.tgz"
    sha256: "5f2c814cd8a5b6feff3703036391d7fea34147ad9c17123756e98fde28beca2a"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/consul-secure-mesh-existing-secrets"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/consul-secure-mesh-existing-secrets --space helm-consul-secure-mesh-existing-secrets-confighub-proof --component Consul --layer App --environment Demo --owner ConfigHubHelm --variant secure-mesh-existing-secrets --unit-label Component=Consul --unit-label HelmChart=hashicorp-consul --unit-label HelmChartVersion=2.0.0 --unit-label Variant=secure-mesh-existing-secrets --unit-label Proof=consul-secure-mesh-existing-secrets-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=/Users/alexis/.confighub/config.yaml"
    space: "helm-consul-secure-mesh-existing-secrets-confighub-proof"
    unitCount: 98
    kubernetesUnitCount: 97
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      - "v1/Secret consul/consul-consul-auth-method"
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/consul-secure-mesh-existing-secrets"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/consul-secure-mesh-existing-secrets"
    result: "timeout"
    summary: ""
  serverSideVariant:
    command: "cub variant create staging helm-consul-secure-mesh-existing-secrets-confighub-proof --environment Staging --region local --space-name-pattern template:Consul-secure-mesh-existing-secrets-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-consul-secure-mesh-existing-secrets-confighub-proof"
    downstreamSpace: "Consul-secure-mesh-existing-secrets-staging"
    clonedUnitCount: 98
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "statefulset-consul-consul-consul-server"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-consul-secure-mesh-existing-secrets-confighub-proof"
      - "Consul-secure-mesh-existing-secrets-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup (/Users/alexis/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "post-upload cub installer plan timed out for this base-specific proof run; upload, variant, scan, and safe-ops were still checked."
    - "rendered Secret resources are separated from ConfigHub Units and must be managed out-of-band."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
