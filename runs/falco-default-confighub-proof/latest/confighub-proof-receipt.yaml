apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "falco-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.80"
  package:
    path: "packages/falcosecurity/falco/9.0.0"
    name: "falcosecurity-falco"
    chart: "falcosecurity/falco"
    chartVersion: "9.0.0"
    bases:
      - "default"
    selectedBase: "default"
    docVerified: true
    targetFactRequirements:
      []
  render:
    command: "cub installer setup --pull packages/falcosecurity/falco/9.0.0 --base default --work-dir .tmp/confighub-proof/falco-default --non-interactive --namespace default"
    result: "pass"
    manifestCount: 7
    separatedSecretCount: 0
    facts:
      {}
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/falco-default"
    result: "pass"
    manifestCount: 7
    separatedSecretCount: 0
  deterministicPackage:
    command: "cub installer package packages/falcosecurity/falco/9.0.0 -o .tmp/confighub-proof/falco-default-archives/falco-a.tgz"
    sha256: "a519fcb3bb6edf83336ad98da88745645597448bd6514358b40993ba4eb6ee9b"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/falco-default"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/falco-default --space helm-falco-default-confighub-proof --component Falco --layer App --environment Demo --owner ConfigHubHelm --variant default --unit-label Component=Falco --unit-label HelmChart=falcosecurity-falco --unit-label HelmChartVersion=9.0.0 --unit-label Variant=default --unit-label Proof=falco-default-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=$HOME/.confighub/config.yaml"
    space: "helm-falco-default-confighub-proof"
    unitCount: 8
    kubernetesUnitCount: 7
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      []
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/falco-default"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/falco-default"
    result: "pass"
    summary: "No changes.\nImages in helm-falco-default-confighub-proof (post-render):\n  init  DaemonSet/falco [falco-driver-loader] docker.io/falcosecurity/falco-driver-loader:0.44.0\n  init  DaemonSet/falco [falcoctl-artifact-install] docker.io/falcosecurity/falcoctl:0.13.0\n      DaemonSet/falco [falco] docker.io/falcosecurity/falco:0.44.0\n      DaemonSet/falco [falcoctl-artifact-follow] docker.io/falcosecurity/falcoctl:0.13.0"
  serverSideVariant:
    command: "cub variant create staging helm-falco-default-confighub-proof --environment Staging --region local --namespace default --space-pattern template:Falco-default-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-falco-default-confighub-proof"
    downstreamSpace: "Falco-default-staging"
    clonedUnitCount: 8
    namespace: "default"
  serverSidePromotion:
    receipt: "variant-promotion-receipt.yaml"
    result: "pass"
    reason: "server-side promotion passed through a changeset"
    changedUnitCaughtUp: "pass"
    addedUnitCloned: "pass"
    deletionHandling: "not-tested"
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "daemonset-default-falco"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-falco-default-confighub-proof"
      - "Falco-default-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup ($HOME/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
