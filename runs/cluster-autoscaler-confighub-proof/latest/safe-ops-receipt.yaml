apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubSafeOpsReceipt"
metadata:
  name: "cluster-autoscaler-safe-ops-20260527"
spec:
  observedAt: "2026-05-27"
  context:
    organization: "Kubara"
    server: "https://hub.confighub.com"
    space: "helm-cluster-autoscaler-confighub-proof"
    selector: "Labels.Proof = 'cluster-autoscaler-confighub-proof'"
  subject:
    chart: "autoscaler/cluster-autoscaler"
    chartVersion: "9.57.0"
    packagePath: "packages/autoscaler/cluster-autoscaler/9.57.0"
    variant: "controller-default-reviewed"
  changeset:
    slug: "cluster-autoscaler-safe-ops-20260527"
    createResult: "pass"
    updateResult: "pass"
    description: "Cluster Autoscaler safe-ops proof: approve reviewed revisions, dry-run apply only"
    labels:
      Proof: "cluster-autoscaler-confighub-proof"
      Lane: "safe-ops"
    annotations:
      proof.confighub.com/scope: "local-test"
      proof.confighub.com/live-apply: "false"
  approval:
    command: "cub unit approve service-default-cluster-autoscaler-aws-cluster-autoscaler --space helm-cluster-autoscaler-confighub-proof --revision HeadRevisionNum --verbose --wait"
    result: "pass"
    representativeUnit: "service-default-cluster-autoscaler-aws-cluster-autoscaler"
    output: "Unit service-default-cluster-autoscaler-aws-cluster-autoscaler (a649c8a5-768d-4285-bfeb-8bad60d9d9b0) has been approved\nAwaiting triggers..."
  applyDryRun:
    command: "cub unit apply --space helm-cluster-autoscaler-confighub-proof --where \"Labels.Proof = 'cluster-autoscaler-confighub-proof'\" --dry-run --wait --timeout 2m"
    result: "blocked-no-target"
    expected: true
    message: "Failed: HTTP 422 for req TqKhambYkZIElQDdlvZToWPORxrNVQuv: cannot invoke action on a unit without a target\nDetails:\n  failed to apply entity"
  cancel:
    command: "cub unit cancel --space helm-cluster-autoscaler-confighub-proof --where \"Labels.Proof = 'cluster-autoscaler-confighub-proof'\""
    result: "pass"
    message: "No units found matching the filter"
  safetyResult: "pass"
  interpretation: "The reviewed Units are visible in ConfigHub, but without a target ConfigHub makes no live deployment claim and blocks apply at the operation boundary."
