apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "terraform-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.79"
  package:
    path: "packages/hashicorp/terraform/1.1.2"
    name: "hashicorp-terraform"
    chart: "hashicorp/terraform"
    chartVersion: "1.1.2"
    bases:
      - "default"
      - "no-crds"
    selectedBase: "no-crds"
    docVerified: true
    targetFactRequirements:
      []
  render:
    command: "cub installer setup --pull packages/hashicorp/terraform/1.1.2 --base no-crds --work-dir .tmp/confighub-proof/terraform-no-crds --non-interactive --namespace default"
    result: "pass"
    manifestCount: 5
    separatedSecretCount: 0
    facts:
      {}
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/terraform-no-crds"
    result: "pass"
    manifestCount: 5
    separatedSecretCount: 0
  deterministicPackage:
    command: "cub installer package packages/hashicorp/terraform/1.1.2 -o .tmp/confighub-proof/terraform-archives/terraform-a.tgz"
    sha256: "737667648f89276c77a2268dd67e52b33ed27b88add655c7aea2232f12ab8ef0"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/terraform-no-crds"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/terraform-no-crds --space helm-terraform-confighub-proof --component Terraform --layer App --environment Demo --owner ConfigHubHelm --variant no-crds --unit-label Component=Terraform --unit-label HelmChart=hashicorp-terraform --unit-label HelmChartVersion=1.1.2 --unit-label Variant=no-crds --unit-label Proof=terraform-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=$HOME/.confighub/config.yaml"
    space: "helm-terraform-confighub-proof"
    unitCount: 6
    kubernetesUnitCount: 5
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      []
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/terraform-no-crds"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/terraform-no-crds"
    result: "pass"
    summary: "No changes.\nImages in helm-terraform-confighub-proof (post-render):\n      Deployment/terraform-terraform-sync-workspace [terraform-sync-workspace] hashicorp/terraform-k8s:1.1.2"
  serverSideVariant:
    command: "cub variant create staging helm-terraform-confighub-proof --environment Staging --region local --space-name-pattern template:{{.Labels.Component}}-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-terraform-confighub-proof"
    downstreamSpace: "Terraform-staging"
    clonedUnitCount: 6
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "deployment-default-terraform-terraform-sync-workspace"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-terraform-confighub-proof"
      - "Terraform-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup ($HOME/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
