apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "external-dns-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.79"
  package:
    path: "packages/external-dns/external-dns/1.21.1"
    name: "external-dns-external-dns"
    chart: "external-dns/external-dns"
    chartVersion: "1.21.1"
    bases:
      - "default"
      - "no-crds"
      - "dry-run-txt-registry"
    selectedBase: "dry-run-txt-registry"
    docVerified: true
    targetFactRequirements:
      []
  render:
    command: "cub installer setup --pull packages/external-dns/external-dns/1.21.1 --base dry-run-txt-registry --work-dir .tmp/confighub-proof/external-dns-dry-run-txt-registry --non-interactive --namespace external-dns"
    result: "pass"
    manifestCount: 5
    separatedSecretCount: 0
    facts:
      {}
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/external-dns-dry-run-txt-registry"
    result: "pass"
    manifestCount: 5
    separatedSecretCount: 0
  deterministicPackage:
    command: "cub installer package packages/external-dns/external-dns/1.21.1 -o .tmp/confighub-proof/external-dns-archives/external-dns-a.tgz"
    sha256: "e988e84b50eb145af6d13f9159c6fb77a02e020b6f55ec6b847e82ad75362a7f"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/external-dns-dry-run-txt-registry"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/external-dns-dry-run-txt-registry --space helm-external-dns-confighub-proof --component ExternalDNS --layer App --environment Demo --owner ConfigHubHelm --variant dry-run-txt-registry --unit-label Component=ExternalDNS --unit-label HelmChart=external-dns-external-dns --unit-label HelmChartVersion=1.21.1 --unit-label Variant=dry-run-txt-registry --unit-label Proof=external-dns-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=/Users/alexis/.confighub/config.yaml"
    space: "helm-external-dns-confighub-proof"
    unitCount: 6
    kubernetesUnitCount: 5
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      []
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/external-dns-dry-run-txt-registry"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/external-dns-dry-run-txt-registry"
    result: "pass"
    summary: "No changes.\nImages in helm-external-dns-confighub-proof (post-render):\n      Deployment/external-dns [external-dns] registry.k8s.io/external-dns/external-dns:v0.21.0"
  serverSideVariant:
    command: "cub variant create staging helm-external-dns-confighub-proof --environment Staging --region local --space-name-pattern template:{{.Labels.Component}}-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-external-dns-confighub-proof"
    downstreamSpace: "ExternalDNS-staging"
    clonedUnitCount: 6
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "deployment-default-external-dns"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-external-dns-confighub-proof"
      - "ExternalDNS-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup (/Users/alexis/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
