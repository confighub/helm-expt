apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ConfigHubProofReceipt"
metadata:
  name: "goldilocks-confighub-proof-20260527"
spec:
  run:
    date: "2026-05-27"
    context:
      organization: "Kubara"
      server: "https://hub.confighub.com"
      cubClientVersion: "Version:    dev"
      cubServerVersion: "v0.1.80"
  package:
    path: "packages/fairwinds-stable/goldilocks/10.3.0"
    name: "fairwinds-stable-goldilocks"
    chart: "fairwinds-stable/goldilocks"
    chartVersion: "10.3.0"
    bases:
      - "default"
    selectedBase: "default"
    docVerified: true
    targetFactRequirements:
      []
  render:
    command: "cub installer setup --pull packages/fairwinds-stable/goldilocks/10.3.0 --base default --work-dir .tmp/confighub-proof/goldilocks-default --non-interactive --namespace default"
    result: "pass"
    manifestCount: 10
    separatedSecretCount: 0
    facts:
      {}
  rerender:
    command: "cub installer render --work-dir .tmp/confighub-proof/goldilocks-default"
    result: "pass"
    manifestCount: 10
    separatedSecretCount: 0
  deterministicPackage:
    command: "cub installer package packages/fairwinds-stable/goldilocks/10.3.0 -o .tmp/confighub-proof/goldilocks-default-archives/goldilocks-a.tgz"
    sha256: "1ccb876ce49d490494141334c674ec6da4f46accbea5f1bdda5f59a20454b68a"
    byteIdenticalAcrossTwoLocalBundles: true
  vet:
    command: "cub installer vet --work-dir .tmp/confighub-proof/goldilocks-default"
    result: "pass"
    note: "Package declares no validators (spec.validators is empty)."
  upload:
    command: "cub installer upload --work-dir .tmp/confighub-proof/goldilocks-default --space helm-goldilocks-default-confighub-proof --component Goldilocks --layer App --environment Demo --owner ConfigHubHelm --variant default --unit-label Component=Goldilocks --unit-label HelmChart=fairwinds-stable-goldilocks --unit-label HelmChartVersion=10.3.0 --unit-label Variant=default --unit-label Proof=goldilocks-default-confighub-proof --retry"
    result: "pass"
    workaround: "CUB_CONFIG=$HOME/.confighub/config.yaml"
    space: "helm-goldilocks-default-confighub-proof"
    unitCount: 11
    kubernetesUnitCount: 10
    installerRecordUnitCount: 1
    separatedSecretsNotUploaded:
      []
  plan:
    preUpload:
      command: "cub installer plan --work-dir .tmp/confighub-proof/goldilocks-default"
      result: "expected-missing-upload-state"
    command: "cub installer plan --work-dir .tmp/confighub-proof/goldilocks-default"
    result: "pass"
    summary: "No changes.\nImages in helm-goldilocks-default-confighub-proof (post-render):\n      Deployment/goldilocks-controller [goldilocks] us-docker.pkg.dev/fairwinds-ops/oss/goldilocks:v4.14.1\n      Deployment/goldilocks-dashboard [goldilocks] us-docker.pkg.dev/fairwinds-ops/oss/goldilocks:v4.14.1"
  serverSideVariant:
    command: "cub variant create staging helm-goldilocks-default-confighub-proof --environment Staging --region local --namespace default --space-pattern template:Goldilocks-default-{{.Labels.Variant}} --allow-exists --wait --timeout 10m"
    result: "pass"
    upstreamSpace: "helm-goldilocks-default-confighub-proof"
    downstreamSpace: "Goldilocks-default-staging"
    clonedUnitCount: 11
    namespace: "default"
  serverSidePromotion:
    receipt: "variant-promotion-receipt.yaml"
    result: "pass"
    reason: "server-side promotion passed through a changeset"
    changedUnitCaughtUp: "pass"
    addedUnitCloned: "pass"
    deletionHandling: "not-tested"
  review:
    unitList: "pass"
    unitData: "pass"
    revisionList: "pass"
    revisionDiff: "pass"
    representativeUnit: "deployment-default-goldilocks-controller"
  retention:
    liveSpacesDeletedAfterProof: true
    reason: "Kubara demo org has a finite Link quota; receipts and command logs preserve the proof after the live spaces are removed."
    spaces:
      - "helm-goldilocks-default-confighub-proof"
      - "Goldilocks-default-staging"
  observedFriction:
    - "cub installer upload needs explicit CUB_CONFIG in this local setup ($HOME/.confighub/config.yaml)."
    - "pre-upload cub installer plan is expected to fail until upload state exists."
    - "dry-run apply is blocked until a target is attached, which is the correct safe boundary."
