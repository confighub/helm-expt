apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ChartDossier"
metadata:
  name: "linkerd-linkerd-crds-1-8-0"
  labels:
    confighub.io/chart-ref: "linkerd/linkerd-crds"
    confighub.io/chart-version: "1.8.0"
    confighub.io/proof-tier: "next80-full"
spec:
  chart: "linkerd/linkerd-crds"
  version: "1.8.0"
  maintainedNotes:
    - "linkerd/linkerd-crds@1.8.0 renders deterministically under Kubernetes 1.30.0 with hooks and tests excluded."
    - "The default variant is intended as a proof baseline; production promotion still follows scan/gate review."
    - "Chart source contains generated/random/time helpers; generated values must be captured before approval if activated by a variant."
    - "Rendered output includes 8 CRDs; CRD lifecycle must be reviewed before upgrade/promotion."
  knownControlPoints:
    - "source-lock"
    - "dependency-lock"
    - "capability-profile"
    - "variant-revision"
    - "rendered-manifest-scan"
    - "helm-equivalence"
    - "generated-facts"
    - "crds"
  proofFocus: "service mesh CRD package"
