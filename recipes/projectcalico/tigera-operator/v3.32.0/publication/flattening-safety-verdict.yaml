apiVersion: "evidence.confighub.com/v1alpha1"
kind: "FlatteningSafetyVerdict"
metadata:
  name: "projectcalico-tigera-operator-v3.32.0"
spec:
  chart:
    repository: "projectcalico"
    name: "tigera-operator"
    version: "v3.32.0"
    packageSHA256: "7e968633c7d8b1de1e648a15fa838995bdbae285bdca9af9badd13156e360450"
  witnessRef: "data/flattening-safety/witnesses/projectcalico-tigera-operator-v3.32.0.yaml"
  auditedBase: "default"
  dispositions:
    -
      class: "helm-hooks"
      finding: "present"
      detail: "templates/tigera-operator/00-uninstall.yaml is a pre-delete Job that tears the installation down; a recorded live run observed that teardown running as an explicit delete-cleanup action instead"
      disposition: "lifecycle route built from the recorded observation, executed by the delivery runtime"
      evidence:
        - "templates/tigera-operator/00-uninstall.yaml:13"
    -
      class: "resource-policy-keep"
      finding: "absent"
      detail: "absent from the packaged chart"
      disposition: "none required"
    -
      class: "lookup"
      finding: "present-gated"
      detail: "the felix-configuration template reads the cluster to choose an apiVersion, behind defaultFelixConfiguration.enabled, false in the audited base"
      disposition: "no route needed for the audited base"
      evidence:
        - "templates/crs/configmap-felixconfiguration-templates.yaml:4"
    -
      class: "webhook-ca"
      finding: "absent"
      detail: "absent from the packaged chart"
      disposition: "none required"
    -
      class: "capabilities-api-versions"
      finding: "absent"
      detail: "absent from the packaged chart"
      disposition: "none required"
    -
      class: "generated-secrets"
      finding: "absent"
      detail: "absent from the packaged chart"
      disposition: "none required"
    -
      class: "crd-ordering"
      finding: "absent"
      detail: "absent from the packaged chart"
      disposition: "none required"
    -
      class: "immutable-fields"
      finding: "not-evaluated"
      detail: "cross-version property; see boundedness"
      disposition: "versioned replacement route when an upgrade pair is audited"
    -
      class: "namespace-creation"
      finding: "absent"
      detail: "absent from the packaged chart"
      disposition: "none required"
    -
      class: "subchart-conditions"
      finding: "absent"
      detail: "absent from the packaged chart"
      disposition: "none required"
    -
      class: "test-hooks"
      finding: "absent"
      detail: "absent from the packaged chart"
      disposition: "none required"
  variantScope:
    -
      values: "defaultFelixConfiguration.enabled: true"
      effect: "renders the felix configuration, whose apiVersion is chosen by reading the cluster; that base trends do-not-flatten"
  verdict:
    lane: "flatten-with-routes"
    rationale: "Everything this base renders survives flattening except the teardown, which only matters when the release goes away and is exactly what a flattened bundle drops in silence. A recorded run watched that teardown happen as an explicit action, so the lane names a companion rather than hoping for one."
    routes:
      - "lifecycle route for the pre-delete teardown, with stages taken from the recorded live observation"
  boundedness:
    - "immutable-field changes are a cross-version property; this verdict compares no second version (the CRD upgrade delta lane holds that precedent)"
    - "the scan is static and does not execute templates, so values-gated reachability is a recorded judgment, not a render"
    - "the witness scans the packaged chart including its vendored subcharts; charts pulled at deploy time are out of scope"
  provenance:
    emittedBy: "scripts/generate-flattening-safety-verdicts.mjs"
    generatedFrom:
      - "data/flattening-safety/witnesses/projectcalico-tigera-operator-v3.32.0.yaml"
      - "recipes/projectcalico/tigera-operator/v3.32.0/source-lock.yaml"
