apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "HelmPlan"
metadata:
  name: "traefik-traefik-41.0.2"
spec:
  readiness:
    status: "usable-with-controls"
    chart: "traefik/traefik"
    version: "41.0.2"
    variants:
      - "default"
    helmObjectsByVariant:
      default: 31
    cubInstallObjectsByVariant:
      default: 32
    helmMatchByVariant:
      default: "31/31"
    scanGate: "warn-review-before-production"
    nextAction: "retain additively only after the 13-lane Kubara live-qualification receipt passes; keep complete Kubara component compatibility separate"
  receipts:
    - "revisions/default/r001/receipts/helm-equivalence-receipt.yaml"
    - "revisions/default/r001/receipts/render-receipt.yaml"
    - "revisions/default/r001/receipts/scan-receipt.yaml"
    - "revisions/default/r001/receipts/install-gate.yaml"
