apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ChartDossier"
metadata:
  name: "gatekeeper-gatekeeper-3-22-2"
  labels:
    confighub.io/chart-ref: "gatekeeper/gatekeeper"
    confighub.io/chart-version: "3.22.2"
    confighub.io/proof-tier: "next80-full"
spec:
  chart: "gatekeeper/gatekeeper"
  version: "3.22.2"
  maintainedNotes:
    - "gatekeeper/gatekeeper@3.22.2 renders deterministically under Kubernetes 1.30.0 with hooks and tests excluded."
    - "The default variant is intended as a proof baseline; production promotion still follows scan/gate review."
    - "Chart source contains Helm hooks; the proof revision uses no-hooks so hook lifecycle must be handled separately before production."
    - "Rendered output includes 17 CRDs; CRD lifecycle must be reviewed before upgrade/promotion."
    - "Rendered output includes cluster-scoped RBAC; production promotion needs RBAC review."
    - "Rendered output includes admission webhooks; live readiness/certificate observations are required after apply."
  knownControlPoints:
    - "source-lock"
    - "dependency-lock"
    - "capability-profile"
    - "variant-revision"
    - "rendered-manifest-scan"
    - "helm-equivalence"
    - "lifecycle-policy"
    - "extension-slots"
    - "crds"
    - "cluster-rbac"
    - "webhooks"
    - "secret-material"
  proofFocus: "OPA admission control, CRDs, webhooks, RBAC"
