apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ChartDossier"
metadata:
  name: "minio-operator-operator-7-1-1"
  labels:
    confighub.io/chart-ref: "minio-operator/operator"
    confighub.io/chart-version: "7.1.1"
    confighub.io/proof-tier: "next80-full"
spec:
  chart: "minio-operator/operator"
  version: "7.1.1"
  maintainedNotes:
    - "minio-operator/operator@7.1.1 renders deterministically under Kubernetes 1.30.0 with hooks and tests excluded."
    - "The default variant is intended as a proof baseline; production promotion still follows scan/gate review."
    - "Rendered output includes 2 CRDs; CRD lifecycle must be reviewed before upgrade/promotion."
    - "Rendered output includes cluster-scoped RBAC; production promotion needs RBAC review."
  knownControlPoints:
    - "source-lock"
    - "dependency-lock"
    - "capability-profile"
    - "variant-revision"
    - "rendered-manifest-scan"
    - "helm-equivalence"
    - "crds"
    - "cluster-rbac"
  proofFocus: "object storage operator, CRDs, RBAC"
