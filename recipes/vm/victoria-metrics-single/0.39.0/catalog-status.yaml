apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "CatalogStatus"
metadata:
  name: "vm-victoria-metrics-single-0.39.0"
spec:
  candidateVariants:
    - "default"
    - "default-reviewed"
  chart: "vm/victoria-metrics-single"
  deferredVariants:
    []
  notes:
    - "Machine proof exists; catalog support is not claimed until variant and product review are complete."
    - "default-reviewed is a realized useful-base alias of the default render; catalog support still requires ConfigHub proof, selected live evidence, and production disposition."
  productionReadiness: "not-reviewed-for-production"
  review:
    humanReviewRequired: true
    lastReviewed: "2026-05-27"
    productReviewRequired: true
  status: "proof-grade"
  supportLevel: "machine-proof-only"
  supportedScopes:
    []
  supportedVariants:
    []
  version: "0.39.0"
