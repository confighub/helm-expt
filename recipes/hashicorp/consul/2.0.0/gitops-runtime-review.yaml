apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "GitOpsRuntimeReview"
metadata:
  name: "consul-secure-mesh-existing-secrets-argocd-health-watch"
  chart: "hashicorp/consul"
  version: "2.0.0"
spec:
  base: "secure-mesh-existing-secrets"
  controller: "Argo CD"
  targetShape: "kind proof rig with target profile kind-three-node"
  receipt: "runs/live-helm-confighub-compare/hashicorp-consul-secure-mesh-existing-secrets/receipt.yaml"
  observedResult: "watch"
  supersededBy: "recipes/hashicorp/consul/2.0.0/runtime-review.yaml"
  note: "The latest live rerun moved this row from a narrow UI Ingress controller-health watch to a broader target runtime watch. Use runtime-review.yaml as the current support artifact."
  currentSummary:
    sync: "Synced"
    health: "Progressing"
    semanticParity: "pass"
    targetProfile: "pass"
    runtime: "watch"
  notClaimed:
    - "This file is no longer the current support artifact for the active Consul row."
    - "The current receipt does not prove ConfigHub OCI/Argo workload convergence for Consul secure mesh on the proof target."
    - "The current receipt does not prove that only the UI Ingress remains as controller-health residue."
