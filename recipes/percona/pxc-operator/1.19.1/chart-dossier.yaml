apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ChartDossier"
metadata:
  name: "percona-pxc-operator-1-19-1"
  labels:
    confighub.io/chart-ref: "percona/pxc-operator"
    confighub.io/chart-version: "1.19.1"
    confighub.io/proof-tier: "next80-full"
spec:
  chart: "percona/pxc-operator"
  version: "1.19.1"
  maintainedNotes:
    - "percona/pxc-operator@1.19.1 renders deterministically under Kubernetes 1.30.0 with hooks and tests excluded."
    - "The default variant is intended as a proof baseline; production promotion still follows scan/gate review."
    - "Chart source contains lookup; future variants using lookup paths must bind target facts in the recipe."
    - "Rendered output includes 3 CRDs; CRD lifecycle must be reviewed before upgrade/promotion."
  knownControlPoints:
    - "source-lock"
    - "dependency-lock"
    - "capability-profile"
    - "variant-revision"
    - "rendered-manifest-scan"
    - "helm-equivalence"
    - "target-facts"
    - "extension-slots"
    - "crds"
  proofFocus: "Percona XtraDB operator, CRDs, RBAC"
