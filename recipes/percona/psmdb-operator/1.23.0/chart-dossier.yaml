apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ChartDossier"
metadata:
  name: "percona-psmdb-operator-1-23-0"
  labels:
    confighub.io/chart-ref: "percona/psmdb-operator"
    confighub.io/chart-version: "1.23.0"
    confighub.io/proof-tier: "successor-full"
spec:
  chart: "percona/psmdb-operator"
  version: "1.23.0"
  category: "database operator"
  licenses:
    chart:
      evidence: "Chart release LICENSE: https://github.com/percona/percona-helm-charts/blob/psmdb-operator-1.23.0/LICENSE"
      spdx: "Apache-2.0"
    images:
      -
        component: "Percona MongoDB operator 1.23.0"
        note: "Operator source LICENSE: https://github.com/percona/percona-server-mongodb-operator/blob/v1.23.0/LICENSE. Percona Server for MongoDB is a separate database image and license; this operator-only chart does not render it."
        spdx: "Apache-2.0"
  maintainedNotes:
    - "percona/psmdb-operator@1.23.0 renders deterministically under Kubernetes 1.30.0 with hooks and tests excluded."
    - "The default variant is intended as a proof baseline; production promotion still follows scan/gate review."
    - "Chart source contains lookup; future variants using lookup paths must bind target facts in the recipe."
    - "Rendered output includes 4 CRDs; CRD lifecycle must be reviewed before upgrade/promotion."
  knownControlPoints:
    - "source-lock"
    - "dependency-lock"
    - "capability-profile"
    - "variant-revision"
    - "rendered-manifest-scan"
    - "helm-equivalence"
    - "target-facts"
    - "crds"
  proofFocus: "MongoDB operator workload, CRDs, RBAC"
