apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "Recipe"
metadata:
  name: "percona-psmdb-operator"
  version: "1.23.0"
  labels:
    confighub.io/chart-ref: "percona/psmdb-operator"
    confighub.io/chart-version: "1.23.0"
    confighub.io/proof-tier: "successor-full"
spec:
  chartRef:
    sourceLock: "source-lock.yaml"
    dependencyLock: "dependency-lock.yaml"
  importMode: "render-and-vendor"
  currentExecutableFixture:
    installerPackage: "packages/percona/psmdb-operator/1.23.0"
    setupCommand:
      - "cub"
      - "installer"
      - "setup"
      - "--pull"
      - "packages/percona/psmdb-operator/1.23.0"
      - "--non-interactive"
      - "--namespace"
      - "default"
  variants:
    - "variants/default/variant.yaml"
