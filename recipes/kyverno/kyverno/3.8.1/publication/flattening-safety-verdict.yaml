apiVersion: "evidence.confighub.com/v1alpha1"
kind: "FlatteningSafetyVerdict"
metadata:
  name: "kyverno-kyverno-3.8.1"
spec:
  chart:
    repository: "kyverno"
    name: "kyverno"
    version: "3.8.1"
    packageSHA256: "a33f35b83b6991daf6a1b1cf995becbe369747fcd84411d5089d55a40ee4ae0d"
  witnessRef: "data/flattening-safety/witnesses/kyverno-kyverno-3.8.1.yaml"
  auditedBase: "default"
  dispositions:
    -
      class: "helm-hooks"
      finding: "present"
      detail: "8 occurrence(s) in the packaged chart"
      disposition: "post-upgrade migration and pre-delete cleanup hooks; the catalog's observed routes are target-owned and not safe as automatic (data/hook-disposition)"
      evidence:
        - "charts/reports-server/templates/hooks/pre-delete-api-service-cleanup.yaml:11"
        - "templates/hooks/post-upgrade-migrate-resources.yaml:11"
        - "templates/hooks/post-upgrade-migrate-resources.yaml:51"
        - "templates/hooks/post-upgrade-migrate-resources.yaml:71"
        - "templates/hooks/post-upgrade-migrate-resources.yaml:84"
        - "templates/hooks/pre-delete-remove-webhooks.yaml:12"
    -
      class: "resource-policy-keep"
      finding: "present"
      detail: "keep rides on kyverno's own config configmap"
      disposition: "prune protection would be required beside any bundle"
      evidence:
        - "charts/reports-server/charts/postgresql/templates/backup/pvc.yaml:22"
        - "templates/config/configmap.yaml:14"
    -
      class: "lookup"
      finding: "present"
      detail: "templates/validate.yaml reads the live cluster in a default-path template"
      disposition: "no emitted route discharges a live validation lookup"
      evidence:
        - "charts/reports-server/charts/postgresql/charts/common/templates/_secrets.tpl:100"
        - "charts/reports-server/charts/postgresql/charts/common/templates/_secrets.tpl:156"
        - "charts/reports-server/charts/postgresql/charts/common/templates/_secrets.tpl:178"
        - "templates/validate.yaml:36"
    -
      class: "webhook-ca"
      finding: "absent"
      detail: "absent from the packaged chart"
      disposition: "none required"
    -
      class: "capabilities-api-versions"
      finding: "present"
      detail: "10 occurrence(s) in the packaged chart"
      disposition: "render inputs pin the kube version; recorded in every certified bundle receipt"
      evidence:
        - "charts/reports-server/charts/postgresql/charts/common/templates/_capabilities.tpl:16"
        - "charts/reports-server/charts/postgresql/charts/common/templates/_capabilities.tpl:19"
        - "charts/reports-server/charts/postgresql/charts/common/templates/_capabilities.tpl:221"
        - "charts/reports-server/charts/postgresql/charts/common/templates/_capabilities.tpl:222"
        - "templates/NOTES.txt:53"
        - "templates/NOTES.txt:54"
    -
      class: "generated-secrets"
      finding: "present"
      detail: "the gated reports-server postgres subchart carries the password-manage helpers"
      disposition: "external Secret reference would be required where that subchart is enabled"
      evidence:
        - "charts/reports-server/charts/postgresql/charts/common/templates/_secrets.tpl:70"
        - "charts/reports-server/charts/postgresql/charts/common/templates/_secrets.tpl:92"
        - "charts/reports-server/charts/postgresql/charts/common/templates/_secrets.tpl:123"
        - "charts/reports-server/charts/postgresql/charts/common/templates/_secrets.tpl:124"
        - "charts/reports-server/charts/postgresql/charts/common/templates/_secrets.tpl:128"
        - "charts/reports-server/charts/postgresql/templates/secrets.yaml:11"
    -
      class: "crd-ordering"
      finding: "present"
      detail: "52 occurrence(s) in the packaged chart"
      disposition: "ordering declaration would ship with any bundle"
      evidence:
        - "25 crds-directory file(s), 27 CRD document(s)"
    -
      class: "immutable-fields"
      finding: "not-evaluated"
      detail: "cross-version property; see boundedness"
      disposition: "versioned replacement route when an upgrade pair is audited"
    -
      class: "namespace-creation"
      finding: "present"
      detail: "2 occurrence(s) in the packaged chart"
      disposition: "namespace ships as its own Unit"
      evidence:
        - "charts/reports-server/templates/namespace.yaml:3"
        - "templates/_templating/namespace.yaml:3"
    -
      class: "subchart-conditions"
      finding: "present"
      detail: "5 occurrence(s) in the packaged chart"
      disposition: "the flatten step must render with the audited base's condition set"
      evidence:
        - "grafana gated by grafana.enabled"
        - "crds gated by crds.install"
        - "kyverno-api gated by crds.install"
        - "openreports gated by openreports.installCrds"
        - "reports-server gated by reportsServer.enabled"
    -
      class: "test-hooks"
      finding: "present"
      detail: "1 occurrence(s) in the packaged chart"
      disposition: "pruned from any bundle"
      evidence:
        - "templates/tests/helper-functions-test.yaml:13"
  variantScope:
    -
      values: "reports-server.enabled and its postgres subchart"
      effect: "adds the generated-password hazard the audited base avoids"
  verdict:
    lane: "do-not-flatten"
    rationale: "Migration and cleanup hooks with target-owned routes, a live lookup in config validation, and keep on the chart's own configmap exceed emitted routes; render-late stays certified, exactly the route the Sveltos fleet example ships (Sveltos installs this chart by Helm on the workload cluster)."
  boundedness:
    - "immutable-field changes are a cross-version property; this verdict compares no second version (the CRD upgrade delta lane holds that precedent)"
    - "the scan is static and does not execute templates, so values-gated reachability is a recorded judgment, not a render"
    - "the witness scans the packaged chart including its vendored subcharts; charts pulled at deploy time are out of scope"
  provenance:
    emittedBy: "scripts/generate-flattening-safety-verdicts.mjs"
    generatedFrom:
      - "data/flattening-safety/witnesses/kyverno-kyverno-3.8.1.yaml"
      - "recipes/kyverno/kyverno/3.8.1/source-lock.yaml"
