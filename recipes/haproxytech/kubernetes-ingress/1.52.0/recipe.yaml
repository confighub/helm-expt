apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "Recipe"
metadata:
  name: "haproxytech-kubernetes-ingress"
  version: "1.52.0"
  labels:
    confighub.io/chart-ref: "haproxytech/kubernetes-ingress"
    confighub.io/chart-version: "1.52.0"
    confighub.io/proof-tier: "next80-full"
spec:
  chartRef:
    sourceLock: "source-lock.yaml"
    dependencyLock: "dependency-lock.yaml"
  importMode: "render-and-vendor"
  currentExecutableFixture:
    installerPackage: "packages/haproxytech/kubernetes-ingress/1.52.0"
    setupCommand:
      - "cub"
      - "install"
      - "setup"
      - "--pull"
      - "packages/haproxytech/kubernetes-ingress/1.52.0"
      - "--non-interactive"
      - "--namespace"
      - "default"
  variants:
    - "variants/default/variant.yaml"
