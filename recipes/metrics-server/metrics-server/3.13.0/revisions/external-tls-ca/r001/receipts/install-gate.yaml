apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "InstallGate"
metadata:
  name: "metrics-server-external-tls-ca-r001"
spec:
  variantRevision: "../variant-revision.yaml"
  renderedObjectSetSHA256: "4b9df6b53cb5d8ed0b655b121696183d1c107ea88511e889bef1aae6708c8dad"
  decision: "warn"
  allowedScopes:
    - "local-test"
  blockedScopes:
    - "production"
  reasons:
    - "Helm equivalence passed for external-tls-ca"
    - "APIService availability needs a fresh observation receipt after apply"
    - "Cluster-scoped RBAC needs production review"
    - "requires target Secret kube-system/metrics-server-tls with tls.crt and tls.key, plus an APIService caBundle that matches the Secret certificate authority"
