apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "CatalogStatus"
metadata:
  name: "prometheus-community-prometheus-node-exporter-4.55.0"
spec:
  candidateVariants:
    - "default"
    - "cluster-metrics-readonly"
  chart: "prometheus-community/prometheus-node-exporter"
  deferredVariants:
    []
  notes:
    - "Machine proof exists; catalog support is not claimed until variant and product review are complete."
    - "cluster-metrics-readonly is a values-profile rerender that changes Helm inputs; catalog support still requires selected live evidence and production disposition."
  productionReadiness: "not-reviewed-for-production"
  review:
    humanReviewRequired: true
    lastReviewed: "2026-05-27"
    productReviewRequired: true
  status: "proof-grade"
  supportLevel: "machine-proof-only"
  supportedScopes:
    []
  supportedVariants:
    []
  version: "4.55.0"
