apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ChartDossier"
metadata:
  name: "prometheus-community-prometheus-blackbox-exporter-11-10-0"
  labels:
    confighub.io/chart-ref: "prometheus-community/prometheus-blackbox-exporter"
    confighub.io/chart-version: "11.10.0"
    confighub.io/proof-tier: "next80-full"
spec:
  chart: "prometheus-community/prometheus-blackbox-exporter"
  version: "11.10.0"
  maintainedNotes:
    - "prometheus-community/prometheus-blackbox-exporter@11.10.0 renders deterministically under Kubernetes 1.30.0 with hooks and tests excluded."
    - "The default variant is intended as a proof baseline; production promotion still follows scan/gate review."
    - "Chart source contains tpl; raw templating extension slots need explicit ownership and review."
  knownControlPoints:
    - "source-lock"
    - "dependency-lock"
    - "capability-profile"
    - "variant-revision"
    - "rendered-manifest-scan"
    - "helm-equivalence"
    - "tpl-extension-slots"
    - "extension-slots"
  proofFocus: "exporter workload, config validation, ServiceMonitor options"
