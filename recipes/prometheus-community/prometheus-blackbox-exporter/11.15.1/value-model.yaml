apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ValueModel"
metadata:
  name: "prometheus-community-prometheus-blackbox-exporter-11.15.1"
spec:
  checkedValues:
    -
      path: "<chart defaults>"
      variant: "default"
      disposition: "default-render-captured"
      reason: "the root-retention proof binds the exact artifact and records its deterministic default render"
  unknownValues: "not-exhaustively-checked-in-root-retention-proof"
  deadValues: "not-exhaustively-checked-in-root-retention-proof"
  ignoredValues: "not-exhaustively-checked-in-root-retention-proof"
