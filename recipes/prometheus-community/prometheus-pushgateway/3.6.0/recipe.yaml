apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "Recipe"
metadata:
  name: "prometheus-community-prometheus-pushgateway"
  version: "3.6.0"
  labels:
    confighub.io/chart-ref: "prometheus-community/prometheus-pushgateway"
    confighub.io/chart-version: "3.6.0"
    confighub.io/proof-tier: "next80-full"
spec:
  chartRef:
    sourceLock: "source-lock.yaml"
    dependencyLock: "dependency-lock.yaml"
  importMode: "render-and-vendor"
  currentExecutableFixture:
    installerPackage: "packages/prometheus-community/prometheus-pushgateway/3.6.0"
    setupCommand:
      - "cub"
      - "installer"
      - "setup"
      - "--pull"
      - "packages/prometheus-community/prometheus-pushgateway/3.6.0"
      - "--non-interactive"
      - "--namespace"
      - "default"
  variants:
    - "variants/default/variant.yaml"
