apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ChartDossier"
metadata:
  name: "prometheus-community-prometheus-adapter-5-3-0"
  labels:
    confighub.io/chart-ref: "prometheus-community/prometheus-adapter"
    confighub.io/chart-version: "5.3.0"
    confighub.io/proof-tier: "next80-full"
spec:
  chart: "prometheus-community/prometheus-adapter"
  version: "5.3.0"
  maintainedNotes:
    - "prometheus-community/prometheus-adapter@5.3.0 renders deterministically under Kubernetes 1.30.0 with hooks and tests excluded."
    - "The default variant is intended as a proof baseline; production promotion still follows scan/gate review."
    - "Chart source contains tpl; raw templating extension slots need explicit ownership and review."
    - "Rendered output includes cluster-scoped RBAC; production promotion needs RBAC review."
  knownControlPoints:
    - "source-lock"
    - "dependency-lock"
    - "capability-profile"
    - "variant-revision"
    - "rendered-manifest-scan"
    - "helm-equivalence"
    - "tpl-extension-slots"
    - "extension-slots"
    - "cluster-rbac"
    - "apiservice"
  proofFocus: "APIService, RBAC, custom metrics"
