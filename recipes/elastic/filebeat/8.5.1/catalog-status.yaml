apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "CatalogStatus"
metadata:
  name: "elastic-filebeat-8.5.1"
spec:
  candidateVariants:
    - "default"
    - "node-or-cluster-collector"
  chart: "elastic/filebeat"
  deferredVariants:
    []
  notes:
    - "Machine proof exists; catalog support is not claimed until variant and product review are complete."
    - "node-or-cluster-collector is a realized useful-base alias of the default render; catalog support still requires ConfigHub proof, selected live evidence, and production disposition."
  productionReadiness: "not-reviewed-for-production"
  review:
    humanReviewRequired: true
    lastReviewed: "2026-05-27"
    productReviewRequired: true
  status: "proof-grade"
  supportLevel: "machine-proof-only"
  supportedScopes:
    []
  supportedVariants:
    []
  version: "8.5.1"
