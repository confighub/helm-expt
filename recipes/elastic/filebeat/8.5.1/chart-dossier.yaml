apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ChartDossier"
metadata:
  name: "elastic-filebeat-8-5-1"
  labels:
    confighub.io/chart-ref: "elastic/filebeat"
    confighub.io/chart-version: "8.5.1"
    confighub.io/proof-tier: "next80-full"
spec:
  chart: "elastic/filebeat"
  version: "8.5.1"
  maintainedNotes:
    - "elastic/filebeat@8.5.1 renders deterministically under Kubernetes 1.30.0 with hooks and tests excluded."
    - "The default variant is intended as a proof baseline; production promotion still follows scan/gate review."
    - "Chart source contains tpl; raw templating extension slots need explicit ownership and review."
    - "Rendered output includes cluster-scoped RBAC; production promotion needs RBAC review."
  knownControlPoints:
    - "source-lock"
    - "dependency-lock"
    - "capability-profile"
    - "variant-revision"
    - "rendered-manifest-scan"
    - "helm-equivalence"
    - "tpl-extension-slots"
    - "extension-slots"
    - "cluster-rbac"
  proofFocus: "DaemonSet, host mounts, log collection"
