apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "Recipe"
metadata:
  labels:
    confighub.io/chart-ref: "elastic/eck-operator"
    confighub.io/chart-version: "3.4.0"
    confighub.io/proof-tier: "next80-full"
  name: "elastic-eck-operator"
  version: "3.4.0"
spec:
  chartRef:
    dependencyLock: "dependency-lock.yaml"
    sourceLock: "source-lock.yaml"
  currentExecutableFixture:
    installerPackage: "packages/elastic/eck-operator/3.4.0"
    setupCommand:
      - "cub"
      - "installer"
      - "setup"
      - "--pull"
      - "packages/elastic/eck-operator/3.4.0"
      - "--non-interactive"
      - "--namespace"
      - "default"
  importMode: "render-and-vendor"
  variants:
    - "variants/default/variant.yaml"
    - "variants/ha/variant.yaml"
