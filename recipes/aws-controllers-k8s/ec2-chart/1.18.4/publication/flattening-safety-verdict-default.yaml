apiVersion: "evidence.confighub.com/v1alpha1"
kind: "FlatteningSafetyVerdict"
metadata:
  name: "aws-controllers-k8s-ec2-chart-1.18.4-default"
spec:
  chart:
    repository: "aws-controllers-k8s"
    name: "ec2-chart"
    version: "1.18.4"
    packageSHA256: "17c082eac9ef1f859fdf72d4e86788ab7add27876da9f2ba7cd145eeea75bc20"
  witnessRef: "data/flattening-safety/witnesses/aws-controllers-k8s-ec2-chart-1.18.4.yaml"
  auditedBase: "default"
  dispositions:
    -
      class: "helm-hooks"
      finding: "absent"
      detail: "absent from the packaged chart"
      disposition: "none required"
    -
      class: "resource-policy-keep"
      finding: "absent"
      detail: "absent from the packaged chart"
      disposition: "none required"
    -
      class: "lookup"
      finding: "absent"
      detail: "absent from the packaged chart"
      disposition: "none required"
    -
      class: "webhook-ca"
      finding: "absent"
      detail: "absent from the packaged chart"
      disposition: "none required"
    -
      class: "capabilities-api-versions"
      finding: "absent"
      detail: "absent from the packaged chart"
      disposition: "none required"
    -
      class: "generated-secrets"
      finding: "absent"
      detail: "absent from the packaged chart"
      disposition: "none required"
    -
      class: "crd-ordering"
      finding: "present"
      detail: "44 occurrence(s) in the packaged chart"
      disposition: "ordering declaration ships with the bundle (crds split or sync waves)"
      evidence:
        - "22 crds-directory file(s), 22 CRD document(s)"
    -
      class: "immutable-fields"
      finding: "not-evaluated"
      detail: "cross-version property; see boundedness"
      disposition: "versioned replacement route when an upgrade pair is audited"
    -
      class: "namespace-creation"
      finding: "absent"
      detail: "absent from the packaged chart"
      disposition: "none required"
    -
      class: "subchart-conditions"
      finding: "absent"
      detail: "absent from the packaged chart"
      disposition: "none required"
    -
      class: "test-hooks"
      finding: "absent"
      detail: "absent from the packaged chart"
      disposition: "none required"
  variantScope:
    -
      values: "deletionPolicy (chart default deletes AWS resources on prune; the eks-inference base sets retain)"
      effect: "not a flattening hazard, but the single most consequential values choice for this chart under a pruning reconciler"
  verdict:
    lane: "flatten-with-routes"
    rationale: "Chart defaults change no quirk class against the eks-inference base; the CRDs remain the one construct needing a companion."
    routes:
      - "CRD ordering declaration for the 22 EC2 CRDs"
  boundedness:
    - "immutable-field changes are a cross-version property; this verdict compares no second version (the CRD upgrade delta lane holds that precedent)"
    - "the scan is static and does not execute templates, so values-gated reachability is a recorded judgment, not a render"
    - "the witness scans the packaged chart including its vendored subcharts; charts pulled at deploy time are out of scope"
  provenance:
    emittedBy: "scripts/generate-flattening-safety-verdicts.mjs"
    generatedFrom:
      - "data/flattening-safety/witnesses/aws-controllers-k8s-ec2-chart-1.18.4.yaml"
      - "recipes/aws-controllers-k8s/ec2-chart/1.18.4/source-lock.yaml"
