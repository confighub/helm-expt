apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "HelmPlan"
metadata:
  name: "aws-controllers-k8s-iam-chart-1.7.3"
spec:
  readiness:
    status: "usable-with-controls"
    chart: "aws-controllers-k8s/iam-chart"
    version: "1.7.3"
    variants:
      - "default"
      - "eks-inference"
    helmObjectsByVariant:
      default: 19
      eks-inference: 19
    cubInstallObjectsByVariant:
      default: 20
      eks-inference: 20
    helmMatchByVariant:
      default: "19/19"
      eks-inference: "19/19"
    scanGate: "warn-production-blocked"
    flatteningVerdict: "flatten-with-routes; the CRD ordering declaration for the nine CRDs is the single route, and the eks-inference producer already ships it as sync waves -20 and -10"
    nextAction: "fill the aws.region placeholder per environment, create the ack-system/aws-creds Secret before the eks-inference controller starts, and decide deletionPolicy per target before promotion"
  receipts:
    - "revisions/default/r001/receipts/helm-equivalence-receipt.yaml"
    - "revisions/default/r001/receipts/render-receipt.yaml"
    - "revisions/default/r001/receipts/scan-receipt.yaml"
    - "revisions/default/r001/receipts/install-gate.yaml"
    - "revisions/eks-inference/r001/receipts/helm-equivalence-receipt.yaml"
    - "revisions/eks-inference/r001/receipts/render-receipt.yaml"
    - "revisions/eks-inference/r001/receipts/scan-receipt.yaml"
    - "revisions/eks-inference/r001/receipts/install-gate.yaml"
