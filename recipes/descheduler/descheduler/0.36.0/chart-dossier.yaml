apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ChartDossier"
metadata:
  name: "descheduler-descheduler-0-36-0"
  labels:
    confighub.io/chart-ref: "descheduler/descheduler"
    confighub.io/chart-version: "0.36.0"
    confighub.io/proof-tier: "next80-full"
spec:
  chart: "descheduler/descheduler"
  version: "0.36.0"
  maintainedNotes:
    - "descheduler/descheduler@0.36.0 renders deterministically under Kubernetes 1.30.0 with hooks and tests excluded."
    - "The default variant is intended as a proof baseline; production promotion still follows scan/gate review."
    - "Chart source contains tpl; raw templating extension slots need explicit ownership and review."
    - "Rendered output includes cluster-scoped RBAC; production promotion needs RBAC review."
  knownControlPoints:
    - "source-lock"
    - "dependency-lock"
    - "capability-profile"
    - "variant-revision"
    - "rendered-manifest-scan"
    - "helm-equivalence"
    - "tpl-extension-slots"
    - "cluster-rbac"
  proofFocus: "scheduler maintenance controller, RBAC, CronJob/Deployment modes"
