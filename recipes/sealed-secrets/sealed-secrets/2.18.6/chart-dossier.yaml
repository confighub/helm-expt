apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ChartDossier"
metadata:
  name: "sealed-secrets-sealed-secrets-2-18-6"
  labels:
    confighub.io/chart-ref: "sealed-secrets/sealed-secrets"
    confighub.io/chart-version: "2.18.6"
    confighub.io/proof-tier: "next80-full"
spec:
  chart: "sealed-secrets/sealed-secrets"
  version: "2.18.6"
  maintainedNotes:
    - "sealed-secrets/sealed-secrets@2.18.6 renders deterministically under Kubernetes 1.30.0 with hooks and tests excluded."
    - "The default variant is intended as a proof baseline; production promotion still follows scan/gate review."
    - "Chart source contains lookup; future variants using lookup paths must bind target facts in the recipe."
    - "Chart source contains generated/random/time helpers; generated values must be captured before approval if activated by a variant."
    - "Chart source contains tpl; raw templating extension slots need explicit ownership and review."
    - "Rendered output includes 1 CRDs; CRD lifecycle must be reviewed before upgrade/promotion."
    - "Rendered output includes cluster-scoped RBAC; production promotion needs RBAC review."
  knownControlPoints:
    - "source-lock"
    - "dependency-lock"
    - "capability-profile"
    - "variant-revision"
    - "rendered-manifest-scan"
    - "helm-equivalence"
    - "target-facts"
    - "generated-facts"
    - "tpl-extension-slots"
    - "extension-slots"
    - "crds"
    - "cluster-rbac"
  proofFocus: "secret controller, CRDs, key lifecycle"
