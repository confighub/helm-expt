apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ChartDossier"
metadata:
  name: "rook-release-rook-ceph-v1-19-5"
  labels:
    confighub.io/chart-ref: "rook-release/rook-ceph"
    confighub.io/chart-version: "v1.19.5"
    confighub.io/proof-tier: "next80-full"
spec:
  chart: "rook-release/rook-ceph"
  version: "v1.19.5"
  maintainedNotes:
    - "rook-release/rook-ceph@v1.19.5 renders deterministically under Kubernetes 1.30.0 with hooks and tests excluded."
    - "The default variant is intended as a proof baseline; production promotion still follows scan/gate review."
    - "Chart source contains lookup; future variants using lookup paths must bind target facts in the recipe."
    - "Rendered output includes 25 CRDs; CRD lifecycle must be reviewed before upgrade/promotion."
    - "Rendered output includes cluster-scoped RBAC; production promotion needs RBAC review."
  knownControlPoints:
    - "source-lock"
    - "dependency-lock"
    - "capability-profile"
    - "variant-revision"
    - "rendered-manifest-scan"
    - "helm-equivalence"
    - "target-facts"
    - "crds"
    - "cluster-rbac"
  proofFocus: "storage operator, large CRD set, RBAC"
