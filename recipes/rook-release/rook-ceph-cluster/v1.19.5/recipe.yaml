apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "Recipe"
metadata:
  name: "rook-release-rook-ceph-cluster"
  version: "v1.19.5"
  labels:
    confighub.io/chart-ref: "rook-release/rook-ceph-cluster"
    confighub.io/chart-version: "v1.19.5"
    confighub.io/proof-tier: "next80-full"
spec:
  chartRef:
    sourceLock: "source-lock.yaml"
    dependencyLock: "dependency-lock.yaml"
  importMode: "render-and-vendor"
  currentExecutableFixture:
    installerPackage: "packages/rook-release/rook-ceph-cluster/v1.19.5"
    setupCommand:
      - "cub"
      - "install"
      - "setup"
      - "--pull"
      - "packages/rook-release/rook-ceph-cluster/v1.19.5"
      - "--non-interactive"
      - "--namespace"
      - "default"
  variants:
    - "variants/default/variant.yaml"
