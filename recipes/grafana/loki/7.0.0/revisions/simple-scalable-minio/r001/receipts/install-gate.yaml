apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "InstallGate"
metadata:
  name: "loki-simple-scalable-minio-r001"
spec:
  variantRevision: "../variant-revision.yaml"
  renderedObjectSetSHA256: "91787d0999a776a5b32de6fa06264eb4ba43bc520a66f82234a07cdf94c2f207"
  decision: "warn"
  allowedScopes:
    - "local-test"
  blockedScopes:
    - "production"
  reasons:
    - "Helm equivalence passed for simple-scalable-minio"
    - "Default chart render blocker is recorded and avoided by this bounded variant"
    - "Loki storage, schema, object-store, and StatefulSet policies need production review"
    - "Any future hook or test enablement needs explicit lifecycle policy before production"
    - "Raw/tpl extension slots must remain empty or be scanned as first-class variant inputs"
    - "satisfies required bucket names, uses one read/write/backend replica for a one-node local target, and renders the MinIO Secret as chart-owned test storage material"
