apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "Recipe"
metadata:
  labels:
    confighub.io/chart-ref: "grafana/rollout-operator"
    confighub.io/chart-version: "0.49.0"
    confighub.io/proof-tier: "next80-full"
  name: "grafana-rollout-operator"
  version: "0.49.0"
spec:
  chartRef:
    dependencyLock: "dependency-lock.yaml"
    sourceLock: "source-lock.yaml"
  currentExecutableFixture:
    installerPackage: "packages/grafana/rollout-operator/0.49.0"
    setupCommand:
      - "cub"
      - "installer"
      - "setup"
      - "--pull"
      - "packages/grafana/rollout-operator/0.49.0"
      - "--non-interactive"
      - "--namespace"
      - "default"
  importMode: "render-and-vendor"
  variants:
    - "variants/default/variant.yaml"
    - "variants/no-crds/variant.yaml"
