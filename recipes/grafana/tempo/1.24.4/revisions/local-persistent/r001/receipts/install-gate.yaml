apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "InstallGate"
metadata:
  name: "tempo-local-persistent-r001"
spec:
  variantRevision: "../variant-revision.yaml"
  renderedObjectSetSHA256: "df7ce7b40425eef8d057bdd80416ea3ae781ababe7ce6a154db74990c0f07905"
  decision: "warn"
  allowedScopes:
    - "local-test"
  blockedScopes:
    - "production"
  reasons:
    - "Helm equivalence passed for local-persistent"
    - "Tempo storage backend and query/observability posture are explicit variant choices"
    - "Helm hook behavior needs explicit lifecycle policy before production"
    - "Tempo StatefulSet, storage, S3 credential Secret, query ingress, NetworkPolicy, ServiceMonitor, and extension slots need production review"
    - "uses local storage, the vanilla kind default StorageClass, and disables Tempo usage reporting so the rendered StatefulSet is deterministic"
