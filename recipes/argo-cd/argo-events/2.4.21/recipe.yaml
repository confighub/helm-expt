apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "Recipe"
metadata:
  labels:
    confighub.io/chart-ref: "argo-cd/argo-events"
    confighub.io/chart-version: "2.4.21"
    confighub.io/proof-tier: "next80-full"
  name: "argo-cd-argo-events"
  version: "2.4.21"
spec:
  chartRef:
    dependencyLock: "dependency-lock.yaml"
    sourceLock: "source-lock.yaml"
  currentExecutableFixture:
    installerPackage: "packages/argo-cd/argo-events/2.4.21"
    setupCommand:
      - "cub"
      - "installer"
      - "setup"
      - "--pull"
      - "packages/argo-cd/argo-events/2.4.21"
      - "--non-interactive"
      - "--namespace"
      - "default"
  importMode: "render-and-vendor"
  variants:
    - "variants/default/variant.yaml"
    - "variants/no-crds/variant.yaml"
