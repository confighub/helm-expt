apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ChartDossier"
metadata:
  name: "argo-cd-argo-events-2-4-21"
  labels:
    confighub.io/chart-ref: "argo-cd/argo-events"
    confighub.io/chart-version: "2.4.21"
    confighub.io/proof-tier: "next80-full"
spec:
  chart: "argo-cd/argo-events"
  version: "2.4.21"
  maintainedNotes:
    - "argo-cd/argo-events@2.4.21 renders deterministically under Kubernetes 1.30.0 with hooks and tests excluded."
    - "The default variant is intended as a proof baseline; production promotion still follows scan/gate review."
    - "Chart source contains tpl; raw templating extension slots need explicit ownership and review."
    - "Rendered output includes 3 CRDs; CRD lifecycle must be reviewed before upgrade/promotion."
    - "Rendered output includes cluster-scoped RBAC; production promotion needs RBAC review."
  knownControlPoints:
    - "source-lock"
    - "dependency-lock"
    - "capability-profile"
    - "variant-revision"
    - "rendered-manifest-scan"
    - "helm-equivalence"
    - "tpl-extension-slots"
    - "extension-slots"
    - "crds"
    - "cluster-rbac"
  proofFocus: "eventing controller, CRDs, RBAC"
