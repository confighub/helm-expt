apiVersion: helm-expt.confighub.com/v1alpha1
kind: InstallGate
metadata:
  name: bitnami-redis-reuse-existing-secret-r001
spec:
  variantRevision: ../variant-revision.yaml
  renderedObjectSetSHA256: "55f424589dfd8581580a51659cdf46e52180961ddbf9f6b7b244d0aacc42546f"
  decision: warn
  allowedScopes:
    - local-test
  blockedScopes:
    - production
  reasons:
    - local scan found 0 high and 2 medium finding(s)
    - production publish is blocked until scan findings are resolved or explicitly waived
    - Helm equivalence passed for reuse-existing-secret variant
    - requires target Secret redis/redis-existing-secret with key redis-password
