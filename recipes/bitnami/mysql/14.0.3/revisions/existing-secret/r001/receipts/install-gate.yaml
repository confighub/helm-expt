apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "InstallGate"
metadata:
  name: "mysql-existing-secret-r001"
spec:
  variantRevision: "../variant-revision.yaml"
  renderedObjectSetSHA256: "024c04f020fbe5cc7af518d743a68e055f999e88a6633b278adccc769c1b7fa8"
  decision: "warn"
  allowedScopes:
    - "local-test"
  blockedScopes:
    - "production"
  reasons:
    - "Helm equivalence passed for existing-secret"
    - "Generated credential handling needs explicit generated fact or target fact policy before production"
    - "Supported bases render no hook objects; future hook-producing values need explicit lifecycle policy before production"
    - "MySQL StatefulSet and volumeClaimTemplates need storage, upgrade, and rollback policy"
    - "requires target Secret mysql/mysql-auth with MySQL password keys before apply"
