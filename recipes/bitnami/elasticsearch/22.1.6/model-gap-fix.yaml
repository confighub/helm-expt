apiVersion: helm-expt.confighub.com/v1alpha1
kind: ModelGapFix
metadata:
  name: elasticsearch-22.1.6-model-gap-fix
  chart: bitnami/elasticsearch
  version: 22.1.6
spec:
  gap_kind: object-set-shape
  bases:
    - base: legacy
      lane: K
      failure_signature: "semantic object parity issue: missing=0 extra=1 diffs=4; extra=v1|Namespace||default; objects include elasticsearch coordinating/data/master StatefulSets and Services"
      receipt: runs/live-kind-parity/bitnami-elasticsearch-legacy/receipt.yaml
  fix_class: applicable-fix-spec
  sibling_base_that_passes: "none"
  decision: "The legacy base resolves the image-pull direction but still has a two-cluster semantic object-shape residue. The installer-added Namespace is likely allowable normalization; the remaining StatefulSet/Service diffs need field-level review."
  fix_spec: "Classify the installer-added default Namespace through the semantic-normalization policy, then diff the legacy base StatefulSet and Service objects against regular Helm and reconcile recipe/render inputs or add a narrow documented normalization for intentional server/defaulting differences."
  verify_non_live: "Render the legacy base offline and diff against regular Helm under the recorded values; only documented allowed normalization should remain before a live rerun."
  rerun_command: "npm run kind-parity:run -- --chart bitnami/elasticsearch --version 22.1.6 --base legacy --repo-url oci://registry-1.docker.io/bitnamicharts"
  automatic: false
  evidence:
    - runs/live-kind-parity/bitnami-elasticsearch-legacy/receipt.yaml
  not_claimed:
    - "This card does not prove the cell passes; it requires applying the fix and a fresh live receipt."

