apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "Recipe"
metadata:
  name: "autoscaler-vertical-pod-autoscaler"
  version: "0.9.0"
  labels:
    confighub.io/chart-ref: "autoscaler/vertical-pod-autoscaler"
    confighub.io/chart-version: "0.9.0"
    confighub.io/proof-tier: "next80-full"
spec:
  chartRef:
    sourceLock: "source-lock.yaml"
    dependencyLock: "dependency-lock.yaml"
  importMode: "render-and-vendor"
  currentExecutableFixture:
    installerPackage: "packages/autoscaler/vertical-pod-autoscaler/0.9.0"
    setupCommand:
      - "cub"
      - "install"
      - "setup"
      - "--pull"
      - "packages/autoscaler/vertical-pod-autoscaler/0.9.0"
      - "--non-interactive"
      - "--namespace"
      - "default"
  variants:
    - "variants/default/variant.yaml"
