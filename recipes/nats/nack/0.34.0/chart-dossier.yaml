apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ChartDossier"
metadata:
  name: "nats-nack-0-34-0"
  labels:
    confighub.io/chart-ref: "nats/nack"
    confighub.io/chart-version: "0.34.0"
    confighub.io/proof-tier: "next80-full"
spec:
  chart: "nats/nack"
  version: "0.34.0"
  maintainedNotes:
    - "nats/nack@0.34.0 renders deterministically under Kubernetes 1.30.0 with hooks and tests excluded."
    - "The default variant is intended as a proof baseline; production promotion still follows scan/gate review."
    - "Chart source contains generated/random/time helpers; generated values must be captured before approval if activated by a variant."
    - "Chart source contains tpl; raw templating extension slots need explicit ownership and review."
    - "Rendered output includes 6 CRDs; CRD lifecycle must be reviewed before upgrade/promotion."
    - "Rendered output includes cluster-scoped RBAC; production promotion needs RBAC review."
  knownControlPoints:
    - "source-lock"
    - "dependency-lock"
    - "capability-profile"
    - "variant-revision"
    - "rendered-manifest-scan"
    - "helm-equivalence"
    - "generated-facts"
    - "tpl-extension-slots"
    - "crds"
    - "cluster-rbac"
  proofFocus: "NATS controller, CRDs, RBAC"
