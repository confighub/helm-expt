apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "EffectiveValues"
metadata:
  name: "nats-nats-2.14.0-ha"
  labels:
    confighub.io/chart: "nats/nats"
    confighub.io/version: "2.14.0"
    confighub.io/variant: "ha"
spec:
  profile: "ha"
  renderDelta: "config.cluster.enabled=true,config.cluster.replicas=3"
  mergedValuesCaptured: false
