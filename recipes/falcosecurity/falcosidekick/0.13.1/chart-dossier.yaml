apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ChartDossier"
metadata:
  name: "falcosecurity-falcosidekick-0-13-1"
  labels:
    confighub.io/chart-ref: "falcosecurity/falcosidekick"
    confighub.io/chart-version: "0.13.1"
    confighub.io/proof-tier: "next80-full"
spec:
  chart: "falcosecurity/falcosidekick"
  version: "0.13.1"
  maintainedNotes:
    - "falcosecurity/falcosidekick@0.13.1 renders deterministically under Kubernetes 1.30.0 with hooks and tests excluded."
    - "The default variant is intended as a proof baseline; production promotion still follows scan/gate review."
    - "Chart source contains Helm hooks; the proof revision uses no-hooks so hook lifecycle must be handled separately before production."
  knownControlPoints:
    - "source-lock"
    - "dependency-lock"
    - "capability-profile"
    - "variant-revision"
    - "rendered-manifest-scan"
    - "helm-equivalence"
    - "lifecycle-policy"
    - "extension-slots"
    - "secret-material"
  proofFocus: "runtime security event forwarding, Secret/config handling"
