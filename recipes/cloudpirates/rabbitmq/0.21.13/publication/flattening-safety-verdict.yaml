apiVersion: "evidence.confighub.com/v1alpha1"
kind: "FlatteningSafetyVerdict"
metadata:
  name: "cloudpirates-rabbitmq-0.21.13"
spec:
  chart:
    repository: "cloudpirates"
    name: "rabbitmq"
    version: "0.21.13"
    packageSHA256: "b1884d0635597974715562e64ce97a24e32a488637538ae7c342e3ee7434588e"
  witnessRef: "data/flattening-safety/witnesses/cloudpirates-rabbitmq-0.21.13.yaml"
  auditedBase: "default"
  dispositions:
    -
      class: "helm-hooks"
      finding: "absent"
      detail: "absent from the packaged chart"
      disposition: "none required"
    -
      class: "resource-policy-keep"
      finding: "present"
      detail: "that Secret carries helm.sh/resource-policy: keep, so it survives an uninstall and a later install finds it again"
      disposition: "named companion artifact required"
      evidence:
        - "templates/secret.yaml:12"
    -
      class: "lookup"
      finding: "present"
      detail: "templates/secret.yaml reads any existing Secret of the same name for both the password and the Erlang cookie; the audited base leaves auth.existingSecret unset, so that Secret renders"
      disposition: "named companion artifact required"
      evidence:
        - "charts/common/templates/_secrets.tpl:16"
        - "templates/secret.yaml:3"
    -
      class: "webhook-ca"
      finding: "absent"
      detail: "absent from the packaged chart"
      disposition: "none required"
    -
      class: "capabilities-api-versions"
      finding: "present-gated"
      detail: "the Ingress template picks its apiVersion from the kube version and renders only when ingress.enabled is true, which the audited base leaves false; the OpenShift helper is never called"
      disposition: "no route needed for the audited base"
      evidence:
        - "charts/common/templates/_helpers.tpl:296"
        - "templates/ingress.yaml:7"
        - "templates/ingress.yaml:9"
        - "templates/ingress.yaml:44"
    -
      class: "generated-secrets"
      finding: "present"
      detail: "the same template falls back to randAlphaNum 32 for the password and again for the Erlang cookie, so two renders of this base differ in two fields"
      disposition: "named companion artifact required"
      evidence:
        - "templates/secret.yaml:23"
        - "templates/secret.yaml:30"
    -
      class: "crd-ordering"
      finding: "absent"
      detail: "absent from the packaged chart"
      disposition: "none required"
    -
      class: "immutable-fields"
      finding: "not-evaluated"
      detail: "cross-version property; see boundedness"
      disposition: "versioned replacement route when an upgrade pair is audited"
    -
      class: "namespace-creation"
      finding: "absent"
      detail: "absent from the packaged chart"
      disposition: "none required"
    -
      class: "subchart-conditions"
      finding: "absent"
      detail: "absent from the packaged chart"
      disposition: "none required"
    -
      class: "test-hooks"
      finding: "absent"
      detail: "absent from the packaged chart"
      disposition: "none required"
  variantScope:
    -
      values: "auth.existingSecret set (the catalog's existing-secret base)"
      effect: "the Secret template is skipped, and the lookups, the generated values and the keep annotation go with it"
  verdict:
    lane: "unsafe-to-flatten"
    rationale: "The password and the Erlang cookie are decided at render time, and the Secret that holds them is annotated to survive an uninstall, so a flattened bundle would carry one render's credentials."
  boundedness:
    - "immutable-field changes are a cross-version property; this verdict compares no second version (the CRD upgrade delta lane holds that precedent)"
    - "the scan is static and does not execute templates, so values-gated reachability is a recorded judgment, not a render"
    - "the witness scans the packaged chart including its vendored subcharts; charts pulled at deploy time are out of scope"
  provenance:
    emittedBy: "scripts/generate-flattening-safety-verdicts.mjs"
    generatedFrom:
      - "data/flattening-safety/witnesses/cloudpirates-rabbitmq-0.21.13.yaml"
      - "recipes/cloudpirates/rabbitmq/0.21.13/source-lock.yaml"
