apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "ChartDossier"
metadata:
  name: "cloudpirates-nginx-0-16-1"
  labels:
    confighub.io/chart-ref: "cloudpirates/nginx"
    confighub.io/chart-version: "0.16.1"
    confighub.io/proof-tier: "successor-full"
spec:
  chart: "cloudpirates/nginx"
  version: "0.16.1"
  category: "web server"
  licenses:
    chart:
      evidence: "artifacthub license annotation read from the pulled chart"
      spdx: "Apache-2.0"
    images:
      -
        component: "nginx"
        note: "upstream nginx source license; image digest-pinned by the chart default"
        spdx: "BSD-2-Clause"
  maintainedNotes:
    - "cloudpirates/nginx@0.16.1 renders deterministically under Kubernetes 1.30.0 with hooks and tests excluded."
    - "The default variant is intended as a proof baseline; production promotion still follows scan/gate review."
    - "Chart source contains lookup; future variants using lookup paths must bind target facts in the recipe."
    - "Chart source contains tpl; raw templating extension slots need explicit ownership and review."
  knownControlPoints:
    - "source-lock"
    - "dependency-lock"
    - "capability-profile"
    - "variant-revision"
    - "rendered-manifest-scan"
    - "helm-equivalence"
    - "target-facts"
    - "tpl-extension-slots"
    - "extension-slots"
  proofFocus: "web server workload, ClusterIP Service exposure, digest-pinned default image"
