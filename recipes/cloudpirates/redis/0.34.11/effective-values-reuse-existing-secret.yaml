apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "EffectiveValues"
metadata:
  name: "cloudpirates-redis-0.34.11-reuse-existing-secret"
  labels:
    confighub.io/chart: "cloudpirates/redis"
    confighub.io/version: "0.34.11"
    confighub.io/variant: "reuse-existing-secret"
spec:
  profile: "reuse-existing-secret"
  renderDelta: "data/successor-track/variant-values/redis-reuse-existing-secret.yaml"
  mergedValuesCaptured: true
  values:
    architecture: "standalone"
    auth:
      existingSecret: "redis-existing-secret"
      existingSecretPasswordKey: "redis-password"
    serviceAccount:
      create: true
  valuesFile: "data/successor-track/variant-values/redis-reuse-existing-secret.yaml"
