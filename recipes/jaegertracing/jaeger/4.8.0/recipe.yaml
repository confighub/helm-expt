apiVersion: "helm-expt.confighub.com/v1alpha1"
kind: "Recipe"
metadata:
  name: "jaegertracing-jaeger"
  version: "4.8.0"
  labels:
    confighub.io/chart-ref: "jaegertracing/jaeger"
    confighub.io/chart-version: "4.8.0"
    confighub.io/proof-tier: "next80-full"
spec:
  chartRef:
    sourceLock: "source-lock.yaml"
    dependencyLock: "dependency-lock.yaml"
  importMode: "render-and-vendor"
  currentExecutableFixture:
    installerPackage: "packages/jaegertracing/jaeger/4.8.0"
    setupCommand:
      - "cub"
      - "installer"
      - "setup"
      - "--pull"
      - "packages/jaegertracing/jaeger/4.8.0"
      - "--non-interactive"
      - "--namespace"
      - "default"
  variants:
    - "variants/default/variant.yaml"
