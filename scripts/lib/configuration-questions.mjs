export const CONFIGURATION_QUESTIONS = {
  "ai-values": {
    group: "common",
    label: "AI wrote these values. What did they actually change?",
    answer: "Render the candidate and compare it with the chart defaults, the Catalog, and optionally the current deployment. Show exact changes and check credentials, permissions, images, storage, hooks, and CRDs.",
    instruction: "Render the candidate, the chart defaults, and any exact Catalog match. Compare their objects and fields. Check credentials, RBAC, images, storage, hooks, CRDs, and target requirements.",
    issueOption: "ai-values: AI wrote these values. What did they actually change?",
    recommendation: "Review the exact object diff, correct the values or rendered objects, and retain the accepted result.",
  },
  "ignored-values": {
    group: "common",
    label: "I set a value. Why did the rendered object not change?",
    answer: "If Helm ignored a setting, check first for a misspelled or wrong values path. Render with and without the value, identify the correct path, or report that the chart does not expose the field.",
    instruction: "Remove each supplied values key one at a time, re-render, and report keys whose removal does not change the object set. Identify the correct value path when the chart exposes one.",
    issueOption: "ignored-values: I set a value. Why did the rendered object not change?",
    recommendation: "Use the chart's effective value path, or treat the requirement as a reviewed post-render change when the chart does not expose it.",
  },
  "upgrade-risk": {
    group: "common",
    label: "Can I upgrade this chart without breaking production?",
    answer: "Render both versions with the same release context. Compare objects, hooks, CRDs, immutable fields, storage, Secrets, and the current Helm release record when available.",
    instruction: "Render both versions with the same release context. Report deleted or renamed objects, immutable-field changes, storage changes, selector changes, Secret requirements, and lifecycle changes.",
    issueOption: "upgrade-risk: Can I upgrade this chart without breaking production?",
    recommendation: "Test the candidate against the retained current configuration, then promote it through a limited environment or rollout wave.",
  },
  "custom-field": {
    group: "common",
    label: "The chart does not expose the field I need. Must I fork it?",
    answer: "Show the rendered object and missing field. Keep the chart unchanged when a reviewed post-render edit is enough, then check that edit for overlap on upgrade.",
    instruction: "Confirm whether the chart exposes the requested field. If it does not, identify the exact rendered object and field, show the smallest post-render edit, and explain how an upgrade overlap would be detected.",
    issueOption: "custom-field: The chart does not expose the field I need. Must I fork it?",
    recommendation: "Keep the chart when possible and record the smallest object-level change as a derived configuration.",
  },
  "lifecycle-work": {
    group: "common",
    label: "How should Argo CD or Flux handle this chart's hooks and CRDs?",
    answer: "List each hook, CRD, and setup job in order. State who runs it, what must already exist, and which controller path has been tested.",
    instruction: "Identify Helm lifecycle work, explain its order, and separate work performed by Helm from work required by kubectl, Argo CD, or Flux. Cite controller-specific evidence when it exists.",
    issueOption: "lifecycle-work: How should Argo CD or Flux handle this chart's hooks and CRDs?",
    recommendation: "Choose an explicit owner and order for every prerequisite and lifecycle action before delivery.",
  },
  "rollback-history": {
    group: "common",
    label: "Can I roll back to exactly what ran before?",
    answer: "Find a retained rendered revision or OCI digest and restore those exact objects. Keep database migrations and other external effects outside the configuration claim.",
    instruction: "Identify which prior rendered objects and package digests are actually retained. Do not treat a version label or a fresh re-render as historical proof. Name external effects that object rollback cannot reverse.",
    issueOption: "rollback-history: Can I roll back to exactly what ran before?",
    recommendation: "Restore a retained object set or OCI digest, and handle external state with its own recovery plan.",
  },
  "config-diff": {
    group: "common",
    label: "How is this candidate different from production?",
    answer: "Compare the two exact object sets and their source records. Keep desired-configuration differences separate from live-cluster drift.",
    instruction: "Load both configurations with their exact identities. Report added, removed, and changed objects and fields, plus lifecycle or prerequisite differences. Separate a desired-config diff from live cluster drift.",
    issueOption: "config-diff: How is this candidate different from production?",
    recommendation: "Review and approve the exact desired-config diff, then check live state separately after delivery.",
  },
  "fleet-image": {
    group: "common",
    label: "Where does this vulnerable image run, and how can I update it safely?",
    answer: "Search supplied estate or ConfigHub records, show every affected environment and cluster, test the candidate, and promote it through a limited rollout wave.",
    instruction: "Inventory every supplied desired configuration and live observation for the named image or digest. Show affected environments and targets, the proposed object changes, the test plan, rollout scope, and rollback boundary.",
    issueOption: "fleet-image: Where does this vulnerable image run, and how can I update it safely?",
    recommendation: "Use ConfigHub or another complete estate inventory to scope the change, then test and roll it out in controlled waves.",
  },
  "install-shape": {
    group: "additional",
    label: "What will this install, and what must already exist?",
    answer: "List the rendered objects and every required namespace, Secret, CRD, API, storage class, cloud service, hook, and setup job.",
    instruction: "List every rendered object and identify required namespaces, Secrets, CRDs, APIs, storage, cloud services, hooks, and setup jobs.",
    issueOption: "install-shape: What will this install, and what must already exist?",
    recommendation: "Provide or route every prerequisite, then deliver only the reviewed object set.",
  },
  "supply-drift": {
    group: "additional",
    label: "Do these version and digest records identify the same bytes?",
    answer: "Record the available chart and package digests and compare them with retained, dated evidence.",
    instruction: "Record the chart and package digests available now. Compare them with retained digests and dated republish evidence when it exists.",
    issueOption: "supply-drift: Do these version and digest records identify the same bytes?",
    recommendation: "Use an immutable digest for the reviewed input and retain the source record with the result.",
  },
};
