interface:
  display_name: "Config Workshop"
  short_description: "Inspect, compare, and retain Kubernetes configuration"
  default_prompt: "Check this configuration against the Config Workshop Catalog, show the exact objects and lifecycle work, and produce a reviewed result."
