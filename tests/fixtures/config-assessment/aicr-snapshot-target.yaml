measurements:
  - type: OS
    subtypes:
      - subtype: kernel-cmdline
        data:
          cmdline: quiet splash
      - subtype: kernel-modules
        data:
          nvidia_peermem: absent
  - type: GPU
    subtypes:
      - subtype: hardware
        data:
          model: L40
          count: 1
