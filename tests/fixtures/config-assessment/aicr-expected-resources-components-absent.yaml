apiVersion: catalog.confighub.com/v1alpha1
kind: ConfigAssessmentFixture
metadata:
  name: aicr-expected-resources-components-absent
spec:
  check: expected-resources
  sourceIntentPresent: true
  destinationAccessPresent: true
  selectedConfigurationDeployed: false
  expectedClassification:
    evidenceState: blocked
    resultState: not-run
    reason: The recipe-declared components are absent, so expected resource health cannot be evaluated.
