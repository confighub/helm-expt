#!/usr/bin/env bash
# cloudpirates/redis 0.34.11 - base variant: reuse-existing-secret
# Path: pull the package, render this base variant locally, read the objects,
# then apply them with kubectl. No ConfigHub account is needed.
# Generated by scripts/generate-public-site.mjs from the committed package
# data for this base variant. Chart page: https://confighub.github.io/helm-expt/site/charts/cloudpirates-redis-0-34-11.html
set -euo pipefail

say() { printf '\n>> %s\n' "$*"; }

if ! command -v cub >/dev/null 2>&1; then
  printf 'cub is not installed. Install the cub CLI with:\n  curl -fsSL https://hub.confighub.com/cub/install.sh | bash\nthen add ~/.confighub/bin to your PATH and re-run this script.\n' >&2
  exit 1
fi

# Installer is a cub plugin. Check it before this script does any work.
if ! cub installer --help >/dev/null 2>&1; then
  printf 'The cub installer plugin is missing. Follow https://confighub.github.io/helm-expt/site/try.html#install-cub, then re-run this script.\n' >&2
  exit 1
fi

if ! command -v kustomize >/dev/null 2>&1; then
  printf 'kustomize is missing. Follow https://confighub.github.io/helm-expt/site/try.html#install-cub, then re-run this script.\n' >&2
  exit 1
fi

if ! command -v kubectl >/dev/null 2>&1; then
  printf 'kubectl is required for the apply step.\n' >&2
  exit 1
fi

say "Pull the package and render the reuse-existing-secret base variant into ./cloudpirates-redis-0-34-11-reuse-existing-secret"
cub installer setup --pull oci://europe-west1-docker.pkg.dev/nth-fort-499605-q5/helm-expt/cloudpirates-redis:0.34.11@sha256:29a96a9f6b3e2cedfe30cefe20c51e46cbcb1e363c69c45c58f9118530099333 --base reuse-existing-secret --work-dir ./cloudpirates-redis-0-34-11-reuse-existing-secret --non-interactive --namespace default

say "Read what was rendered; nothing has touched the cluster yet"
ls ./cloudpirates-redis-0-34-11-reuse-existing-secret/out/manifests

say "Ensure the default namespace exists"
kubectl create namespace default --dry-run=client -o yaml | kubectl apply -f -

if [ "${REQUIREMENTS_READY:-0}" != "1" ]; then
  cat >&2 <<'EOF_REQUIREMENTS'
This base variant needs resources you must create with your own values first:
  - Secret default/redis-existing-secret key redis-password
    kubectl -n default create secret generic redis-existing-secret --from-literal=redis-password=<value>
Complete these prerequisites before applying the rendered objects.
Replace any <...> placeholders with values or files for your environment.
When the resources exist, re-run with:
  REQUIREMENTS_READY=1 bash try.sh
EOF_REQUIREMENTS
  exit 1
fi

if [ -d ./cloudpirates-redis-0-34-11-reuse-existing-secret/out/secrets ]; then
  say "Apply rendered Secrets first"
  kubectl apply -f ./cloudpirates-redis-0-34-11-reuse-existing-secret/out/secrets
fi

say "Apply the rendered objects"
kubectl apply -f ./cloudpirates-redis-0-34-11-reuse-existing-secret/out/manifests

say "Done. The cluster received exactly the files in ./cloudpirates-redis-0-34-11-reuse-existing-secret/out."
