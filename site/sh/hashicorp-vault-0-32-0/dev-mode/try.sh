#!/usr/bin/env bash
# hashicorp/vault 0.32.0 - base variant: dev-mode
# Path: pull the package, render this base variant locally, read the objects,
# then apply them with kubectl. No ConfigHub account is needed.
# Generated by scripts/generate-public-site.mjs from the committed package
# data for this base variant. Chart page: https://confighub.github.io/helm-expt/site/charts/hashicorp-vault-0-32-0.html
set -euo pipefail

say() { printf '\n>> %s\n' "$*"; }

if ! command -v cub >/dev/null 2>&1; then
  printf 'cub is not installed. Install the cub CLI with:\n  curl -fsSL https://hub.confighub.com/cub/install.sh | bash\nthen add ~/.confighub/bin to your PATH and re-run this script.\n' >&2
  exit 1
fi

# Installer is a cub plugin. Check it before this script does any work.
if ! cub installer --help >/dev/null 2>&1; then
  printf 'The cub installer plugin is missing. Follow https://confighub.github.io/helm-expt/site/try.html#install-cub, then re-run this script.\n' >&2
  exit 1
fi

if ! command -v kustomize >/dev/null 2>&1; then
  printf 'kustomize is missing. Follow https://confighub.github.io/helm-expt/site/try.html#install-cub, then re-run this script.\n' >&2
  exit 1
fi

if ! command -v kubectl >/dev/null 2>&1; then
  printf 'kubectl is required for the apply step.\n' >&2
  exit 1
fi

say "Pull the package and render the dev-mode base variant into ./hashicorp-vault-0-32-0-dev-mode"
cub installer setup --pull oci://europe-west1-docker.pkg.dev/nth-fort-499605-q5/helm-expt/hashicorp-vault:0.32.0@sha256:7216f8cbbb3e2dafe3ed987f1e4b6fa962de60b2bf5ba7d2d0b7eb45eb138292 --base dev-mode --work-dir ./hashicorp-vault-0-32-0-dev-mode --non-interactive --namespace vault

say "Read what was rendered; nothing has touched the cluster yet"
ls ./hashicorp-vault-0-32-0-dev-mode/out/manifests

say "Ensure the vault namespace exists"
kubectl create namespace vault --dry-run=client -o yaml | kubectl apply -f -

if [ -d ./hashicorp-vault-0-32-0-dev-mode/out/secrets ]; then
  say "Apply rendered Secrets first"
  kubectl apply -f ./hashicorp-vault-0-32-0-dev-mode/out/secrets
fi

say "Apply the rendered objects"
kubectl apply -f ./hashicorp-vault-0-32-0-dev-mode/out/manifests

say "Done. The cluster received exactly the files in ./hashicorp-vault-0-32-0-dev-mode/out."
