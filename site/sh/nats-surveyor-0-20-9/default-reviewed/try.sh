#!/usr/bin/env bash
# nats/surveyor 0.20.9 - base variant: default-reviewed
# Path: pull the package, render this base variant locally, read the objects,
# then apply them with kubectl. No ConfigHub account is needed.
# Generated by scripts/generate-public-site.mjs from the committed package
# data for this base variant. Chart page: https://confighub.github.io/helm-expt/site/charts/nats-surveyor-0-20-9.html
set -euo pipefail

say() { printf '\n>> %s\n' "$*"; }

if ! command -v cub >/dev/null 2>&1; then
  printf 'cub is not installed. Install the cub CLI with:\n  curl -fsSL https://hub.confighub.com/cub/install.sh | bash\nthen add ~/.confighub/bin to your PATH and re-run this script.\n' >&2
  exit 1
fi

# Installer is a cub plugin. Check it before this script does any work.
if ! cub installer --help >/dev/null 2>&1; then
  printf 'The cub installer plugin is missing. Follow https://confighub.github.io/helm-expt/site/try.html#install-cub, then re-run this script.\n' >&2
  exit 1
fi

if ! command -v kustomize >/dev/null 2>&1; then
  printf 'kustomize is missing. Follow https://confighub.github.io/helm-expt/site/try.html#install-cub, then re-run this script.\n' >&2
  exit 1
fi

if ! command -v kubectl >/dev/null 2>&1; then
  printf 'kubectl is required for the apply step.\n' >&2
  exit 1
fi

say "Pull the package and render the default-reviewed base variant into ./nats-surveyor-0-20-9-default-reviewed"
cub installer setup --pull oci://europe-west1-docker.pkg.dev/nth-fort-499605-q5/helm-expt/nats-surveyor:0.20.9@sha256:7d989d267c54979f5cfdc133ac9f83ea6fd520a649aa584651c60c46bc29ca4d --base default-reviewed --work-dir ./nats-surveyor-0-20-9-default-reviewed --non-interactive --namespace default

say "Read what was rendered; nothing has touched the cluster yet"
ls ./nats-surveyor-0-20-9-default-reviewed/out/manifests

say "Ensure the default namespace exists"
kubectl create namespace default --dry-run=client -o yaml | kubectl apply -f -

if [ -d ./nats-surveyor-0-20-9-default-reviewed/out/secrets ]; then
  say "Apply rendered Secrets first"
  kubectl apply -f ./nats-surveyor-0-20-9-default-reviewed/out/secrets
fi

say "Apply the rendered objects"
kubectl apply -f ./nats-surveyor-0-20-9-default-reviewed/out/manifests

say "Done. The cluster received exactly the files in ./nats-surveyor-0-20-9-default-reviewed/out."
